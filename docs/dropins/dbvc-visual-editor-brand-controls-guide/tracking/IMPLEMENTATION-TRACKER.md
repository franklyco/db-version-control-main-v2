# Implementation Tracker

Update this file with evidence-backed status. Do not mark a release complete from code existence alone; all release gates must pass.

## Status legend

- `Not started`
- `Discovery`
- `Planned`
- `In development`
- `In review`
- `QA`
- `Feature-gated production`
- `Generally available`
- `Complete`
- `Blocked`
- `Deferred`

## Program summary

| Release | Outcome | Status | Owner/session | Evidence link | Release gate |
|---|---|---|---|---|---|
| R0 | Discovery and corrected baseline | Complete | Codex R0 / 2026-08-14 | `../01-DISCOVERY-AND-CURRENT-STATE-REVIEW.md`; `EVIDENCE-LOG.md` | Complete at repository-reconciled planning checkpoint `5db4b40` |
| R1 | Media Manager scan/report | Complete (signed off) | Codex R1-A/R1-B/R1-C/R1-D/R1-E / 2026-08-16 | `MediaManager/`; `MediaManagerController`; Media Manager R1 tests; corrected R1-D fixture/mockup; E-030/E-032/E-035/E-038-E-052 | R1-A through R1-E complete; live-REST auth-enforcement proof (401 before resolution) and complete multi-owner candidate-traversal/raw-read scaling (no field-definition/capability/permalink N+1) complete. **R1 signed off by the user on 2026-08-16** with the residual gates (authenticated table-data runtime, real AT/VoiceOver, real Safari, large-list responsiveness, aggregate lint completion) explicitly accepted as documented risk (D-044) |
| R2 | Media Manager direct remediation | Implemented (R2-E1–E4 done; real-browser QA deferred) | Codex R2-A..R2-E4 / 2026-08-18 | Decisions D-044..D-052; `MediaManagerController`; `MediaFindingDescriptorBridge`; `MediaAssignmentService`; `media-manager-app.js`; R2-A..R2-C/R2-E/R2-E2/R2-E4 tests; R2-D/R2-E3 jsdom states; `MEDIA-MANAGER-RELEASE-NOTES-AND-ROLLBACK.md` | R2-A..R2-D complete after R1 sign-off. R2-E fully sequenced (D-049): **E1** journal/cache; **E2** security/stale/permission hardening (`VisualEditorMediaManagerR2E2Test` 9/67); **E3** single-active `wp.media` frame lifecycle + surgical no-reload DOM patch (RK-011, 4 jsdom); **E4** feature isolation — `VisualEditorMediaManagerR2E4Test` (4 tests) proves the kill switch closes every REST route when the MM flag, VE master flag, base capability, or auth is absent, plus a consolidated release-notes/rollback runbook. Full suite 755 with the same six inherited failures; agent docs 54/418/0. Remaining deferred: real-browser/AT QA (D-049) and the Persistent Media Index Phase 1 discovery |
| R2-F | Media Manager entity media inventory & replace | Implemented (Slices 1–3) | Codex R2-F / 2026-08-17 | Decision D-050; `releases/R2F-...`; RK-012; `MediaScanService`; `MediaScanReadModel`; `MediaFindingDescriptorBridge::resolveReplaceable`; `MediaAssignmentService::replace`; `VisualEditorMediaManagerR2FTest`; `VisualEditorMediaManagerR2FReplaceTest` | New phase (D-050 disabled the "no editing non-empty fields" exclusion). **Slice 1 complete**: `expandGroup` reports populated fields (`status: assigned`) with a sanitized preview (URL/alt/count), deduped against resolved findings, top-level list/counts unchanged. **Slice 2 complete**: left-aligned lazy square thumbnail per field with an accent placeholder for empty fields and a `+{count}` gallery badge. **Slice 3 complete**: gated replace via a dedicated `/replacement` route enforcing an expected-current-value fingerprint precondition (`valueRef` → `resolveReplaceable` → shared `applyMutation`), journaled/audited/cache-invalidated, never deleting attachments; 6 PHP tests/62 assertions + 3 jsdom cases; full suite 738 with the same six inherited failures; agent docs 54/418/0. Browser QA deferred per D-049 |
| R2-F fix | Group-nested ACF media field save | Corrected (live-confirmed) | Codex / 2026-08-18 | RK-013; `AbstractAcfResolver::writeGroupedFieldValue`/`readGroupedFieldValue`; `MediaFindingDescriptorBridge::buildDescriptor` (`group_write_path`); `VisualEditorMediaManagerGroupedFieldTest`; updated `VisualEditorMediaManagerR2ATest`; E-066/E-068 | A nested ACF Group subfield is stored prefixed (`{group}_{subfield}`) and must be written through the root group. The first fix (leaf-key selector) stopped the error but wrote to the wrong meta — no persistence (masked by R2-G's optimistic thumbnail). Corrected: write/read THROUGH the root group via `group_write_path`, with readback verification so a non-persisting write errors honestly. Scoped to Media Manager findings (main editor unchanged). Media Manager PHP 58 tests/1,613 assertions; full suite 742 with the same six inherited failures. **Live nested-group save re-test on the reporting site is pending.** The overlay "descriptor session unavailable" toast is a separate overlay subsystem, not touched by this work |
| R2-F Slice 4 | Post-assign replaceability | Implemented (D-052) | Codex / 2026-08-18 | `releases/R2F-...` Slice 4; D-052; E-071; `MediaScanReadModel::expandGroup`; R2-C/R1-C tests | Field report: a just-assigned (previously-missing) field projected as `resolved_or_changed` with a "refresh the scan" dead end and no replace control. Fixed: `expandGroup` projects a now-populated original finding as `assigned` (valueRef + replace) via `projectAssignedField`, incrementing `populated` not `resolvedOrChanged`; `resolved_or_changed` kept only for genuinely gone/ineligible fields. Read-model projection only. R2-C PHP + R1-C count assertions + R2-C jsdom updated to `assigned`; added replace-after-assign jsdom assertion. Full suite 751 with the same six inherited failures; jsdom 34/34; agent docs 54/418/0 |
| R2-G | Media Manager UX polish | Implemented (Slices 1–2) | Codex R2-G / 2026-08-18 | `releases/R2G-...`; `media-manager-app.js`; `media-manager.css`; `visual-editor-media-manager-state.test.cjs`; E-067 | Two presentation-only refinements (no new REST/mutation/PHP surface). **Slice 1**: the picked media populates the field's left thumbnail immediately after save (no reload), preferring the server reread preview and falling back to the client-held pick. **Slice 2**: the scan status panel + action buttons moved into the header as a compact strip (smaller labels/buttons), decluttering the results body. 2 new jsdom cases (30 total); a faithful static layout preview verified; R1-D invariant intact; lint clean; agent docs 54/418/0 |
| R2-H | Persistent Media Index (Phase 1) | **Complete** (Slices 1–5) | Codex R2-H / 2026-08-19 | D-053, D-054, D-055, D-056, D-057; `releases/R2H-PERSISTENT-MEDIA-INDEX-PHASE-1.md`; `MediaIndexStore`/`Projector`/`ReadModel`/`Invalidator`/`Reconciler`/`Scheduler`/`MediaIndexController`/`MediaIndexRebuildController`/`MediaIndexJsonExporter`; `AssetLoader` (`indexList` flag); `media-manager-app.js`/`api-client.js` (index source); `VisualEditorMediaIndex*Test`; E-073..E-082 | New phase (D-053): durable, cross-user, incrementally-maintained media-field-completeness index (custom table + JSON export; Action Scheduler + WP-Cron fallback; read-time per-user eligibility filtering). **Slices 1–4**: table + store; completion-action population + read model with read-time filtering; incremental invalidation; background reconcile + scheduler. **Slice 2b (server plumbing)**: stable opaque `vemx_` entity refs + `getByEntityRef`; `MediaScanCoordinator::snapshotEntity` (detached per-entity snapshot, never clobbers "latest"); `MediaIndexController` with `GET .../index` + `POST .../index/expand`. **Slice 2c (frontend flip, D-054)**: index query parity (search/entity/field/sort over the index) + the flag-gated (`indexList`) open-from-index with `vemx_`-keyed rows, index-expand adopting the detached snapshot as the per-expansion working identity (mutations unchanged), and automatic fallback to the ephemeral scan on index error/empty. **Slice 4b-1 (D-055)**: `EligibilityPolicy` structural mode + `MediaIndexBuilder` chunked cross-user first-run build (structural enumeration, cursor across runs, scheduler-drained), and the completion hook rewired to `onScanRefreshed`/`refreshFromSnapshot` (refresh-in-place, no rotate/clobber) so the index is complete for all users. **Slice 4b-2 (D-056)**: schema v2 splits serving/building generations so a topology/exclusion rebuild is atomic — `MediaIndexStore::beginRebuild()` mints a fresh building generation the builder writes into (reads keep serving the OLD generation until the drain completes), `MediaIndexBuilder` calls `completeRebuild()` on its final chunk to atomically swap the serving pointer and prune every other generation, and `MediaIndexInvalidator` dual-writes into both generations so mid-rebuild saves survive the swap. `MediaIndexRebuildController` wires the triggers: `acf/update_field_group`/`_delete_field_group` (+ trash/untrash), the two exclusion options, and a `wp_loaded` topology fingerprint check that catches post-type/taxonomy (de)registration. **Slice 5 (D-057)**: `MediaIndexJsonExporter` writes a derived JSON mirror at `{sync}/visual-editor/media-index.json` on every completion boundary (build completion, rebuild swap, reconcile sweeps) — only the serving generation, envelope `{schema, exported_at, source, generation, count, entities}`; a guarded importer hydrates an empty table from the mirror on bootstrap (adopts the mirror's generation, marks the builder complete), never overwriting a populated table. 6+5+5+4+5+5+5+4+5 PHP + 4 jsdom tests; full suite 799 with the same six inherited failures; jsdom 38; agent docs 54/431/0 |
| R3 | Registry-backed Brand Control Center | **Complete** (R3-A + R3-BX + R3-B + R3-C-1 + R3-C-2 + R3-D shipped; R4/VerticalControlProvider are separate follow-ons) | Codex R3-A + R3-BX + R3-B + R3-C-1 + R3-C-2 + R3-D / 2026-08-23..29 | `releases/R3-REGISTRY-BACKED-BRAND-CONTROL-CENTER.md`; `addons/visual-editor/src/Registry/{ControlProvider,ControlRecord,ControlRegistry}.php`; `addons/visual-editor/src/Registry/Providers/SharedGlobalsControlProvider.php`; `addons/visual-editor/src/Curation/{FieldCandidateProvider,FieldCurationRecommender,CurationStore,CurationExporter}.php`; `addons/visual-editor/src/Admin/CurationPage.php`; `addons/visual-editor/src/Bootstrap/Addon.php`; `addons/visual-editor/bootstrap.php`; `tests/phpunit/VisualEditorControlRegistryTest.php`; `tests/phpunit/VisualEditorSharedGlobalsControlProviderTest.php`; `tests/phpunit/VisualEditorCuration*Test.php`; E-088; E-089; E-094; D-063 | **R3-A (2026-08-23)**: provider-agnostic control registry foundation — `ControlProvider` interface, `ControlRecord` value object with whitelisted normalization + safe list projection (no `source` bag on the wire), `ControlRegistry` collects/validates/dedupes/filters-by-user-visibility with deterministic ordering. Duplicate providers, duplicate local ids within a provider, and malformed records are all rejected observably via `dbvc_visual_editor_control_registry_invalid`. `getVisibleRecord($publicId)` is the internal bridge R3-C will use for the open route. **No REST, no UI, no wiring in `Addon::register` yet** (nothing to serve until R3-B registers Shared Globals as the first provider). 11 PHP tests / 44 assertions; full suite 810 with six inherited failures + one pre-existing dirty-tree `ProposalDiffContractTest` failure (not R3-A caused); agent docs 54/432/0. **R3-B (2026-08-28)**: `SharedGlobalsControlProvider` (in `Registry/Providers/`) adapts the existing configured Shared Globals field list onto the registry as a discovery-only surface — headless, kill-switch gated (new `OPTION_CONTROL_CENTER_ENABLED` setting requiring the master Visual Editor switch AND the new switch), one `ControlRecord` per valid `relationship`/`post_object` field (`category=globals`, `ownerType=option`, `ownerSubtype=acf_options`, opaque `source={field_name, field_key}`, `meta.badge="Shared Global"`), with a `visibleTo` closure that reproduces `SharedGlobalFieldsController::canManageSharedGlobalOptions` verbatim. `Bootstrap\Addon::__construct` instantiates `ControlRegistry` unconditionally; `Bootstrap\Addon::register` registers the provider only when the kill switch is on; `getControlRegistry()` exposes the same instance for R3-C. `SharedGlobalFieldsController` stays intact — the popover keeps working today. `SETTINGS_VERSION` 5→6; R1A test assertion updated in place. 5 new PHP tests / 28 assertions; full suite 844 with same 7 pre-existing failures; agent docs 54/437/0 (3 extension-point hashes rotated by bootstrap.php line shifts, re-mapped). **R3-C-1 (2026-08-28)**: Brand Control Center backend + descriptor factory extraction. Extracted `SharedGlobalFieldsController::buildDescriptor` (and every private helper participating in the descriptor build) into a stateless `Registry\Providers\SharedGlobalsDescriptorFactory`; controller now delegates — popover route's public response shape unchanged (byte-identical key ordering preserved). `ControlProvider` interface widened with `buildDescriptor(ControlRecord, sessionId, pageContext): ?EditableDescriptor`; `SharedGlobalsControlProvider` implements it (re-resolves the ACF field via the existing seam, re-validates the type is still relationship/post_object, delegates to the factory). New `ControlRegistry::buildDescriptorForRecord()` keeps the private providers map encapsulated. Two new session-scoped REST controllers `ControlCenterListController` (`GET .../control-center/controls`) and `ControlCenterOpenController` (`POST .../control-center/open`) wired into `Rest\Routes` under the R3-B kill switch (D-063 stays authoritative); both fail closed on the standard axes (edit-mode inactive, session expired, unknown/malformed publicId, provider returns null, empty reference_post_types, capability denied, session-attach fails). `Rest\Routes::__construct` gained a `ControlRegistry` param; `Bootstrap\Addon::__construct` passes `$this->control_registry`. No frontend JS/CSS (drawer is R3-C-2). 10 new PHPUnit tests / 36 assertions; full suite 854 with the same 7 pre-existing failures; agent docs 54/439/0 (2 new REST routes mapped; 1 REST + 1 extension-point hash rotated). **R3-C-2 (2026-08-29)**: Brand Control Center drawer UI — production translation of the accepted mockup. New `assets/js/brand-control-center-app.js` (~900 lines) mirrors `media-manager-app.js`'s IIFE shape: `state` store, event delegation on `[data-dbvc-ve-control-center-action]`, `ensureRoot()` lazy DOM build, `mount()` wires document events + exposes `window.DBVCVisualEditorBrandControlCenter = {open, close, toggle, list, isOpen, getState}`. Covers the mockup's §4 state matrix (loading-initial / list / opening / opened / open-error / empty / empty-filtered / error). **Filtering is client-side** (tab / status / priority / field-family / search) — no round-trips (pinned decision #3). Row-focus continuity, single polite `role="status"` region, `prefers-reduced-motion` suppression. Rows carry ONLY `data-public-id` — no forbidden target attrs (schematic §6 invariant 2, jsdom-asserted). New `assets/css/control-center.css` — direct translation of the mockup's production selectors, uses existing `--dbvc-ve-*` tokens. One net-new `:root` token in `overlay.css`: `--dbvc-ve-z-drawer: 120015`. `AssetLoader` gained a `controlCenter` enqueue branch mirroring media-manager (kill-switch gated, filemtime-versioned, Bricks-exclusion inherited) + `controlCenter: {enabled, restBase}` bootstrap block + ~40 drawer i18n strings. `overlay-app.js` gained four surgical additions: `isControlCenterEnabled()`/`dispatchControlCenterEvent()` helpers, new dock toolbar entry after `shared-globals` (pinned decision #2), `handleToolbarClick` branch for `'control-center'`, and `bindControlCenterBridge()` — ONE document listener for `dbvc:visual-editor:absorb-descriptor` that reuses the existing internal `mergeSharedGlobalInventory` + `openToolbarDescriptorPanel` helpers so the panel behaves identically whether opened from the popover or the drawer (pinned decision #1). jsdom `tests/visual-editor-brand-control-center-state.test.cjs` = **14/14 pass**. Media-manager jsdom baseline preserved at **42/42** (the resume file's earlier claim of 43 was stale; corrected). Full PHPUnit unchanged at 854/7. Agent docs 54/439/0 (one filter hash rotated on `AssetLoader::enqueue` due to line shifts from the new `controlCenter` block; re-mapped). **R3-D (2026-08-29)**: production hardening — R3 core is now ship-ready. New `VisualEditorControlCenterHardeningTest` (7 tests) locks down the kill-switch registration guarantee (both REST routes registered only when both switches are on; absent when either off; mid-session flip immediately reflects on the next REST-init dispatch) and the Bricks Builder isolation (drawer assets short-circuit at `AssetLoader::enqueue` via `FrontendRuntimeGuard::shouldRunFrontend()` when `$_GET['bricks']='1'`). New `docs/dropins/dbvc-visual-editor-brand-controls-guide/releases/BRAND-CONTROL-CENTER-RELEASE-NOTES-AND-ROLLBACK.md` mirrors the Media Manager sibling: what shipped, verified feature gates cross-referenced to test names, side effects & boundaries, residual/deferred (browser QA per the Media Manager D-049 residual shape), 6-step rollback runbook + pre-flip verification recipe, verification snapshot. Full PHP suite 861 tests (was 854), same 7 pre-existing failures; drawer + Media Manager jsdom baselines preserved at 14/42; agent docs unchanged at 54/439/0. **Follow-ons (separate slices with their own gates)**: R4 (expanded drawer, UI-only) and the cross-repo VerticalControlProvider (reads the R3-BX curation JSON export, produces `ControlRecord[]` covering the 400 approved controls). |
| R4 | Expanded Global & Brand Control Center | **R4-A + R4-B + R4-C + R4-D-1 + R4-D-2 + R4-D-3 landed** (R4 arc COMPLETE + defensively hardened — backend + mockup + production drawer + save-status-strip + coexistence re-QA + long-label calibration + release-notes update + label overflow-wrap defense all shipped; R4-D-2 residual CLOSED 2026-09-03; R4-D-3 label overflow-wrap defense-in-depth LANDED 2026-09-04 via `.dbvc-ve-control-center__label` gaining `max-width: 100%` + `overflow-wrap: anywhere` in `addons/visual-editor/assets/css/control-center.css` — closes R4-D-2 QA report item 11's exotic-single-word overflow finding; R6 unblocked from every R4 concern. See E-129) | Codex R4-B + R4-A + R4-C-1a + R4-C-1b + R4-C-2 + R4-D-1 / 2026-08-29..31 | `docs/ui-mockups/dbvc-visual-editor/r4-expanded-brand-control-center/` (R4-B); `releases/R4-EXPANDED-GLOBAL-BRAND-CONTROL-CENTER.md` §R4-A checkpoint; `addons/visual-editor/src/Registry/{ControlRecord,ControlProvider,ControlRegistry}.php`; `addons/visual-editor/src/Registry/Providers/SharedGlobalsControlProvider.php`; `addons/visual-editor/src/Rest/Controllers/{ControlCenterListController,ControlCenterValueSummariesController}.php`; `addons/visual-editor/src/Rest/Routes.php`; `addons/visual-editor/src/Bootstrap/Addon.php`; `tests/phpunit/VisualEditorControlCenterR4ATest.php` (new, 19/60); Vertical repo `functions/features/dbvc-visual-editor/{dbvc-visual-editor.php, includes/class-vf-vertical-control-provider.php}`; `addons/visual-editor/assets/js/brand-control-center-app.js` (R4-C-1a — contract-consumer wiring); `addons/visual-editor/assets/css/control-center.css` (R4-C-1a — banner + label-block + description selectors); `addons/visual-editor/src/Assets/AssetLoader.php` (R4-C-1a — 4 new i18n keys + widened search placeholder); `tests/visual-editor-brand-control-center-state.test.cjs` (R4-C-1a — +7 jsdom cases, 14 → 21; R4-C-1b — +4 jsdom cases + IntersectionObserver polyfill, 21 → 25; R4-C-2 — +5 jsdom cases, 25 → 30; R4-D-1 — +3 jsdom cases, 30 → 33); R4-D-1 also touches `addons/visual-editor/assets/js/overlay-app.js` (new `dispatchPanelSaved` on save success paths) — first R4 slice to touch overlay-app.js; Vertical `themes/vertical/functions/features/dbvc-visual-editor/{dbvc-visual-editor.php,includes/class-vf-vertical-control-provider.php}` (R4-A live-site TypeError fix — `vf_field_context_get_entry_primary_purpose()` now receives resolved entry via `vf_field_context_get_entry_for_runtime_field($field_name, 'option')`, applied to both dbvc-codexchanges + frameworkflo-live checkouts); E-100; E-101; E-102; E-103; E-104; D-064 | **R4-B (2026-08-29)**: static mockup pinned 7 design decisions (view-mode toggle for the category system; description on muted second line; per-family safe value-summary shape; provider-error fail-soft; `sortKey ASC → label ASC → publicId ASC` global sort) and shipped six deliverables under `docs/ui-mockups/dbvc-visual-editor/r4-expanded-brand-control-center/` (README, DESIGN-DECISIONS, COMPONENT-NOTES, styles.css, index.html happy path with ~20 rows, states.html gallery with 13 state cells). **R4-D-1 (2026-08-31)**: save-status-strip — first R4-D sub-slice and first R4 slice to touch `overlay-app.js` in any way. `assets/js/overlay-app.js` — new `dispatchPanelSaved(token)` helper next to `dispatchControlCenterEvent`; gated behind `isControlCenterEnabled()` matching the existing dispatch policy so kill-switch-off callers dispatch nothing. Fires `dbvc:visual-editor:panel:saved` with detail `{token}` on BOTH `handleSave` success paths (composite + non-composite), placed AFTER the panel-state / status-bar updates and BEFORE the `shouldReloadAfterSave` `window.location.reload` so the drawer has one tick to render its strip before a page reload. `assets/js/brand-control-center-app.js` — `state.activeToken` stored in `openRow` alongside `activePublicId`; new `state.saveStatusStrip` + `state.saveStatusTimer` + `SAVE_STATUS_STRIP_MS = 2500` constant. New `handlePanelSaved(event)` gates on `token === state.activeToken` AND `state.activePublicId` non-empty; populates strip with `labelForPublicId(state.activePublicId)`; schedules the fade timer (clears any pending timer first so back-to-back saves reset the fade); calls `renderSaveStatusStrip()`; announces via the single polite live region (announcer stays authoritative; strip carries `aria-hidden="true"` to prevent AT double-announcement). New `renderSaveStatusStrip()` inserts / removes the `.__save-status-strip` at the top of the table wrap. `close()` clears the fade timer, nulls the strip state, and clears `activeToken` so a reopen starts clean. `renderList()` calls `renderSaveStatusStrip()` after `renderRefreshOverlay(wrap)` so a filter change / view-mode flip mid-fade preserves the confirmation. `publicState()` gains `activeToken` + `saveStatusStrip`. Document listener wired in `mount()` — one listener, added alongside the existing control-center:toggle / :close listeners. `assets/css/control-center.css` — new `.__save-status-strip` (sticky top:0, success-green background, `dbvc-ve-cc-save-status` keyframe fade over 2500ms) + reduced-motion block suppressing the animation. `src/Assets/AssetLoader.php` — 1 new i18n key `controlCenterSaveStatus = "Saved {label}."`. `tests/visual-editor-brand-control-center-state.test.cjs` — +3 R4-D-1 cases. **Cross-panel coexistence preserved**: the listener gates on `activeToken`, so a viewer with the drawer open who also uses the Shared Globals popover to edit a DIFFERENT descriptor sees NO strip in the drawer for that save (matches R3-C-2 D-063 coexistence). **Also landed same day (unrelated R4-A live-site bug fix)**: `themes/vertical/functions/features/dbvc-visual-editor/dbvc-visual-editor.php` + `.../includes/class-vf-vertical-control-provider.php` — both callsites of `vf_field_context_get_entry_primary_purpose()` were passing a string field name where the function requires the resolved entry array. Fixed by resolving the field name via `vf_field_context_get_entry_for_runtime_field($field_name, 'option')` first. Applied to both Vertical checkouts (dbvc-codexchanges + frameworkflo-live mirror). **Validation**: `node --test tests/visual-editor-brand-control-center-state.test.cjs` → **33 pass / 33 tests** (30 → 33). `node --test tests/visual-editor-media-manager-state.test.cjs` → **42 pass** (preserved — overlay-app.js addition is Control-Center-scoped, media-manager save flow untouched). `vendor/bin/phpunit --filter "VisualEditor(Control\|SharedGlobals)"` → **60 / 200 OK** (unchanged — R4-D-1 touched no backend). `composer agent-docs:refresh && composer agent-docs:check` → **54 / 443 / 0** (unchanged — 1 new i18n key added; no new hook or REST surface). No content mutated; no persistent WP option toggled outside test setUp/tearDown; no git operations either repo. **Follow-on**: R4-D-2 (real-browser QA at 1440×900 + 1280×720 with the 400-row registry, long-label truncation calibration, release-notes/rollback update). See E-104. **R4-C-2 (2026-08-31)**: visual polish + view-mode toggle + collapsible group headers + search-wrap DOM + invariant sweep. **R4-C now fully landed** (R4-C-1a + R4-C-1b + R4-C-2). `assets/js/brand-control-center-app.js` — biggest R4-C refactor. Added `DEFAULT_QUERY.provider` axis; module constants `VIEW_MODES`, `LS_KEY_VIEW_MODE`, `LS_KEY_EXPANDED_GROUPS`; state `viewMode` (default 'category') + `expandedGroups` (records DEVIATIONS from the collapsed-by-default rule so new fixture groups still collapse by default). localStorage helpers `readStoredString` / `writeStoredString` (try/catch-wrapped for private-window safety) + `loadStoredViewMode` (validates against `VIEW_MODES`) + `loadStoredExpandedGroups` (guards bad JSON) + `persistExpandedGroups`. `setViewMode(mode)` snapshots the active row's publicId BEFORE re-render, persists, restores focus in the new layout. `toggleGroup(groupKey)` flips the map entry, persists, re-renders. `clearSearchInput()` clears + fires `loadControls({reason:'query'})`. Provider helpers `providerIdFromPublicId` / `providerLabel` / `providersFromItems` / `tabAxisForViewMode` / `tabEntriesForViewMode` / `tabLabelForViewMode`. `ensureRoot()` rehydrates `viewMode` + `expandedGroups` from localStorage on first open. `createHeader()` inserts the `.__view-toggle` segmented control (`role="tablist"` with `aria-label="Category view"` for a11y disambiguation vs the category tablist below). `renderViewToggle()` keeps `aria-selected` + `tabIndex` in sync. `createFilters()` replaces the bare input with `.__search-wrap` (leading `.__search-icon` glyph + trailing `.__search-clear` button); `renderFilters()` toggles Clear visibility per `state.query.search === ''`. `handleClick` gains `set-view-mode` / `toggle-group` / `clear-search` branches; `select-tab` reads `data-tab-slug` with `data-category` / `data-provider` fallbacks (R3-C-2 tests remain compatible). `handleKeydown` gains `ArrowLeft` / `ArrowRight` nav on the view-toggle tablist. `renderTabs()` + `createTabButton()` are view-mode-aware. `selectTab()` routes into `state.query.category` or `state.query.provider` depending on `viewMode`. `itemMatchesFilters()` branches on `viewMode` — provider mode filters on `providerIdFromPublicId(item.publicId)`. `clearFilters()` preserves BOTH `category` and `provider` axes. **`createTableWrap()` no longer creates the R3-C-2 single `<tbody>` sentinel** — rows now live inside per-group `<tbody class="__group">` elements built by `renderList()`. New `clearTableTbodies(wrap)` / `buildGroupsFromVisible(visible)` (keys `{providerId}::{group}`, preserves first-appearance order — matches R4-A `sortKey → label → publicId` global sort) / `renderGroupTbody(group)` (emits tbody with `data-group-key` + `data-provider-id` + `is-collapsed` when not expanded) / `renderGroupHeaderRow(group, isExpanded)` (colspan-2 disclosure row with `.__group-toggle` `aria-expanded` + `aria-label` Expand/Collapse, chevron, title, count badge). `renderList()` overhauled — clears all tbodies via `clearTableTbodies`, then renders panel-state OR iterates `buildGroupsFromVisible` and appends one tbody per group. `registerVisibleValueSummaryTargets(wrap)` signature simplified — takes wrap only, queries rows at the wrap level so per-group tbodies are picked up. Dead `clearRows(tbody)` helper removed. `publicState()` gains `viewMode` + `expandedGroups`. `assets/css/control-center.css` — new `.__view-toggle*` (segmented control with `aria-selected="true"` bg swap, focus-visible ring) + `.__search-wrap` + `.__search-icon` + `.__search-clear` + `.__group-header*` + `.__group-toggle*` (chevron rotates 90deg on `[aria-expanded="true"]` via 160ms transition) + `.__group-title` + `.__group-count` + `.__group.is-collapsed .__row { display: none }` (semantic tree stays intact) + `@media (prefers-reduced-motion: reduce)` block suppressing chevron rotation + segmented-control transitions. `src/Assets/AssetLoader.php` — 11 new i18n keys (`controlCenterViewToggleLabel`, `…ViewByCategory`, `…ViewByProvider`, `controlCenterGroupExpand`, `…GroupCollapse`, `…GroupControlsCount`, `…GroupUnnamed`, `controlCenterProviderShared`, `…ProviderVertical`, `…ProviderUnknown`, `controlCenterClearSearch`). `tests/visual-editor-brand-control-center-state.test.cjs` — 5 new cases. Two assertions use `JSON.stringify` comparison instead of `deepStrictEqual` because jsdom's plain-object prototype differs from Node's plain-object prototype (documented inline). **All 7 pinned R4-B decisions honored end-to-end** (persistent search input; flat tab strip; collapsible group headers; description as muted second line; per-family value-summary chips; Shared Globals folded + view-mode toggle; view-mode toggle in drawer header with localStorage persistence). **Validation**: `node --test tests/visual-editor-brand-control-center-state.test.cjs` → **30 pass / 30 tests** (was 25/25 pre-R4-C-2 — 5 new cases). `node --test tests/visual-editor-media-manager-state.test.cjs` → **42 pass** (preserved). `vendor/bin/phpunit --filter "VisualEditor(Control\|SharedGlobals)"` → **60 tests / 200 assertions OK** (unchanged — R4-C-2 touched no backend). `composer agent-docs:refresh && composer agent-docs:check` → **54 curated / 443 discovered / 0 unmapped** (unchanged — 11 new i18n keys added; no new hook or REST surface). No content mutated; no persistent WP option toggled outside test setUp/tearDown; no git operations either repo. **`addons/visual-editor/docs/handoffs/DBVC_R4C_RESUME_PROMPT.md` archived to `archive/`** (R4-C fully landed; R4-D resume prompt is the next handoff artifact — not yet written). **Follow-ons**: R4-D (real-browser QA at 1440×900 + 1280×720 with the 400-row registry, long-label truncation calibration, save-status-strip that dispatches from `overlay-app.js` on successful save, release-notes + rollback update) and R5.x (per-family editing factories that flip Vertical rows from `unsupported` to `available`). See E-103. **R4-C-1b (2026-08-31)**: IntersectionObserver-driven batch value-summary loader that consumes the R4-A `POST .../control-center/value-summaries` endpoint. `assets/js/brand-control-center-app.js` — new `state.valueSummaries` (publicId → summary\|null\|`'loading'` cache, lifetime-scoped), `state.valueSummaryObserver` (lazy IO rooted on `.__table-wrap` with `threshold: 0`), `state.valueSummaryPending` (dedup queue), `state.valueSummaryFlushTimer` (50ms debounce). New helpers: `ensureValueSummaryState()` / `ensureValueSummaryObserver(wrap)` / `teardownValueSummaryObserver()` (called in `close()`, which also empties the cache so reopen re-hydrates); `handleValueSummaryIntersect(entries)` (unobserves each intersecting row immediately, short-circuits cached publicIds, pushes fresh ones into the pending queue, schedules a flush); `scheduleValueSummaryFlush()` (50ms); `valueSummariesUrl()` builder; `flushValueSummaries()` (batch cap **20** per flush — D-064 server cap is 50 as headroom — marks `'loading'` before POST, chains a follow-up flush if the queue still has entries, fail-soft collapses to `null` on network error so a broken endpoint cannot create a retry loop); `patchValueSummarySlot(publicId)` (surgical per-row DOM patch — mirrors Media Manager R2-E3's per-row patch pattern; no full `renderList()` so focus + sibling observer registrations survive); `renderValueSummaryChip(summary)` (family dispatcher — only `relationship` + `post_object` render for R4-C-1b as `<strong>{count}</strong> connected` with `title="{firstTitles.join(', ')}"`; every other family and every `null` summary returns no chip → empty slot; R5.x owns the remaining chip shapes); `registerVisibleValueSummaryTargets(wrap, tbody)` (called at the end of `renderList()`, observes only rows whose `data-status="available"` AND whose publicId is unhydrated so filter changes + rerenders re-attach without thrashing the endpoint). `renderRow()` gained a `.__value-summary[data-public-id]` slot before the Open button via new `renderValueSummarySlot(item, status, publicId)` — cached summaries render synchronously, non-`available` rows render empty slots. `close()` extended to call `teardownValueSummaryObserver()` and null-out the caches. `publicState()` extended with `valueSummaries` for test observability. **Graceful degrade**: `typeof window.IntersectionObserver !== 'function'` short-circuits — rows render empty slots, no fallback fetch. `assets/css/control-center.css` — new `.__value-summary` (inline-flex chip container, `max-width: 200px`, ellipsis, `:empty { display: none }` so unhydrated / null rows do not claim visual space) + `.__value-relationship` (count + label chip). `src/Assets/AssetLoader.php` — 1 new i18n key `controlCenterValueRelationshipConnected`. `tests/visual-editor-brand-control-center-state.test.cjs` — minimal `IntersectionObserver` polyfill (`window.__ioRegistry.triggerAll()` / `trigger(predicate)`; honors `unobserve` + `disconnect` matching real browser behavior) + 4 new cases. **Validation**: `node --test tests/visual-editor-brand-control-center-state.test.cjs` → **25 pass / 25 tests** (was 21/21 pre-R4-C-1b — 4 new cases). `node --test tests/visual-editor-media-manager-state.test.cjs` → **42 pass** (preserved). `vendor/bin/phpunit --filter "VisualEditor(Control\|SharedGlobals)"` → **60 tests / 200 assertions OK** (unchanged — R4-C-1b touched no backend). `composer agent-docs:refresh && composer agent-docs:check` → **54 curated / 443 discovered / 0 unmapped** (unchanged — one i18n key added; no new hook or REST surface). No content mutated; no persistent WP option toggled outside test setUp/tearDown; no git operations either repo. **R4-A backend surface now fully consumed by production code** — `GET .../controls?family=&q=` (R4-C-1a) + `POST .../value-summaries` (R4-C-1b) + `POST .../open` (R3-C-2, unchanged). R4-C-2 is purely presentational. See E-102. **R4-C-1a (2026-08-31)**: production drawer starts consuming the R4-A backend contract — smallest sub-slice of the confirmed R4-C-1a/1b/2 split. `assets/js/brand-control-center-app.js` — `priorityFromItem()` rewritten to derive `must`/`should`/`nice` from the `sortKey` prefix `vertical_[1|2|3]_*` (the pre-R4-A `item.priority` fallback never populated against real R4-A data); `listUrl(params)` encodes `?family=…&q=…`; `loadControls({reason})` supports `initial`/`query`/`retry` and sets `state.requestStatus='loading-refresh'` for non-initial requests when items are already loaded — keeps the existing rows visible under the previously-unused `.__refresh-overlay`; `handleInput` search debounce widened 180 → **250ms**; `toggleChip` fires an immediate round-trip when the axis is `fieldFamily`, stays client-side for `status`/`priority`; `itemMatchesFilters` drops the search + `fieldFamily` branches (server owns them); `clearFilters` fires a round-trip only when clearing removed a server-scoped param; new `renderProviderErrorBanner()` inserts `.__notice--provider-error` (`role="status"`, dismiss button) between the filter strip and the table wrap when the R4-A `payload.providerErrors` map is non-empty and not dismissed; new `renderRefreshOverlay()` adds/removes the dimmed overlay based on request status; new `dismissProviderErrors()` handler; `renderRow()` wraps the label in a new `.__label-block` and appends a `.__description` `<p>` (`textContent`, never innerHTML) only when the record ships `description !== ''`; `close()` resets `providerErrorsDismissed` so a fresh open re-surfaces still-throwing providers; `publicState()` exposes the new fields for test observability. `assets/css/control-center.css` — new `.__notice--provider-error` (warning-tone banner), `.__label-block` (flex column), `.__description` (muted, 2-line `-webkit-line-clamp`). `src/Assets/AssetLoader.php` — widened `controlCenterSearchPlaceholder` to "Search labels, descriptions, owner…" matching the R4-A `q` semantics; 4 new i18n keys (`controlCenterRefreshing`, `controlCenterProviderErrorSingular`, `controlCenterProviderErrorPlural`, `controlCenterProviderErrorDismiss`). `tests/visual-editor-brand-control-center-state.test.cjs` — 7 new jsdom cases + `sleep()` + `extractQuery()` helpers. **Pinned in-slice decisions (D-064 refinements, not new DECISION-LOG rows)**: mixed filter model (`family` + `q` server-side; `status` + `priority` + `category` client-side to keep tab counts cheap); priority chip stays client-side derived from `sortKey`; save-status-strip deferred to R4-D (overlay-app.js dispatch straddles R3-C-2 baseline); IntersectionObserver-unavailable in R4-C-1b will render empty slots silently. **Validation**: `node --test tests/visual-editor-brand-control-center-state.test.cjs` → **21 pass / 21 tests** (14 → 21). `node --test tests/visual-editor-media-manager-state.test.cjs` → **42 pass** (preserved). `vendor/bin/phpunit --filter "VisualEditor(Control\|SharedGlobals)"` → **60 tests / 200 assertions OK** (unchanged from R4-A landing — R4-C-1a touched no backend). `composer agent-docs:refresh && composer agent-docs:check` → **54 curated / 443 discovered / 0 unmapped** (unchanged — AssetLoader i18n additions do not rotate hook or REST hashes). No content mutated; no persistent WP option toggled outside test setUp/tearDown; no git operations either repo. **Follow-ons**: R4-C-1b (IntersectionObserver-driven batch value-summary loader against the R4-A `POST .../value-summaries` endpoint), R4-C-2 (view-mode toggle + collapsible group headers + search-wrap + state-gallery + invariant sweep). See E-101 for the full row. **R4-A (2026-08-30)**: expanded backend consumed by the R4-C drawer. `ControlRecord` widened with `description` + `sortKey` (both optional, both round-tripped through `fromArray` + `toListItem`); `ControlProvider` widened with `buildValueSummary(ControlRecord, sessionId): ?array`. `ControlRegistry`: `listControls()` accepts new `family` + `q` args; sort switched to `sortKey ASC → label ASC (case-insensitive) → publicId ASC` globally (previously per-provider); provider `getControls()` wrapped in try/catch with new `REJECT_PROVIDER_THREW` observer reason + `getProviderErrors()` accessor; new `buildValueSummaryForRecord()` delegator. `SharedGlobalsControlProvider`: emits `description` via new `dbvc_visual_editor_control_center_description` filter → ACF `instructions` fallback → empty; emits `sortKey` `shared_{fieldName}`; real `buildValueSummary()` for relationship/post_object families returning `{family, count, firstTitles≤3, hasMore}` with option-owned capability recheck; new 4th optional `$optionValueResolver` constructor seam (production wires `get_field($name, 'option', false)`). `ControlCenterListController`: `viewModelVersion` bumped 1→2, `query` echoes all four params, new `providerErrors` map surfaces alongside `items`, `q` clamped at 128 chars. New `ControlCenterValueSummariesController` route `POST .../session/{id}/control-center/value-summaries` body `{publicIds}` batch cap 50, per-record capability recheck via descriptor → summary or null. Wired under the R3-D two-part kill switch. Cross-repo: Vertical's provider emits `description` via `vf_field_context_get_entry_primary_purpose()` → curation `notes` → empty; emits `sortKey` `vertical_{must=1|should=2|nice=3|empty=9}_{fieldName}`; hooks the DBVC description filter so Shared Globals rows also pick up Field Context text. **Validation**: `vendor/bin/phpunit --filter "VisualEditorControlCenterR4A"` → **19 tests / 60 assertions OK**. Full PHPUnit **888 tests, 7 failures** (was 869/7 pre-R4-A — same 7 pre-existing unrelated failures; the 19 delta matches the new R4-A suite). No new mutation authority, no new frontend UI (drawer is R4-C), kill switch still default off. **Follow-ons**: R4-C (production drawer consumes `viewModelVersion=2`, new params, `providerErrors` banner, batch value-summaries endpoint), R4-D (Shared Globals transition + hardening). |
| R5.1 | Scalar ACF option fields | **R5.1 complete** (R5.1-a DBVC text-family + R5.1-b Vertical cross-repo unlock — 182 curated group-nested text-family Vertical records now flip from Unsupported to Available on the live site) | Codex R5.1-a + R5.1-b / 2026-08-31..2026-09-01 | `addons/visual-editor/src/Registry/Providers/SharedGlobalsControlProvider.php` (widened getControls whitelist via new `supportedFieldTypes()` + `mapFieldFamily()` static helpers; buildValueSummary text branch); `addons/visual-editor/src/Registry/Providers/SharedGlobalsDescriptorFactory.php` (new `buildSharedFieldDescriptor()` private helper for text-family; relationship path byte-identical); `addons/visual-editor/assets/js/brand-control-center-app.js` (renderValueSummaryChip text branch); `addons/visual-editor/assets/css/control-center.css` (`.__value-text` + `.__value-text-count`); `addons/visual-editor/src/Assets/AssetLoader.php` (`controlCenterValueTextTruncated`); `tests/phpunit/VisualEditorControlCenterR51ATest.php` (new, 15/124); `tests/phpunit/VisualEditorSharedGlobalsControlProviderTest.php` (renamed skip test to use color_picker); `tests/visual-editor-brand-control-center-state.test.cjs` (+2 R5.1-a text-chip cases, 33 → 35); `docs/agents/manifest.json` (rotated description-filter discovery id `5d884d1f → c6dfb57c` from line-shift of new static helpers); E-105 | **R5.1-b (2026-09-01)**: Vertical cross-repo unlock — 182 curated group-nested text-family Vertical records flip from Unsupported → Available on the live site. **DBVC**: `addons/visual-editor/src/Registry/Providers/SharedGlobalsDescriptorFactory.php` — extended `buildSharedFieldDescriptor()` with a new private `walkGroupChain($leaf_field)` helper that walks the ACF `parent` chain via `acf_get_field()` from leaf up to a `group_XXX` field-group boundary. When the chain is non-empty, source gets `group_write_path` + `is_grouped_field=true` + root `field_selector` (`AbstractAcfResolver::writeGroupedFieldValue` reads these to route the save THROUGH the parent group's own `update_value` — Media Manager R2-F RK-013 pattern proven at group-nested image scale). Top-level shared globals fields see an empty chain and fall through unchanged. **Vertical (cross-repo, both checkouts)**: `themes/vertical/functions/features/dbvc-visual-editor/includes/class-vf-vertical-control-provider.php` — `mapRecord` computes `$status` via new private `resolveStatus($unlocks_at, $field_type)` (`'available'` when `unlocks_at==='R5.1'` AND `field_type ∈ [text,textarea,url,email,number]`; else `'unsupported'`). Source projection widened with `field_type` + `unlocks_at`. `buildDescriptor` — for `record->status==='available'` records: gates on `get_field_object` availability, lifts the leaf ACF field object via `get_field_object($field_key, 'option', false, false)` (key-based lookup handles nested paths), guards against ACF returning a group instead of leaf (`resolved_type` must be text-family), delegates to DBVC's `SharedGlobalsDescriptorFactory`. New lazy `descriptorFactory()` + `setDescriptorFactory()` test-seam. `buildValueSummary` mirrors DBVC's text summary shape — gates on available + text-family + `get_field`, reads via `get_field($field_key, 'option', false)` (unwraps group-subfield prefixed meta). Mirrored byte-identically to both Vertical checkouts (dbvc-codexchanges + frameworkflo-live) verified via `diff -q`. **Tests**: `tests/phpunit/VisualEditorVerticalProviderR51BTest.php` (new, 9/34) — loads the Vertical class file via a guarded `require_once` with `markTestSkipped` fallback for CI environments without the theme checkout, then covers the pure-PHP claims: mapRecord flips text-family R5.1 records to `available` across a 5-type `@dataProvider`; non-text-family R5.1 records stay unsupported (deliberate curation-mismatch case); text-family records tagged for later R5.x stay unsupported; buildDescriptor null on unsupported; buildValueSummary null on unsupported. Happy-path buildDescriptor + buildValueSummary need real ACF and are exercised at runtime on the live site. **Validation**: `vendor/bin/phpunit --filter "VisualEditorVerticalProviderR51B"` → **9 / 34 OK**. `vendor/bin/phpunit --filter "VisualEditor(Control\|SharedGlobals\|Vertical)"` → **84 / 358 OK** (was 75/324 — +9 R5.1-b new). `node --test tests/visual-editor-brand-control-center-state.test.cjs` → **35 pass** (preserved — R5.1-b touched no drawer JS/CSS). `node --test tests/visual-editor-media-manager-state.test.cjs` → **42 pass** (preserved). `composer agent-docs:check` → **54 / 443 / 0** (unchanged — no new hook or REST surface). No content mutated; no persistent WP option toggled outside test setUp/tearDown; no git operations either repo. **R5.1 is now fully landed.** Next: R5.2 (choice/link/wysiwyg families — 157 records; needs new resolver? verify. AcfChoiceResolver + AcfLinkResolver + AcfWysiwygResolver already exist per Resolvers/ dir), or R5.2+color_picker (20 records; would need a color_picker resolver — not yet in Resolvers/). See E-106. **R5.1-a (2026-08-31)**: DBVC-side text-family unlock. Zero new REST routes, zero new mutation authority, zero new resolvers, zero new mutation contracts — this is a pure "wire the existing AcfTextResolver + shared_field mutation contract through the drawer" slice. The AcfTextResolver has supported text/textarea/url/email/number since before R3; MutationContractService::isWritable has whitelisted `shared_field` in that same slice. R5.1-a widens SharedGlobalsControlProvider's field-type whitelist (now via `supportedFieldTypes()`) to cover the AcfTextResolver families; adds `mapFieldFamily()` to collapse text/textarea/url/email/number onto the ControlRecord `'text'` bucket; teaches SharedGlobalsDescriptorFactory to branch on `$field_type` and mint a `shared_field` contract descriptor (`source.type='acf_field'`, `ui.input='text'|'textarea'`, `resolver.name='acf_text'`, `mutation.contract='shared_field'`, `kind='scalar'`, `target='field'`, `renderContext='field'`) for text-family types while leaving the relationship / post_object shape byte-identical; widens `buildValueSummary` to emit `{family:'text', preview, charCount, truncated}` with preview capped at 32 chars via `mb_substr` (empty / non-scalar → null); adds the frontend `family === 'text'` chip renderer with an optional `.__value-text-count` truncation suffix; adds `.__value-text*` CSS + one i18n key. **Validation**: `vendor/bin/phpunit --filter "VisualEditorControlCenterR51A"` → **15 tests / 124 assertions OK**. `vendor/bin/phpunit --filter "VisualEditor(Control\|SharedGlobals)"` → **75 tests / 324 assertions OK** (was 60/200 — +15 R5.1-a new; 1 pre-existing R3-B test renamed + refactored to use a still-unsupported color_picker family since the old `type=text` fixture now surfaces a real record). `node --test tests/visual-editor-brand-control-center-state.test.cjs` → **35 pass / 35 tests** (33 → 35). `node --test tests/visual-editor-media-manager-state.test.cjs` → **42 pass** (preserved). `composer agent-docs:refresh && composer agent-docs:check` → **54 curated / 443 discovered / 0 unmapped** (one description-filter discovery id rotated from line-shift; re-mapped). No content mutated; no persistent WP option toggled outside test setUp/tearDown; no git operations either repo. **Follow-on**: R5.1-b (Vertical cross-repo: flip `unlocks_at="R5.1"` curated records from `status="unsupported"` to `status="available"` and wire VerticalControlProvider::buildDescriptor to delegate to DBVC's SharedGlobalsDescriptorFactory — 182 rows visible on the maintainer's site after landing). See E-105. |
| R5.2 | Choice/link/WYSIWYG option fields (+ color_picker + true_false sub-slices) | **R5.2 complete + R5.2+color_picker complete + true_false complete** (R5.2 a+b: 155 records; R5.2+color_picker: new AcfColorPickerResolver + 20 records; true_false: new AcfTrueFalseResolver + new createBooleanController + 2 records. Combined 177 Vertical records flip Unsupported → Available for R5.2 tags — every curated R5.2 family now editable) | Codex R5.2 + R5.2+color_picker + true_false / 2026-09-01 | `addons/visual-editor/src/Registry/Providers/SharedGlobalsControlProvider.php` (widened whitelist + 5 new static summary builders); `addons/visual-editor/src/Registry/Providers/SharedGlobalsDescriptorFactory.php` (config-bag refactor + choice / link / wysiwyg branches + `projectAcfChoices` helper); `addons/visual-editor/assets/js/brand-control-center-app.js` (3 new family branches in renderValueSummaryChip); `addons/visual-editor/assets/css/control-center.css` (`.__value-choice*`, `.__value-link*`, `.__value-wysiwyg*`); `addons/visual-editor/src/Assets/AssetLoader.php` (2 new i18n keys); Vertical (both checkouts) `themes/vertical/functions/features/dbvc-visual-editor/includes/class-vf-vertical-control-provider.php` (resolveStatus R5.2 branch + buildDescriptor widened supported list + buildValueSummary delegation to DBVC static builders); `tests/phpunit/VisualEditorControlCenterR52Test.php` (new, 20/90); `tests/phpunit/VisualEditorVerticalProviderR51BTest.php` (+7 R5.2 dataProvider cases + true_false-stays-unsupported); `tests/visual-editor-brand-control-center-state.test.cjs` (+4 R5.2 chip cases, 35 → 39); E-107. **true_false (2026-09-01)**: second R5 slice to add a new resolver primitive (after R5.2+color_picker). **2 curated group-nested Vertical true_false records flip Unsupported → Available**. New `addons/visual-editor/src/Resolvers/AcfTrueFalseResolver.php` extends `AbstractAcfResolver` (name `acf_true_false`; supports `field_type='true_false'`; validate accepts null/empty/bool/int-0-1/string-`0`/`1`/`true`/`false`/`on`/`off`/`yes`/`no` and rejects everything else; sanitize normalizes to ACF's canonical `1`/`0` integer; save inherits group-nested writes; new public static `coerceToBool` reused by summary + panel controller). Registered in `ResolverRegistry`. `SharedGlobalsControlProvider::supportedFieldTypes()` widened by `true_false`; new public static `buildBooleanSummary` returns `{family:'boolean', value, label:__('On'|'Off')}` — never null (booleans always render a state); delegates to `coerceToBool` for single-source truthy interpretation. `SharedGlobalsDescriptorFactory` gains `true_false` branch (`resolver='acf_true_false'`, `input='true_false'`, contract `shared_field`). `overlay-app.js` gains a real new panel controller `createBooleanController(value)` — checkbox + live-updating "On"/"Off" label span, getValue returns `1`/`0` matching ACF, `coerceBooleanValue` client-side helper mirrors the server's coerceToBool. Frontend chip `.__value-boolean` pill with `is-on` variant (success-tone `color-mix`). CSS. Vertical (both checkouts, byte-identical) `resolveStatus` R5.2 branch widened by true_false + supported list + buildValueSummary delegation. **Cross-layer coercion consistency**: same truthy-shape definition lives in `AcfTrueFalseResolver::coerceToBool` (server) + `buildBooleanSummary` (server chip payload) + `coerceBooleanValue` (client panel controller) — three parallel implementations covering the same bool / int / string / on-off-yes-no domain, kept aligned via the canonical spec in the resolver docblock. **Nested reference-collection investigation** (not landed): traced `AcfReferenceCollectionResolver::save()` → `writeCollectionValue()` → `writeAcfValue()` which DOES route through `writeGroupedFieldValue` when `is_grouped_field=true`. Group-only nesting would work if the factory emitted `group_write_path` on the relationship branch. BUT the specific remaining record `settings_nav_menus>menus>menu_posts` almost certainly has `menus` as a repeater; repeater-subfield writes need `writeRepeaterSubfieldValue`, not `writeGroupedFieldValue`, so a partial factory extension would ship broken. Deferred to a dedicated slice that adds ACF introspection to detect group-vs-repeater chains. **Tests**: new `VisualEditorControlCenterTrueFalseTest` (15/34 — resolver + coercion matrix of 7 truthy + 8 falsy inputs + validate accept/reject + factory + provider + summary). 3 pre-existing skip fixtures re-based onto still-unsupported `taxonomy` (R51A, R3-B) or nested-relationship (R51B). `test_r52_true_false_stays_unsupported` renamed and inverted. `manifest.json` discovery id rotated for `dbvc_visual_editor_restricted_option_group_roots`. **Live-site coverage after true_false**: R5.1 (182) + R5.2 (155) + R5.2+color_picker (20) + R5.3 (37) + R5.4 (1) + true_false (2) = **397 of 400 Vertical records editable (99.25%)**. Only 3 records remain: 1 nested reference-collection (deferred; needs factory extension with repeater/group detection) + 2 tagged `later` / no unlocks_at (not intended for current slices). **Validation**: `VisualEditorControlCenterTrueFalse` → **15/34 OK**. `VisualEditor(Control\|SharedGlobals\|Vertical\|Resolver)` → **177/614 OK** (was 152/564 — +25). Drawer jsdom **42 → 44** (+2). Media-manager **42/42**. Agent docs **54/443/0**. See E-111. **R5.2+color_picker (2026-09-01)**: first R5 slice to add a genuinely new resolver primitive. **20 curated group-nested Vertical color_picker records flip Unsupported → Available**. New `addons/visual-editor/src/Resolvers/AcfColorPickerResolver.php` extends `AbstractAcfResolver` (name `acf_color_picker`; supports `field_type='color_picker'`; validate accepts empty/null/recognized color shapes — `#rgb`, `#rrggbb`, `#rrggbbaa`, `rgb(...)`, `rgba(...)` — and rejects `javascript:` scheme + named colors + HTML-tag-shaped inputs; sanitize normalizes hex to lowercase 7-char with 3-char → 6-char expansion; save inherits group-nested writes via `writeGroupedFieldValue`; new public static `isRecognizedColorString` reused from tests). Registered in `ResolverRegistry`. `SharedGlobalsControlProvider::supportedFieldTypes()` widened by `color_picker`; new public static `buildColorSummary($value)` (family='color', hex normalized). `SharedGlobalsDescriptorFactory` gains `color_picker` branch (`resolver='acf_color_picker'`, `input='color'`, contract `shared_field`; group-nesting inherits from R5.1-b). `overlay-app.js` `createFieldController` switch adds a one-line `case 'color':` that delegates to the existing `createInputController('color', value)` — **zero new panel controller code** — which produces a native `<input type="color">`. Frontend chip renders `.__value-color` (12×12 swatch with background-color set via `style.setProperty` on server-validated hex + monospace hex label + full hex title tooltip). CSS `.__value-color`/`-swatch`/`-hex`. Vertical (both checkouts) `resolveStatus` R5.2+color_picker branch + supported list + `buildValueSummary` delegates to DBVC static. New `tests/phpunit/VisualEditorControlCenterR52ColorPickerTest.php` (15/40 — resolver + summary shape + factory + provider + `@dataProvider` matrices of 6 recognized + 5 rejected color shapes including security-sensitive `javascript:` reject). Existing R5.1-a "skip" fixture and R3-B "skip" fixture re-based onto `true_false` since color_picker no longer represents the "still-unsupported" example. Manifest: `dbvc_visual_editor_restricted_option_group_roots` discovery id rotated from line shifts. **Data-safety**: chip renderer sets background via `style.setProperty` on server-validated hex only; browser CSS parser rejects unknown values as a final layer. **Validation**: `VisualEditorControlCenterR52ColorPicker` → **15/40 OK**. `VisualEditor(Control\|SharedGlobals\|Vertical\|Resolver)` subset → **152/564 OK** (was 129/519 — +23 new). Drawer jsdom **41 → 42** (+1 color chip case). Media-manager **42/42**. Agent docs **54/443/0**. **Live-site coverage after R5.1 + R5.2 + R5.2+color_picker + R5.3 + R5.4**: **395 of 400 Vertical records editable (98.75%)** — remaining 5 = 2 true_false + 1 nested reference-collection + 2 other. See E-110. **R5.2 (2026-09-01)**: `SharedGlobalsControlProvider::supportedFieldTypes()` widened by 6 R5.2 types (select/checkbox/radio/button_group/link/wysiwyg). `buildValueSummary` dispatch widened with 5 new branches, each delegating to a new public static helper so Vertical can reuse without duplicating logic: `buildTextSummary`, `buildSingleChoiceSummary`, `buildMultiChoiceSummary`, `buildLinkSummary`, `buildWysiwygSummary`. Choice summary shape mirrors relationship (`{family:'choice', count, firstLabels[≤3], hasMore}` for multi-checkbox; `{family:'choice', label}` for single-select). Link summary is `{family:'link', title, url}` with host fallback when title empty. Wysiwyg is `{family:'wysiwyg', preview:stripped_text_at_40_chars, wordCount, truncated}`. `SharedGlobalsDescriptorFactory::build()` refactored to a config-bag pattern — every family branch delegates to `buildSharedFieldDescriptor()` with a per-family `{resolver, input, source_context, ui_options}` config. Choice branch projects ACF `choices` onto `ui.options=[{value,label}]` via new private `projectAcfChoices()` (both value + label sanitized). All emit `mutation.contract='shared_field'`, `kind='scalar'`; group-nesting inherits from R5.1-b `walkGroupChain()`. Frontend `renderValueSummaryChip` gains three branches (choice with single-label or multi-count-with-tooltip; link with title-or-url + `↗` glyph; wysiwyg with stripped preview + word-count suffix). Vertical `resolveStatus` R5.2 branch flips 6 types to available when `unlocks_at='R5.2'`; `buildDescriptor` accepts the widened set; `buildValueSummary` delegates to DBVC's static builders (choice lifts field object for choices map). Mirrored byte-identically to both Vertical checkouts. **Validation**: `vendor/bin/phpunit --filter "VisualEditorControlCenterR52Test"` → **20/90 OK**. `VisualEditor(Control\|SharedGlobals\|Vertical)` subset → **111/468 OK** (was 84/358 — +27 new). Drawer jsdom **35 → 39** (+4). Media-manager jsdom **42/42**. Agent docs **54/443/0** (2 new i18n keys, no hook/REST). **Live-site coverage after R5.2**: R5.1 (182) + R5.2 (155) = **337 of 400 Vertical records now editable**. Remaining: R5.2+color_picker (20 — needs new AcfColorPickerResolver), R5.3 (37 image), R5.4 (2 post_object/relationship), true_false (2 — needs boolean controller). See E-107. |
| R5.3 | Image/gallery option fields | **R5.3 image landed** (37 curated group-nested image records flip Unsupported → Available; gallery family absent from curation JSON — no R5.3 gallery scope) | Codex R5.3 / 2026-09-01 | `addons/visual-editor/src/Registry/Providers/SharedGlobalsControlProvider.php` (widened whitelist + `mapFieldFamily` image branch + `buildImageSummary` static helper + `coerceAttachmentId` private helper + buildValueSummary image branch); `addons/visual-editor/src/Registry/Providers/SharedGlobalsDescriptorFactory.php` (image factory branch — resolver=acf_image, ui.input=media_reference, source_context=toolbar_shared_global_option_image; walkGroupChain inherited for group-nesting); `addons/visual-editor/assets/js/brand-control-center-app.js` (image chip renderer with 24×24 thumbnail via setAttribute per jsdom loading-attr quirk + filename + title tooltip); `addons/visual-editor/assets/css/control-center.css` (`.__value-image`, `.__value-image img`, `.__value-image-filename`); Vertical (both checkouts) `themes/vertical/functions/features/dbvc-visual-editor/includes/class-vf-vertical-control-provider.php` (resolveStatus R5.3 image branch + buildDescriptor supported list widened + buildValueSummary image delegation to DBVC's static helper); `tests/phpunit/VisualEditorControlCenterR53Test.php` (new, 10/28); `tests/phpunit/VisualEditorVerticalProviderR51BTest.php` (+1 R5.3 image case + fixture re-basing on the "unsupported" test); `tests/visual-editor-brand-control-center-state.test.cjs` (+2 image chip cases, 39 → 41); E-108. **R5.3 (2026-09-01)**: SharedGlobalsControlProvider whitelist widened by `'image'`; `mapFieldFamily` explicitly returns `'image'` for the image type (unlike R5.2 families which collapse to `'other'`, image gets its own ControlRecord whitelist slot). New public static `buildImageSummary($value)` returns `{family:'image', attachmentId, filename, thumbUrl}` — collapses ACF's three value shapes (bare int / numeric string / `[ID|id]` array / WP_Post `->ID`) onto a positive integer id via new private `coerceAttachmentId`, then resolves filename via `get_attached_file → basename` (fallback `wp_get_attachment_metadata['file']`) and thumb URL via `wp_get_attachment_image_src($id, 'thumbnail')`. Empty / unresolvable / non-attachment id → null. `SharedGlobalsDescriptorFactory::build()` gains image branch delegating to `buildSharedFieldDescriptor()` with `{resolver:'acf_image', input:'media_reference', source_context:'toolbar_shared_global_option_image'}` — group-nesting inherits from R5.1-b's `walkGroupChain()`. `AbstractAcfResolver::writeGroupedFieldValue` routes group-nested image saves THROUGH the parent group's own `update_value` (RK-013 pattern) so attachment ids persist to the correct prefixed meta key. Frontend chip renders `.__value-image` container with 24×24 `<img loading="lazy">` (via `setAttribute` per jsdom quirk) + `.__value-image-filename` span + `title` tooltip. Vertical `resolveStatus` R5.3 branch flips `image` type when `unlocks_at='R5.3'`; `buildDescriptor` accepts image; `buildValueSummary` delegates to DBVC's static builder. **Validation**: `VisualEditorControlCenterR53Test` → **10/28 OK**. `VisualEditor(Control\|SharedGlobals\|Vertical)` subset → **122/499 OK** (was 111/468 — +11 new). Drawer jsdom **39 → 41** (+2). Media-manager **42/42**. Agent docs **54/443/0** (no i18n / hook / REST additions). **Live-site coverage after R5.3**: R5.1 (182) + R5.2 (155) + R5.3 (37) = **374 of 400 Vertical records now editable**. Remaining: R5.2+color_picker (20 — needs new AcfColorPickerResolver), R5.4 (2 post_object/relationship — Vertical parity for R3-B), true_false (2 — needs boolean controller + resolver). See E-108. |
| R5.4 | Connected/taxonomy option fields (+ nested reference-collection sub-slice) | **R5.4 landed + nested reference-collection landed** (R5.4: top-level relationship + post_object flip. Nested sub-slice: DBVC factory now walks parent chain via walkGroupChain and emits group_write_path for group-only nested chains; Vertical resolveStatus lifts ACF field object for nested reference records and gates on group-only ancestry via new hasGroupOnlyAncestry helper. The 1 remaining curated `settings_nav_menus>menus>menu_posts` record flips available IF `menus` is a group; stays unsupported IF it's a repeater. Repeater-subfield reference-collection support is a separate future slice with new primitives) | Codex R5.4 + nested-reference / 2026-09-01 | `addons/visual-editor/src/Registry/Providers/SharedGlobalsControlProvider.php` (new public static `buildReferenceSummary($value, $family)` + private static twins `normalizeToPostIdsList` + `coerceOnePostIdValue`; instance buildValueSummary reference-collection tail delegates to the static); Vertical (both checkouts) `themes/vertical/functions/features/dbvc-visual-editor/includes/class-vf-vertical-control-provider.php` (resolveStatus signature widened to accept `$field_name` so R5.4 branch can gate nesting via `strpos($name, '>') === false`; R5.4 branch flips post_object + relationship when top-level; buildDescriptor supported list widened by relationship + post_object; buildValueSummary reference-collection branch delegates to DBVC static); `tests/phpunit/VisualEditorVerticalProviderR51BTest.php` (+3 R5.4 cases: top-level relationship/post_object → available via @dataProvider; nested reference → unsupported); `tests/phpunit/VisualEditorControlCenterR52Test.php` (+4 buildReferenceSummary static-helper cases: 5-title walk with real WP posts; family gate rejects text; null on empty/false/null; ACF associative-array shape coercion); E-109. **Nested reference-collection (2026-09-01)**: DBVC `SharedGlobalsDescriptorFactory` relationship / post_object branch extended to walk the ACF parent chain via the existing `walkGroupChain($field)` (R5.1-b's helper). When chain is non-empty, descriptor emits `source.group_write_path` + `source.is_grouped_field=true` + root `source.field_selector`/`field_selector_raw` + `path.rootFieldName`/`rootFieldKey`. Top-level shape stays byte-identical (regression-pinned via factory test). `AcfReferenceCollectionResolver::save()` → `writeCollectionValue()` → `writeAcfValue()` → `writeGroupedFieldValue()` handles the group-nested write via the parent group's `update_value`. Vertical (both checkouts, byte-identical): `resolveStatus` signature widened to accept `$field_key`; R5.4 branch's blanket `>` guard replaced with a two-tier check — top-level records short-circuit (no ACF hit), nested records call new private static `hasGroupOnlyAncestry($field_key)` that lifts leaf via `acf_get_field()` and walks parent chain, returning true only when every intermediate is `type='group'`. Repeater / flexible / other intermediates keep the record unsupported. Fail-safe: returns false when ACF isn't loaded. **Also spotted (deferred)**: R5.1-b's `buildSharedFieldDescriptor` shares the same `walkGroupChain` without a type-check on intermediates — a repeater-nested text field would exhibit the same wrong-meta-key bug. All 182 R5.1-b records work in production, meaning none are repeater-nested; but a small future hardening slice should add the same group-only guard on the text-family path. **Tests**: new `VisualEditorControlCenterNestedReferenceTest` (3/24 — top-level regression guard; single-level group-nested; multi-level group chain; all use a global `acf_get_field` stub reading from `$GLOBALS['vfe_test_acf_fields']`). Extended `VisualEditorVerticalProviderR51BTest` (+2 — group-only ancestry flips available; repeater-in-chain stays unsupported; added `tear_down` unsetting the fixture global and `set_up` resetting to `[]` so pre-existing skip fixtures don't need the stub). **Live-site impact**: **1 curated record potentially unlocks** — depends on the ACF structure of `menus` (the one nested reference-collection record). If group, unlocks; if repeater, stays correctly unsupported. **Validation**: `VisualEditorControlCenterNestedReference` → **3/24 OK**. `VisualEditor(Control\|SharedGlobals\|Vertical\|Resolver)` subset → **182/642 OK** (was 177/614 — +5 new). Drawer jsdom **44 preserved** (no drawer JS/CSS touched — R4-C-1b relationship chip already handles both top-level and group-nested identically). Media-manager **42/42**. Agent docs **54/443/0** (no i18n/hook/REST additions). **Live-site coverage after all R5 slices**: **397–398 of 400 Vertical records editable (99.25–99.5%)**. See E-112. **R5.4 (2026-09-01)**: R4-A instance-method relationship / post_object summary walk extracted into a new public static `buildReferenceSummary($value, $family)` — takes both raw value AND expected family string so the walker cannot be accidentally routed with a text value (family gate rejects non-`relationship|post_object`). Body is byte-identical to the R4-A instance code. Two new private static twins `normalizeToPostIdsList` + `coerceOnePostIdValue` (instance methods still exist for backward compatibility). Instance `buildValueSummary()` reference-collection tail delegates to the static so R4-A tests keep passing byte-identically. Vertical `resolveStatus()` signature widened to accept `$field_name`; R5.4 branch flips post_object + relationship to available when `unlocks_at='R5.4'` AND `strpos($field_name, '>') === false` (top-level only — R3-C-1's reference-collection factory branch does NOT emit `group_write_path` for reference collections, so nested paths would save to the wrong meta key. Nested reference-collection support is a separate later slice). Vertical `buildDescriptor` supported list widened by relationship + post_object; `buildValueSummary` delegates to DBVC's new static. **Live-site impact**: **1 of the 2 R5.4 curated records unlocks** — the top-level `office_manager` post_object on `site-settings` flips Unsupported → Available. The other record (`settings_nav_menus>menus>menu_posts`, relationship nested inside a repeater) stays unsupported by design. Zero new REST routes / mutation authority / resolvers / mutation contracts. Every family the drawer + backend contract supports now has a public static summary builder (`buildTextSummary`, `buildSingleChoiceSummary`, `buildMultiChoiceSummary`, `buildLinkSummary`, `buildWysiwygSummary`, `buildImageSummary`, `buildReferenceSummary`) — single source of truth Vertical + any future provider reuses. **Validation**: `VisualEditor(Control\|SharedGlobals\|Vertical)` subset → **129/519 OK** (was 122/499 — +7 R5.4 new). Drawer jsdom **41 preserved** (R5.4 touched no drawer JS/CSS — the relationship chip R4-C-1b already renders both `relationship` and `post_object`). Media-manager jsdom **42/42**. Agent docs **54/443/0** (unchanged — no new hook / REST / i18n). **Live-site coverage after R5.1 + R5.2 + R5.3 + R5.4**: **375 of 400 Vertical records editable (93.75%)**. Remaining families: R5.2+color_picker (20 — needs new AcfColorPickerResolver + color panel controller), true_false (2 — needs new boolean controller + resolver), nested reference collections (1 — needs factory reference-collection branch to walk parent + emit `group_write_path`). See E-109. |
| R5.later-tests | Overlay-app.js jsdom scaffolding (test-infra prerequisite) | **PLANNED (2026-09-05, sequenced FIRST before the three follow-ons)** — Stands up the missing jsdom test file for overlay-app.js so R5.later-perf's instrumentation, R5.later-c's per-family presenters, and R5.later-cache's correctness invariants all have deterministic coverage instead of "shipped without regression tests → live QA caught it → hotfix" (the pattern R5.later-y-2b/c/d + R5.later-b instigated). New `tests/visual-editor-overlay-app-state.test.cjs` mirroring the drawer + media-manager test files. Coverage targets: palette overview controller mount + swatch save flow, palette bulk-popover modal mount + copy-hex + copy-all, `handleSave` early-return guard for `input='palette_grid'`, `handleOpenControlRequest` event routing, `renderEditorPanel` dispatch on `ui.input` (R5.later-y-2c regression guard). **Sizing**: medium — one-time scaffolding cost + per-controller test fixtures. **Sequencing**: first — unblocks R5.later-cache correctness (which cannot safely ship without deterministic invariant tests). Full spec in `docs/dropins/dbvc-visual-editor-brand-controls-guide/releases/R5-REMAINING-UNLOCK-FRONTIERS.md` §Planned phases before R6 — R5.later-tests. | — / planned | (planned files) `tests/visual-editor-overlay-app-state.test.cjs` (new — jsdom + IntersectionObserver polyfill + `window.fetch` mock + `DBVCVisualEditorBootstrap`/`DBVCVisualEditorApi` mocks copied from media-manager test file); optional refactor to expose panel controllers via `window.DBVCVisualEditorPanelControllers` for test access if jsdom's script loader can't handle the 13K-line IIFE (prefer no production surface change). |
| R5.later-perf | Deep frontend performance audit | **PLANNED (2026-09-05, sequenced SECOND after R5.later-tests)** — Systematic audit of the Brand Control Center's frontend load path measuring where time + network bytes go from `?dbvc_ve_editmode=1` through drawer-opens through first meaningful interaction. Discovery/report slice — produces a scoped findings report + prioritized fixes, no production code changes. Instrumentation via `window.performance.mark`/`.measure` at each network round-trip + significant render pass, hidden behind D-064's existing performance profiler. Real-browser measurement against the deployed local site at 400 curated records × 2 supported viewports **in real foregrounded Chrome (NOT Claude-in-Chrome automation — E-123 documented IO throttling in backgrounded tabs)**. N=5+ runs per scenario with medians reported (not single-shot; real-browser variance is 20-40% run-to-run). Baseline numbers committed to the repo so future audits can prove regressions or improvements. Produces `qa/R5-LATER-PERF-AUDIT-REPORT.md` with baseline measurements, top-N findings ranked by cost × frequency, per-finding recommendations (fix in flight vs. fix in cache — the latter feeds R5.later-cache), a "wins we chose not to pursue" residuals list, and a LocalWP→production translation caveat (local host lacks CDN + PHP-FPM contention — findings may not translate 1:1 to a WP Engine + Cloudflare shape). **Sizing**: small-medium — mostly measurement + writing. **Sequencing**: after R5.later-tests, before R5.later-c (findings inform per-row hydration cost) and R5.later-cache (baselines prove the cache helped). Full spec in `docs/dropins/dbvc-visual-editor-brand-controls-guide/releases/R5-REMAINING-UNLOCK-FRONTIERS.md` §Planned phases before R6 — R5.later-perf. | — / planned | (planned files) `docs/dropins/dbvc-visual-editor-brand-controls-guide/qa/R5-LATER-PERF-AUDIT-REPORT.md` (new); temporary instrumentation in `addons/visual-editor/assets/js/brand-control-center-app.js` + `overlay-app.js` behind a debug flag (removed after audit unless found broadly useful); zero production code changes to be shipped in this slice. |
| R5.later-c | Drawer inline current-value display | **PLANNED (2026-09-05, sequenced THIRD after R5.later-perf)** — Every drawer row will render the field's current stored value as a compact sanitized preview sibling to `.__meta`. **PINNED design decisions (2026-09-05)**: (1) R4-C-1b chip MOVES from action cell to label cell (not "add a second slot" — one location is cleaner); (2) repeater/palette parents show a compact aggregate (`[N rows]`/`[N colors]`); (3) row-height cap 1 line + ellipsis, wysiwyg stripped-preview capped ~40 chars with `title` tooltip; (4) search-scope stays label + description ONLY (widening `q` to match values would push server-side re-derivation per keystroke — too expensive). Extends R4-C-1b's value-summary infrastructure: widens the existing `POST /value-summaries` endpoint's per-family builders to cover every R5.x-shipped family (text, choice, link, image, color, boolean, date, wysiwyg) alongside the existing relationship/post_object. Reuses R4-C-1b's IntersectionObserver batch-fetch + cache invariants + R4-D-3's overflow-wrap defense + R5.later-tests scaffolding. Zero new REST routes. **Known dependency**: if R5.later-perf flags image-family hydration as a top-3 offender, R5.later-cache must land BEFORE R5.later-c (sequence flips to perf → cache → c) — the audit's findings determine this. **Sizing**: medium — 8 per-family presenter functions + one drawer JS render block + one CSS block + moving the R4-C-1b slot. **Sequencing**: after R5.later-perf. Full spec in `docs/dropins/dbvc-visual-editor-brand-controls-guide/releases/R5-REMAINING-UNLOCK-FRONTIERS.md` §Planned phases before R6 — R5.later-c. | — / planned | (planned files) `addons/visual-editor/src/Registry/Providers/SharedGlobalsControlProvider.php` (widen `buildValueSummary` per-family switch); `themes/vertical/functions/features/dbvc-visual-editor/includes/class-vf-vertical-control-provider.php` (both checkouts — extend `buildValueSummary` to delegate for every R5.x family); `addons/visual-editor/assets/js/brand-control-center-app.js` (MOVE `.__value-summary` slot from action cell to new `.__value-inline` slot after `.__meta` in label cell + per-family presenter functions); `addons/visual-editor/assets/css/control-center.css` (new `.__value-inline` selector — muted subtext line, monospaced for hex + swatch preview, lazy thumb for image, ellipsis + R4-D-3 overflow-wrap defense; remove old `.__value-summary` action-cell selector); `addons/visual-editor/src/Assets/AssetLoader.php` (per-family i18n keys); `tests/visual-editor-brand-control-center-state.test.cjs` + `tests/visual-editor-overlay-app-state.test.cjs` (extend coverage — R5.later-tests scaffolding is prerequisite). |
| R5.later-cache | Session-persistent scan cache | **PLANNED (2026-09-05, sequenced LAST before R6)** — Client-side cache that survives page reloads. **PINNED design decisions (2026-09-05)**: (1) kill-switch **defaults OFF** (opt-in) — correctness bar too high to enable-by-default until real users validate; (2) localStorage + LRU eviction above 4MB (not IndexedDB — too much ceremony for the payload; not WP transient — server-side cache out-of-scope); (3) cache key includes `{userId}` so demoted users don't see cached-value ghosts; cache burns on session-token rotation via new `panel:session-changed` event; (4) **multi-signal invalidation, not just viewModelVersion** — invariants below. **Correctness invariants (must be provably maintained via R5.later-tests scaffolding)**: (a) viewModelVersion bumps burst; (b) R4-D-1's `panel:saved` invalidates the affected row; (c) **palette per-swatch saves invalidate too** (R5.later-y-2 currently does NOT fire `panel:saved` — cache slice must dispatch a compatible event from the per-swatch save path); (d) ETag-per-row background revalidation via new `rowVersion` on R4-A's list response + `If-None-Match` per row (catches other-user + wp-admin edits); (e) cross-tab drift via `window.storage` event listener; (f) capability-change burn on session-token rotation; (g) `QuotaExceededError` graceful degradation — full burn + fall-through to no-cache render. Cache-hit UX: instant render from cache + background revalidation + subtle "revalidating…" indicator + surgical per-row patches (R4-C-1b `patchValueSummarySlot` shape). **Sizing**: medium-large — mechanics straightforward but invariants + tests + palette-event coupling add non-trivial coverage. Requires R5.later-tests scaffolding + R5.later-perf baselines + (probably) R5.later-c hydration shape. **Sequencing**: last. Full spec in `docs/dropins/dbvc-visual-editor-brand-controls-guide/releases/R5-REMAINING-UNLOCK-FRONTIERS.md` §Planned phases before R6 — R5.later-cache. | — / planned | (planned files) `addons/visual-editor/assets/js/brand-control-center-app.js` (new cache read/write helpers + LRU eviction + `window.storage` listener + wrap `loadControls` with cache-first render + background revalidation + ETag round-trip); `addons/visual-editor/assets/js/overlay-app.js` (dispatch `panel:saved`-shaped event from per-swatch save path with leaf publicId + token so cache invalidates correctly on palette edits); `addons/visual-editor/src/Rest/Controllers/ControlCenterListController.php` (add `rowVersion` hash per row to R4-A's list response + honor `If-None-Match` conditional GET); `addons/visual-editor/src/Bootstrap/Addon.php` (new kill-switch setting `dbvc_visual_editor_control_center_cache_enabled` **default OFF**); `addons/visual-editor/assets/css/control-center.css` (visual "revalidating…" indicator); `tests/visual-editor-overlay-app-state.test.cjs` + `tests/visual-editor-brand-control-center-state.test.cjs` (cache hit + all 7 invariants + revalidation-reconcile test cases). |
| R5.later-y-3 | Pre-mint leaf descriptors (first-save perf) | **R5.later-y-3 landed 2026-09-05**. Closes the last captured R5.later palette-UX frontier. Server pre-mints each leaf's own single-color descriptor + token at palette-parent open time; `ControlCenterOpenController` registers each leaf in the session alongside the palette parent; frontend palette controllers pre-warm their descriptorCache from `source.leaves[i].token`. **First save per swatch drops from 2 round-trips to 1** (previously: `/control-center/open` for leaf → `/save`; now: `/save` directly). Zero new REST routes / mutation authority / resolvers. Zero user-visible UI change; identical save feedback. **The R5.later Palette Grouping arc is now feature-complete for all Options + frontiers** — X, Y, Z, y-2 inline editing, y-3 perf. | Codex R5.later-y-3 / 2026-09-05 | `themes/vertical/functions/features/dbvc-visual-editor/includes/class-vf-vertical-control-provider.php` (both checkouts, byte-identical — `buildPaletteOverviewDescriptor` hoists the leaf-descriptor factory build we already run for value resolution; attaches `token` + `descriptor` fields to each leaf payload; explicit null reset per iteration guards bleed-over); `addons/visual-editor/src/Rest/Controllers/ControlCenterOpenController.php` (new `use EditableDescriptor` import; after main descriptor registration, iterates `source.leaves[]` and registers each embedded leaf descriptor via `addDescriptorToSession` with per-leaf capability re-check; response's `descriptors` map now carries all N+1 tokens via `exportPublicMap($collected_descriptors)`); `addons/visual-editor/assets/js/overlay-app.js` (side panel's `createPaletteOverviewController` + modal's `renderPaletteBulkPopoverContent` both pre-warm their descriptorCache from `leaf.token` at mount time; defensive fallback to lazy-open when a leaf lacks a pre-minted token); `tests/phpunit/VisualEditorControlCenterR5LaterAPaletteTest.php` (regression guard in `test_r5later_y_build_descriptor_mints_palette_grid_descriptor` asserts leaves carry `token` + `descriptor` fields, soft-conditional on `get_field_object` availability). **Zero user-visible UI change** — controller-scoped perf optimisation. **Validation**: PHPUnit `VisualEditor\|Curation` → **441 / 3036 OK** preserved. Drawer jsdom **60 pass** preserved. Media-manager **42 pass** preserved. PHP lint clean. `diff -q` confirms both Vertical checkouts identical. No agent-docs hash rotation. See E-133. |
| R5.later-b | Dedicated palette bulk-edit popover (Option Y — second surface) | **R5.later-b landed 2026-09-05**. Palette-role tree parents now render an Expand icon button alongside the existing Open button. Click Expand → full-attention centered modal opens with a larger swatch grid (200px min cells, 96px swatch height), per-cell Copy hex button, header Copy-all-as-CSS-variables action. Focus trap + Escape + click-outside close. Same lazy-open + save infrastructure as R5.later-y-2 byte-identical — zero new REST routes, zero new mutation authority, zero backend touches. The Copy-all-as-CSS-variables export produces `--kebab-case: #hex;` newline blocks for direct paste into design-token files. **The R5.later Palette Grouping arc is now COMPLETE for all three design-review Options (X in-drawer tree + Y dedicated popup + Z panel palette render mode).** | Codex R5.later-b / 2026-09-05 | `addons/visual-editor/assets/js/brand-control-center-app.js` (new `renderPaletteExpandAction()` helper; new `open-palette-bulk` case in click delegate dispatches CustomEvent); `addons/visual-editor/assets/js/overlay-app.js` (new `openPaletteBulkPopover()` + `buildPaletteBulkPopoverShell()` + `renderPaletteBulkPopoverContent()` + `buildPaletteBulkCell()` + `saveBulkCell()` + `openLeafDescriptorForBulk()` + `setBulkCellState()` + `copyToClipboard()` + `showBulkStatus()`; module-scoped `paletteBulkPopoverState` for single-modal semantics; event listener registered in `bindControlCenterBridge()`); `addons/visual-editor/assets/css/overlay.css` (~200 lines of `.dbvc-ve-palette-bulk-popover*` selectors — backdrop, container, header, grid, cell state transitions, layered transparent color input, reduced-motion); `addons/visual-editor/assets/css/control-center.css` (`.__row-expand` + `.__row-expand-icon` — 24×24 icon button); `addons/visual-editor/src/Assets/AssetLoader.php` (+8 i18n keys — `controlCenterPaletteExpand`, `panelPaletteBulkLoading`/`Failed`/`FallbackTitle`/`Close`/`CopyHex`/`CopyAll`/`Copied`/`CopyFailed`); `tests/visual-editor-brand-control-center-state.test.cjs` (+3 R5.later-b jsdom cases). **Zero backend touches** — Vertical provider unchanged; palette descriptor shape from R5.later-y unchanged; every save round-trip reuses existing `/control-center/open` + `/save` routes byte-identical. **Validation**: PHPUnit `VisualEditor\|Curation` → **441 / 3036 OK** preserved. Drawer jsdom **60 pass** (was 57 — +3 cases). Media-manager **42 pass** preserved. PHP lint clean. No agent-docs hash rotation. **Test coverage caveat**: the modal itself (`openPaletteBulkPopover` chain in overlay-app.js) has no jsdom coverage — overlay-app has no dedicated test file. Modal behaviour relies on unchanged backend + drawer-side event dispatch coverage + live QA. See E-132. **Remaining palette UX frontier**: R5.later-y-3 (optional pre-mint of leaf descriptors at panel-open for 1-round-trip first save — perf-only). |
| R5.later-y-2 | Inline swatch editing inside the palette overview panel | **R5.later-y-2 landed 2026-09-05**. Extends R5.later-y with live per-swatch editing — each swatch is now a `<label>` wrapping a native `<input type="color">`; changing a color fires lazy-open + save via existing R5.2+color_picker contract with per-publicId token cache. First edit = 2 round-trips (open + save); subsequent edits on same swatch = 1 round-trip (cache hit). 250ms debounce for live-drag inputs. Per-cell save-status indicators + aria-live announcements + border colour transitions. Panel Save button guarded against palette_grid to prevent `value: null` writes. Zero new REST routes / mutation authority / resolvers / backend touches. Curator now edits N palette colors without leaving the panel. | Codex R5.later-y-2 / 2026-09-05 | `addons/visual-editor/assets/js/overlay-app.js` (`createPaletteOverviewController` refactored from `<button>` cells to `<label>`-wrapping-`<input type="color">`; new controller-scoped `descriptorCache` + `debounceTimers` maps; new `saveSwatch()` / `openLeafDescriptor()` / `setCellState()` helpers; `handleSave()` early-return guard for `input='palette_grid'`); `addons/visual-editor/assets/css/overlay.css` (new `.__palette-overview-swatch-wrap` / `.__palette-overview-input` / `[data-state]` cell selectors / `.__palette-overview-cell-status`); `addons/visual-editor/src/Assets/AssetLoader.php` (+3 i18n keys — `panelPaletteOverviewCellSaving`/`Saved`/`Error`; +2 updated header keys for new UX wording). **Zero backend touches** — Vertical provider unchanged (byte-identical both checkouts), descriptor shape from R5.later-y is exactly what inline editing needs. **Validation**: PHPUnit `VisualEditor\|Curation` → **441 / 3032 OK** preserved. Drawer jsdom **57 pass** preserved. Media-manager **42 pass** preserved. PHP lint clean. No agent-docs hash rotation. **Test coverage note**: overlay-app.js has no jsdom test file — inline editing coverage relies on unchanged backend contract (32 PHPUnit palette tests) + unchanged drawer contract (57 jsdom tests) + live QA. Adding overlay-app.js test scaffolding captured as a future slice. See E-131. **Remaining palette UX frontiers**: R5.later-b (Option Y popup — alternative surface), R5.later-y-3 (optional pre-mint of leaf descriptors at panel-open for 1 round-trip first-save perf). |
| R5.later-y | Palette overview panel (Option Z — panel palette render mode) | **R5.later-y landed 2026-09-04**. Palette parents now surface an Open button alongside R5.later-a's disclosure triangle; clicking Open mounts a dedicated palette overview panel showing every color in the palette as a responsive swatch grid. Each swatch is a `<button>` — click dispatches `dbvc:visual-editor:open-control` CustomEvent → drawer routes to leaf's own R5.2+color_picker panel via existing save infrastructure. Zero new REST routes / mutation authority / resolvers. Curator now has two orthogonal ways to reach any palette color: drawer expand → leaf Open, OR palette parent Open → swatch click. R5.later Palette Grouping arc COMPLETE for Option Z from the design review. | Codex R5.later-y / 2026-09-04 | `themes/vertical/functions/features/dbvc-visual-editor/includes/class-vf-vertical-control-provider.php` (both checkouts, byte-identical — `buildPaletteParentRecord` flips status='unsupported' → 'available'; new `buildDescriptor` branch for `source.role='palette_parent'` mints `input='palette_grid'` descriptor via new private `buildPaletteOverviewDescriptor()` which pre-scans cached records for sibling color_pickers, resolves each leaf's current hex via `get_field()` + `sanitize_hex_color`, builds leaves array with `{publicId, label, field_key, field_name, current_hex}`, uses `$this->slugify()` for publicId to mirror mapRecord's id path); `addons/visual-editor/assets/js/brand-control-center-app.js` (`renderRow`'s tree-parent branch renders Open button when `meta.role='palette'` + `status='available'`; new `handleOpenControlRequest(event)` listener with drawer-open + items-list guards routes to `openRow(publicId)`; `mount()` registers the new listener); `addons/visual-editor/assets/js/overlay-app.js` (new `case 'palette_grid'` in `createFieldController`; new `createPaletteOverviewController(descriptor)` renders responsive CSS grid of `<button>` swatch cells with hover lift + focus ring + click dispatches CustomEvent; controller returns null from `getValue()` for read-only semantics); `addons/visual-editor/assets/css/overlay.css` (new `.dbvc-ve-panel__palette-overview*` selectors — responsive `grid-template-columns: repeat(auto-fill, minmax(120px, 1fr))`, swatch 56px height, monospaced hex label, `.is-empty` diagonal-line pattern, `prefers-reduced-motion` block); `addons/visual-editor/src/Assets/AssetLoader.php` (+4 i18n keys — `panelPaletteOverviewHeaderOne`/`HeaderMany`/`Empty`/`CellLabel`); `tests/phpunit/VisualEditorControlCenterR5LaterAPaletteTest.php` (+4 tests / +27 assertions — status-flip guard, descriptor happy-path, null for non-existent palette, null for empty palette_key; +1 existing test's status assertion flipped from unsupported → available); `tests/visual-editor-brand-control-center-state.test.cjs` (+4 jsdom tests — Open button present when palette+available, no Open when repeater+unsupported, open-control event routes to openRow, unknown publicId ignored). **Validation**: PHPUnit `VisualEditor\|Curation` → **441 / 3032 OK** (was 437/3005 pre-slice). Drawer jsdom **57 pass** (was 53). Media-manager **42 pass** preserved. PHP lint clean on all 3 touched PHP files. `diff -q` confirms both Vertical checkouts identical. No agent-docs hash rotation. See E-130 for full evidence. **Remaining palette UX frontiers**: R5.later-y-2 (inline per-swatch editing — turn each swatch into a live `<input type="color">` firing per-color saves without leaving the palette panel); R5.later-b (Option Y — dedicated popup — an alternative surface that could ship on top of R5.later-y's infrastructure). |
| R5.later-a.6 | Curator-set `palette_display_label` override for palette parent drawer label | **R5.later-a.6 landed 2026-09-04**. Closes the label-override deferred follow-up from R5.later-a.5 — curators can now type a readable name like "Brand Colors" or "Semantic Palette" in the new label input under each palette color's row, and the drawer's palette parent renders that string instead of the humanised slug. Override is optional (empty leaves humanised-slug fallback intact), only ONE child needs the label (first non-empty wins across pre-scan — curator annotates a single canonical color instead of duplicating on every row), and survives store → export → provider as its own field distinct from `palette_group_key`. Zero live-site status changes today; palette parent identity now fully curator-controllable. | Codex R5.later-a.6 / 2026-09-04 | `addons/visual-editor/src/Curation/CurationStore.php` (new `PALETTE_DISPLAY_LABEL_MAX_LENGTH=60` via `sanitize_text_field` preserves casing + spaces; `emptyDecision` + `isEmptyDecision` extended for lone-label survival); `addons/visual-editor/src/Curation/CurationExporter.php` (passthrough as first-class record field); `addons/visual-editor/src/Admin/CurationPage.php` (second text input under palette_group_key input with humanised-default placeholder; new `humanisePaletteSlugPreview()` helper mirroring Vertical provider's `humanizePaletteGroupKey`); `addons/visual-editor/assets/js/curation.js` (readRowDecision starter extended with `palette_display_label: ''` so clearing persists); `addons/visual-editor/assets/css/curation.css` (`.dbvc-ve-curation__palette-label` selector — italic placeholder + 3px top margin, non-monospaced); `themes/vertical/functions/features/dbvc-visual-editor/includes/class-vf-vertical-control-provider.php` (both checkouts, byte-identical — `countPaletteMembers` return shape extended to `{count, display_label}` with first-non-empty-wins pre-scan + defensive re-sanitize; `buildPaletteParentRecord` signature gains `$display_label` 4th arg preferred over humanised slug via `trim + fallback`); `tests/phpunit/VisualEditorControlCenterR5LaterAPaletteTest.php` (+7 new label-override tests + 3 additional assertions; `paletteRecord` helper gains 5th optional `$palette_display_label` param defaulting to `''`). **R5.later-a.6 (2026-09-04)**: Wire-through slice — 4 DBVC PHP files + 2 DBVC frontend files (JS + CSS) + 1 Vertical file (mirrored) + 1 test file. Pure metadata plumbing whose runtime behaviour (label passthrough) is covered exhaustively by the 7 new PHPUnit assertions. **Validation**: PHPUnit `VisualEditor\|Curation` → **437 / 3005 OK** (was 427 / 2993 pre-slice — +10 tests / +12 assertions net). Drawer jsdom **53 pass** preserved (no drawer JS touched). Media-manager **42 pass** preserved. PHP lint clean on all 4 DBVC + 1 Vertical PHP files. `diff -q` confirms both Vertical checkouts identical. No agent-docs hash rotation. **The R5.later-a palette-grouping arc is now feature-complete**: R5.later-a + R5.later-a.1 + R5.later-a.2 + R5.later-a.3 + R5.later-a.5 + R5.later-a.6 = every design surface from R5.later Option X shipped. See E-128. |
| R5.later-a.5 | palette_group_key auto-default via recommender heuristic + palette parent humanised-slug label | **R5.later-a.5 landed 2026-09-03**. FieldCurationRecommender heuristically suggests palette_group_key for color_picker records nested inside a "palette"-named parent group; CurationExporter uses suggestion when curator has no explicit override; CurationPage shows the auto behaviour as input placeholder + hint. Vertical provider `buildPaletteParentRecord` (both checkouts) now always uses humanised slug for parent label, avoiding "Site Settings Advanced" duplication with the drawer's category header. **The site's Global Palette activates automatically on the maintainer's next Export click — no per-row curator toggle required.** Live-verified: 19 color_picker records fold under "Vertical Global Palette" disclosure in the frontend drawer | Codex R5.later-a.5 / 2026-09-03 | `addons/visual-editor/src/Curation/FieldCurationRecommender.php` (new public `deriveSuggestedPaletteGroupKey($candidate)` — color_picker + parent-name-contains-"palette" heuristic returns sanitize_key(parent segment)); `addons/visual-editor/src/Curation/CurationExporter.php` (new private `resolvePaletteGroupKey($decision, $candidate)` — curator override wins, recommender suggestion fallback); `addons/visual-editor/src/Admin/CurationPage.php` (`prepareRows` injects `_palette_group_key_suggested`; `renderRow` uses suggestion as placeholder + adds `is-auto` class + renders "auto: {slug}" hint); `addons/visual-editor/assets/css/curation.css` (`.dbvc-ve-curation__palette-key.is-auto` dashed border + warm-yellow tint; `.dbvc-ve-curation__palette-key-hint` 10px muted monospace); `themes/vertical/functions/features/dbvc-visual-editor/includes/class-vf-vertical-control-provider.php` (both checkouts, byte-identical — `buildPaletteParentRecord` always uses humanised slug for parent label); `tests/phpunit/VisualEditorCurationRecommenderTest.php` (+4 tests / +7 assertions covering heuristic happy-paths + edge cases); `tests/phpunit/VisualEditorControlCenterR5LaterAPaletteTest.php` (+2 exporter tests covering override-vs-fallback branches; existing curator-group test renamed + assertion flipped for the humanised-slug parent label); `addons/visual-editor/curation/vertical-approved-controls.json` (regenerated via wp-admin Export click — 19 records gained `palette_group_key="vertical_global_palette"` automatically); E-127. **R5.later-a.5 (2026-09-03)**: Wire-through slice — 5 DBVC files + 1 Vertical file + 2 test files + regenerated JSON. **Live-verified end-to-end**: BCC Curation shows auto-hints on all 19 palette rows; Export click populates JSON; frontend drawer folds all 19 colors under "Vertical Global Palette" disclosure with correct child-count chip + expand/collapse behaviour + individual Open buttons per leaf routing to R5.2+color_picker's native `<input type="color">` panel. Zero curator manual toggle needed going forward — new color_picker records added under a `*_palette` parent group auto-activate the palette on next export. **Validation**: PHPUnit `VisualEditor\|Curation` → **427 / 2993 OK** (was 421 / 2982 pre-slice — +6 tests / +11 assertions net). Drawer jsdom **53 pass** preserved. Media-manager **42 pass** preserved. PHP lint clean. `diff -q` confirms both Vertical checkouts identical. No agent-docs hash rotation. See E-127 for full evidence + live-verification details. |
| R5.later (Path B) | `vf_palette` permanently out-of-scope for the drawer | **R5.later Path B landed 2026-09-03**. Investigation showed `vf_palette` is a per-taxonomy-term color grid whose native ACF UI at Settings → Site Settings Advanced already offers a complete per-term color-picker editor; adding drawer support would duplicate the existing admin functionality with zero workflow value. Path A (theme-provided resolver extension point) would design a general abstraction against a single consumer — wrong shape; defer until a genuinely-different custom field type appears | Codex R5.later Path B / 2026-09-03 | `addons/visual-editor/src/Curation/FieldCurationRecommender.php` (FAMILY_UNLOCK_MAP gains `'vf_palette' => 'never'` with rationale comment linking to R5-REMAINING-UNLOCK-FRONTIERS.md §R5.later); `addons/visual-editor/curation/vertical-approved-controls.json` (settings_vf_term_colors record flipped `unlocks_at` from `"later"` to `"never"` via jq; 400 record count preserved); `tests/phpunit/VisualEditorCurationRecommenderTest.php` (+1 assertion for `deriveUnlocksAt('vf_palette') === 'never'`); `tests/phpunit/VisualEditorVerticalProviderR51BTest.php` (new defensive `test_r5later_pathb_never_unlocks_at_stays_unsupported`); E-126. **R5.later Path B (2026-09-03)**: Small doc + defensive-test slice — 4 files touched, no Vertical provider code changes needed (`resolveStatus`'s default "unrecognized unlocks_at → unsupported" path handles `never` sentinel unchanged), no cross-repo mirror needed. **The R5.x arc is now COMPLETE for every intentionally-unlocked field family** — every curated record either surfaces as Available today OR has an explicit R5.x slice sequenced for its unlock. `vf_palette` is the one record explicitly marked out-of-scope by design. Path B does NOT close the door on a future "theme-provided resolver extension point" slice if a genuinely-different custom ACF field type ever warrants it. **Validation**: `VisualEditor\|Curation` PHPUnit → **421/2982 OK** (was 420/2977 — +1 test / +5 assertions). Drawer jsdom **53 pass** unchanged. Media-manager **42 pass** preserved. PHP lint clean. No agent-docs hash rotation. **Live-site impact**: 0 records change status today; drawer coverage stays at 397+/400. **R6 (Frontend Site Manager Workspace) is now unblocked from every R5.x residual** and is the natural next major phase. See E-126 + `releases/R5-REMAINING-UNLOCK-FRONTIERS.md` §R5.later marked CLOSED. |
| R5.later-a.3 | CurationPage admin UI for palette_group_key | **R5.later-a.3 landed 2026-09-03**. Curators can now opt fields into palettes via Settings → DBVC Export → BCC Curation without WP-CLI. New "Palette group" column appended to the curation table (14 cols total, was 13); per-row text input bound via the existing `data-dbvc-ve-curation="field" data-field="palette_group_key"` wire-through → AJAX save → `CurationStore::setDecision` (which R5.later-a already handles). Zero PHP changes — pure admin-UI wiring | Codex R5.later-a.3 / 2026-09-03 | `addons/visual-editor/src/Admin/CurationPage.php` (new col in colgroup + new `<th>` with tooltip + new `<td>` with text input in `renderRow()`; `$palette_group_key_value` capture from decision; `colspan="13"` → `"14"` on both empty-state fallback rows); `addons/visual-editor/assets/js/curation.js` (`readRowDecision` starter object extended with `palette_group_key: ''` so a cleared input sends the empty value / unset rather than being omitted — subtle guard against setDecision's merge preserving prior value); `addons/visual-editor/assets/css/curation.css` (new `.dbvc-ve-curation__palette-key` — monospace, 11px, full-width so the slug shape reads at a glance); E-124. **R5.later-a.3 (2026-09-03)**: Small wire-through slice — 3 files, ~30 LOC total. Backend end-to-end already covered by R5.later-a's 16/48 PHPUnit suite (store + exporter + provider); this slice is the missing curator-facing surface only. **Live-verified via Claude in Chrome** against `https://dbvc-codexchanges.local/wp-admin/admin.php?page=dbvc-visual-editor-curation`: 770 palette-key inputs render (one per candidate); typing `r5_later_a3_smoke` on the `colorAccent` row fires the existing AJAX save (row transitioned `is-saving` → `is-include`), value persists across page refresh, clearing the value re-fires save and empty state survives refresh. Puzzle observed: my CLI-side `php wp-load.php + $store->getAll()` returned 0 decisions during verification while the browser rendered 400 include + 370 defer decisions — root cause is LocalWP running each site's MySQL on a per-site UNIX socket while my CLI's `mysqli_connect` uses TCP `localhost:3306` (hits Homebrew MySQL — different mysqld). Not a defect; noted as a Claude-Code + LocalWP-socket gotcha for future QA runbooks. **Validation**: PHPUnit `VisualEditor\|Curation` → **420 / 2977 OK** (unchanged; backend already covered). Drawer jsdom **53 pass** (unchanged; drawer JS untouched). Media-manager **42 preserved**. Curation admin has no jsdom coverage (never had one — the JS is a thin wrapper around AJAX); live-verification pass covers the wire-through end-to-end. PHP lint clean. No cross-repo mirror needed. No agent-docs hash rotation. **R5.later-a is now COMPLETE end-to-end**: backend (R5.later-a) + drawer render (R5.later-a.2) + live QA (R5.later-a.1) + curator-facing admin UI (R5.later-a.3). See E-124. |
| R5.later-a.2 | Drawer tree-parent gate generalisation + live-site QA CLOSED | **R5.later-a.2 landed + R5.later-a live-verified 2026-09-03**. Real-browser QA against `dbvc-codexchanges.local` with a 3-record palette seed on the `brand_color_palette>vertical_global_palette` group caught + fixed a shipping bug — the R5.7-b `isRepeaterParent` gate required `meta.role='repeater_parent'` verbatim and did NOT recognise `'palette'`, so R5.later-a palette parents surfaced as flat unsupported records and got filtered out. Fix widened the gate to a `TREE_PARENT_ROLES` whitelist so future tree-adjacent roles register without further JS change. Palette tree render + expand/collapse + persistence + leaf Open + color-picker panel mount all verified live post-fix (14/23 pass live, 8 N/A, 1 content-mutation skipped). Seed reverted after QA | Codex R5.later-a.2 / 2026-09-03 | `addons/visual-editor/assets/js/brand-control-center-app.js` (new `TREE_PARENT_ROLES = Object.freeze(['repeater_parent', 'palette'])` module constant; `isRepeaterParent` widened to whitelist membership check, function name kept for R5.7-b call-site stability, docblock updated); `tests/visual-editor-brand-control-center-state.test.cjs` (new `paletteParentItem()` + `paletteLeafItem()` helpers + new `R5.later-a: palette parent renders as tree parent with disclosure + count chip` test — parent gets `is-parent` + `aria-level=1`, disclosure + child-count chip, no Open button; children hidden while parent collapsed; toggle flips `aria-expanded` + `is-expanded` + reveals leaves; drawer jsdom 52 → 53); `docs/dropins/dbvc-visual-editor-brand-controls-guide/qa/R5-LATER-A-PALETTE-GROUPING-QA-REPORT.md` (rewritten from PENDING to CLOSED with actual live results); E-122. **R5.later-a.2 (2026-09-03)**: Small JS + jsdom slice — 1 file JS change (~15 LOC net), 1 file jsdom addition (~90 LOC). No PHP touched; no cross-repo mirror needed (`brand-control-center-app.js` is DBVC-only). PHPUnit unchanged at **420 / 2977 OK**. Drawer jsdom **53 pass** (was 52 — +1 R5.later-a new). Media-manager **42 preserved**. No agent-docs hash rotation. **Follow-ups flagged** in the QA report: (a) palette parent label ergonomics — curator's `group` field flows through as parent label, which for the QA seed rendered as the ACF group_title `Site Settings Advanced` rather than a nicer palette name; future R5.later-a.3 admin UI could pair `palette_group_key` with an optional `palette_display_label` input; (b) palette-leaf value-summary chip investigation FULLY RESOLVED via two-stage probe (E-123): storage-side probe showed every sampled color returns `''` from `get_field()` → `buildColorSummary('')` correctly returns null → empty chip is intended; drawer-side instrumentation showed all pipeline entry points reached + observer properly constructed but `handleValueSummaryIntersect` never fires because `document.visibilityState === 'hidden'` and Chrome throttles IO callbacks in background tabs (Claude in Chrome runs its automated tab in the background — reproduced 0-callback behavior with a manual IO on `document.body` from the same eval context). Direct REST probe returns valid summaries (`Default Posts` → `{family:'relationship', count:10}`). **No R4-C-1b defect exists; no fix needed**; foregrounded real users see chips render normally; (c) CurationStore + exported JSON drift — the live decisions option was empty despite the JSON carrying 400 records; pre-existing drift, ergonomics hazard for any future admin-UI curation write. **Answer to R5.later design review §3 Q2 (inline vs delegate edit) is now empirically resolved as DELEGATE** — leaf Open opens the existing R5.2+color_picker panel correctly and the save path is byte-identical, so inline editing modes in Option Y / Z offer no meaningful advantage for the color-only MVP. See E-122 + QA report. |
| R5.later-a | Palette grouping — curator opt-in (Option X from design review) | **R5.later-a landed** (curators mark sibling `color_picker` records with a shared `palette_group_key`; Vertical provider dedupe-emits ONE synthetic palette parent per key when ≥ 2 records opt in; children carry `parentPublicId` so R5.7-b's tree renderer groups them under one disclosure. Read-only in this slice — individual colors still save through R5.2+color_picker unchanged. Color-picker-only in MVP; non-color records carrying the key contribute nothing to the count. Palette-level actions per the reference screenshots — drag reorder, copy hex, inline swatch grid — deferred to R5.later-b / R5.later-y) | Codex R5.later-a / 2026-09-03 | `addons/visual-editor/src/Curation/CurationStore.php` (new `PALETTE_GROUP_KEY_MAX_LENGTH=40` constant; `normalizeDecision` gains palette_group_key branch via sanitize_key + substring cap; `emptyDecision` returns `palette_group_key=''`; `isEmptyDecision` checks the new key so a lone palette opt-in is not pruned); `addons/visual-editor/src/Curation/CurationExporter.php` (`export()` records[] shape gains `palette_group_key` passthrough); Vertical (both checkouts, byte-identical) `themes/vertical/functions/features/dbvc-visual-editor/includes/class-vf-vertical-control-provider.php` (new pre-scan `countPaletteMembers($records)` runs before main loop; new palette-grouping block in `getControls()` after the R5.7-a repeater-expansion block gates on `field_type==='color_picker'` + non-empty sanitize_key + `count>=2`; new private `buildPaletteParentRecord($key, $count, $mapped)` mints the synthetic parent with `meta.role='palette'` + `meta.childCount=N` + `status='unsupported'` + `fieldFamily='other'` + `source.role='palette_parent'`; new private `humanizePaletteGroupKey($slug)` splits on `_`/`-` and title-cases; palette parent dedup shares the existing `$parents_emitted` map with R5.7-a); `tests/phpunit/VisualEditorControlCenterR5LaterAPaletteTest.php` (new, 16/48 covering store + exporter + provider dedupe + child linking + label-fallback + non-color-picker ignore + solo-opt-in flat + two-different-keys emit two parents + hyphenated + underscore-separated humanization); E-121. **R5.later-a (2026-09-03)**: Option X from the R5.later Palette Grouping design review (lo-fi artifact `https://claude.ai/code/artifact/18ee646a-2266-4e93-b7ad-ee195c5f2b66`; sequenced pick recommendation: ship X first for the information architecture, treat Y as its natural follow-on). Reuses R5.7-b's `parentPublicId` primitive + tree renderer end-to-end — **zero drawer JS or CSS changes**. Palette parents coexist with R5.7-a repeater expansion via the same `$parents_emitted` map (repeater path `continue`s past palette grouping; palette grouping only fires on records the repeater path didn't consume). Parent label prefers the curator's `group` display string on the first-seen child; falls back to a humanised slug (`brand_primary` → `Brand Primary`, `brand-warm` → `Brand Warm`). SortKey inherits the first-seen child's so the palette parent sits where the group would naturally appear; `ControlRegistry::flattenParentChildOrder` then places children after regardless of their own sortKeys. **Live-site record status changes: zero** — R5.later-a is UX-only. When a curator sets `palette_group_key` on two or more sibling color_picker records via CurationStore, the next curation export + drawer render folds them under one palette disclosure. **Guards**: `sanitize_key` on both store ingest and provider consumption; solo opt-ins (count<2) stay flat (no palette-of-one disclosure); non-color-picker fields carrying the key are ignored in `countPaletteMembers` (color-only in MVP; future frontiers can widen the field-type gate). **Validation**: `VisualEditorControlCenterR5LaterA` → **16/48 OK**. `VisualEditor\|Curation` subset → **420/2977 OK** (was 404/2929 pre-slice — +16 R5.later-a new). Drawer jsdom **52 pass** (unchanged — R5.later-a touched no drawer JS/CSS; R5.7-b tree absorbs the new parent records without extension). Media-manager **42/42**. PHP lint clean on all three touched files. `diff -q` confirms both Vertical checkouts identical. No agent-docs hash rotation (no tracked extension points live in touched files, verified via manifest.json grep). No content mutated; no persistent WP option toggled outside test setUp/tearDown; no git operations either repo. **Next frontier decision**: Option Y (dedicated palette popup) vs Option Z (panel palette render mode) becomes actionable once §3 Q2 of the design review (inline vs delegate edit) has a live answer against real curated palette groups. `vf_palette` custom-field resolver (R5.later's Vertical-side design decision) remains a separate frontier — different data shape (one ACF field storing N colors internally vs N sibling color_picker fields folded via palette_group_key). See E-121 + release plan §R5.later. |
| R5.5 | Date picker unlock (micro-slice) | **R5.5 landed** (new AcfDatePickerResolver + wire-through pattern; 1 curated Vertical record potentially unlocks once curation tags `admin_site_verbiage>site_business_type` with `unlocks_at="R5.1"`; date_picker rides the R5.1 slot per plan doc decision). Panel side reuses existing `createInputController('date', value)` — native `<input type="date">`, zero new panel controller code | Codex R5.5 / 2026-09-02 | `addons/visual-editor/src/Resolvers/AcfDatePickerResolver.php` (new — extends AbstractAcfResolver, ISO-8601 validation via `checkdate`, two public statics `isRecognizedIsoDate` + `normalizeIsoDate`, save delegates to writeAcfValue → group + repeater nesting inherited); `addons/visual-editor/src/Resolvers/ResolverRegistry.php` (registered next to AcfTrueFalseResolver); `addons/visual-editor/src/Registry/Providers/SharedGlobalsControlProvider.php` (supportedFieldTypes widened by `'date_picker'`; new public static `buildDateSummary` returning `{family:'date', iso, label}` or null; buildValueSummary dispatch gains date_picker branch); `addons/visual-editor/src/Registry/Providers/SharedGlobalsDescriptorFactory.php` (new date_picker branch in build() + resolveRepeaterLeafFamilyConfig case); `addons/visual-editor/assets/js/overlay-app.js` (one-line `case 'date':` in createFieldController delegating to existing createInputController); `addons/visual-editor/assets/js/brand-control-center-app.js` (renderValueSummaryChip family=date branch); `addons/visual-editor/assets/css/control-center.css` (new `.__value-date` monospaced pill); Vertical (both checkouts, byte-identical) `themes/vertical/functions/features/dbvc-visual-editor/includes/class-vf-vertical-control-provider.php` (resolveStatus R5.1 branch widened by date_picker; buildDescriptor supported list widened; buildValueSummary date_picker delegation to DBVC static); `tests/phpunit/VisualEditorControlCenterR55DatePickerTest.php` (new, 25/51 tests covering resolver + summary + factory + provider with dataProvider matrix of 4 valid + 12 invalid date shapes including calendar-invalid Feb 30, month 13, day 32); `tests/phpunit/VisualEditorVerticalProviderR51BTest.php` (+2 — dedicated `test_r55_date_picker_record_maps_to_available_status` since fieldFamily='other' differs from R5.1 'text' bucket + R5.6 matrix `R5.1 date_picker` case); `tests/visual-editor-brand-control-center-state.test.cjs` (+1 R5.5 chip case, 51 → 52); `docs/agents/manifest.json` (extension-point discovery id `dbvc_visual_editor_restricted_option_group_roots` rotated `3b65b39c → e23acbb9` from ResolverRegistry line shifts); E-120. **R5.5 (2026-09-02)**: Smallest R5.x slice (~150 LOC net-new + ~250 test LOC). Follows R5.2+color_picker's wire-through pattern — reuses `createInputController` factory with native `<input type="date">` (zero new panel controller code). Date-shape validation ships as two public statics on the resolver (`isRecognizedIsoDate` + `normalizeIsoDate`) that the summary builder delegates to — same single-source pattern R5.7-a used for `buildRepeaterRowSignature` and true_false for `coerceToBool`. Group-nested writes inherit from R5.1-b's `walkGroupChain`; repeater-nested writes inherit from R5.7-a's `writeRepeaterSubfieldValue` (via `resolveRepeaterLeafFamilyConfig`'s date_picker case). Zero new REST routes, zero new mutation authority, zero new mutation contracts (routes through `shared_field` contract). **Validation**: `VisualEditorControlCenterR55DatePicker` → **25/51 OK**. `VisualEditorVerticalProviderR51B` → **46/155 OK** (was 44/148 — +2 R5.5 new). `VisualEditor(*)` subset → **404/2929 OK** (was 377/2871 — +27 R5.5 new). Drawer jsdom **52 pass** (was 51 — +1 chip case). Media-manager **42/42**. Agent docs **54/443/0** (1 discovery id rotated). PHP lint clean; `diff -q` confirms Vertical checkouts identical. **Live-site coverage after R5.5**: moves from 397/400 (99.25%) to 398/400 (99.5%) when the curated date_picker record's `unlocks_at` tag lands. Only 1 record still unsupported: `vf_palette` custom field (R5.later's Vertical-side design decision). See E-120 + release plan §R5.5. |
| R5.6 | Nested-record ancestry hardening (defensive) | **R5.6 landed** (Vertical resolveStatus refactored to apply the shared parent-chain gate uniformly across every unlocked family; helper renamed to `parentChainIsGroupOnly` for family-agnostic role. Zero live-site record status changes today — all 397 currently-Available nested records happen to have group-only chains. Purely defensive: any future repeater/flexible-nested curation now stays Unsupported at the drawer surface instead of surfacing an Open button that would silently mis-save) | Codex R5.6 / 2026-09-01 | `themes/vertical/functions/features/dbvc-visual-editor/includes/class-vf-vertical-control-provider.php` (resolveStatus refactor: compute `is_unlocked_family` boolean per family gate, short-circuit unsupported outside every family, short-circuit available for top-level, delegate to `parentChainIsGroupOnly` for nested — same shared gate for R5.1 / R5.2 / R5.2+color_picker / R5.3 / R5.4; helper renamed from `hasGroupOnlyAncestry` — private static, no external callers; docblock updated); mirrored byte-identically to both Vertical checkouts (`dbvc-codexchanges` + `frameworkflo-live`) via `cp` + `diff -q`; `tests/phpunit/VisualEditorVerticalProviderR51BTest.php` (+8 test methods / 5 existing nested-family tests seeded with new `seedGroupOnlyChain($leaf_key, $leaf_type)` helper so their family-gate intent stays intact under the broadened ancestry gate; 3 previously-implicit "stays unsupported" tests now explicitly seed a repeater-in-chain fixture via new `seedRepeaterInChain($leaf_key, $leaf_type)` helper with updated docblock comments replacing stale "R3-C-1 factory doesn't yet emit group_write_path" reasoning; new 16-case `test_repeater_in_chain_blocks_every_unlocked_family` matrix `@dataProvider unlockedFamilyMatrixProvider` covers every (unlocks_at, field_type) pair the drawer currently supports); class docblock extended with an R5.6-specific paragraph; E-113. **R5.6 (2026-09-01)**: Post-nested-reference-slice, three latent bugs shared the same shape — R5.1 text, R5.2 choice/link/wysiwyg/true_false, R5.2+color_picker, and R5.3 image nested branches all called `walkGroupChain` (R5.1-b helper) without checking that every intermediate is a Group field. A repeater-nested record in any of these families would emit `group_write_path` and route through `writeGroupedFieldValue`, which addresses the wrong meta key for repeater subfields. All 397 currently-Available nested records in production happen to have group-only chains (surveyed), so the bug never fired — but any future curation addition inside a repeater would. R5.6 lifts the R5.4 branch's ancestry gate into a shared post-family-check that runs unconditionally: (1) is this record's family unlocked? (2) is it top-level? if so, available. (3) otherwise, does the ACF parent chain via `acf_get_field()` walk to a `group_XXX` boundary with every intermediate `type='group'`? Only then available; else unsupported. The R5.4 branch's family-specific `hasGroupOnlyAncestry` call disappears; the helper is renamed `parentChainIsGroupOnly` to reflect its family-agnostic role (private static — no external callers to migrate). Fail-safe unchanged: `parentChainIsGroupOnly` returns false when ACF isn't loaded, when the field lookup fails, or when any intermediate isn't a group. **Test surface**: 5 existing nested-family tests were previously passing because their fixtures didn't touch `$GLOBALS['vfe_test_acf_fields']` — after the broadening those tests need explicit group-only chain seeds. Three previously-passing "stays unsupported" tests were relying on an empty ACF stub (parentChainIsGroupOnly's ACF-not-loaded fallback returns false); those now explicitly seed a repeater-in-chain fixture so the intent is documented rather than accidental. The new matrix test covers every unlocked (unlocks_at, field_type) pair — 16 cases including R5.1 text/textarea/url/email/number, R5.2 select/checkbox/radio/button_group/link/wysiwyg/true_false, R5.3 image, R5.2+color_picker color_picker, R5.4 relationship/post_object. **Deferred**: DBVC-side factory rejection as defense-in-depth layer #2 (see R5-REMAINING-UNLOCK-FRONTIERS.md §R5.6 step 5) was deliberately not added because Vertical resolveStatus runs unconditionally before any descriptor mint, and duplicating the check on both sites without a discovered case of it firing is premature abstraction. **Validation**: `vendor/bin/phpunit --filter "VisualEditorVerticalProviderR51B"` → **39 / 105 OK** (was 31 / 91 pre-slice — +8 new). `vendor/bin/phpunit --filter "VisualEditor"` → **349 / 2714 OK** (full VisualEditor PHPUnit subset green). `node --test tests/visual-editor-brand-control-center-state.test.cjs` → **44 pass** (unchanged — R5.6 touched no drawer JS/CSS; the drawer already renders whatever `status` a provider returns). `node --test tests/visual-editor-media-manager-state.test.cjs` → **42 pass** (preserved). PHP lint clean on Vertical provider + test file. `diff -q` confirms both Vertical checkouts identical. No content mutated; no persistent WP option toggled outside test setUp/tearDown; no git operations either repo. **Live-site coverage** remains at 397–398 of 400 (same as post-nested-reference-slice — R5.6 is defensive, not additive). The unlock frontier is now cleanly split: R5.5 (date_picker, 1 record — micro-slice), R5.7 (repeater/flexible actual capability — substantial), R5.later (`vf_palette`, 1 record — Vertical-side design decision). See E-113 + planning doc `releases/R5-REMAINING-UNLOCK-FRONTIERS.md`. |
| R5.7 | Repeater / flexible-content nested support (inline tree — Option C-1) | **R5.7 complete** (R5.7-a backend + Vertical unroll + row-signature integrity 2026-09-01; R5.7-b drawer tree UI 2026-09-02; R5.7-c release notes + QA plan + live-site QA 2026-09-02). Real-browser QA CLOSED in the defensive-infrastructure sense — 7/22 pass live, 13 N/A on current curated set (the `menus > menu_posts` record turns out to route via R5.4 group-only path since `menus` is a Group, not a Repeater), 2 pending operator authorization for content-mutation items. R5.7 shipped correctly-dormant defensive infrastructure for any future repeater-nested curation. R5.7-C-2 (drag reorder) + R5.7-C-3 (roll-up summaries) + R5.7-C-4 (expand-all controls) deferred | Codex R5.7-a + R5.7-b + R5.7-c / 2026-09-01..02 | `addons/visual-editor/src/Registry/Providers/SharedGlobalsControlProvider.php` (new public static `buildRepeaterRowSignature($row)` + private `normalizeSignatureValue`); `addons/visual-editor/src/Registry/Providers/SharedGlobalsDescriptorFactory.php` (new public `buildRepeaterSubfieldDescriptor()` method + private `resolveRepeaterLeafFamilyConfig()` 10-family dispatch); `addons/visual-editor/src/Resolvers/AbstractAcfResolver.php` (new protected `verifyExpectedRowSignature($descriptor, $row)` + hook inside existing `writeRepeaterSubfieldValue` between validation and post-meta fallback); Vertical (both checkouts, byte-identical) `themes/vertical/functions/features/dbvc-visual-editor/includes/class-vf-vertical-control-provider.php` (new private static `parseParentChain()` shared walker; `parentChainIsGroupOnly` refactored to thin wrapper; new private static `parentChainImmediateRepeaterInfo()`; `getControls()` runs `maybeExpandRepeaterRecord()` for repeater-nested candidates; new private `pickRepeaterRowDisplayLabel()` heuristic; `buildDescriptor()` recognizes `source.role='repeater_leaf'` and routes through new `buildRepeaterLeafDescriptor()`; `buildValueSummary()` recognizes role via new `extractRepeaterLeafValue()`); `tests/phpunit/VisualEditorControlCenterR57ATest.php` (new, 18/60); `tests/phpunit/VisualEditorVerticalProviderR51BTest.php` (+5 expansion cases + `get_field` stub + `seedRepeaterChain` helper); `docs/dropins/dbvc-visual-editor-brand-controls-guide/releases/R5.7-INLINE-TREE-IMPLEMENTATION-PLAN.md` (planning doc — R5.7-a annotation added); E-114 (Q2 spike outcome) + E-115 (implementation). **R5.7-a (2026-09-01)**: First slice of the Option C-1 inline-tree implementation plan (chosen from the four-option design review; product/UX-recommended shape). **1 curated record unlocks live-site**: `settings_nav_menus > menus > menu_posts` now surfaces as one drawer row per configured menu row (up to N depending on the maintainer's ACF setup). Q2 row-identity spike outcome: positional `row_index` + `expected_row_signature` (sha1 hash of row data) chosen over raw-meta row-key extraction — signatures catch concurrent reorders AND concurrent edits cleanly; row-key extraction adds complexity for a case the signature already handles. **DBVC**: `SharedGlobalsControlProvider::buildRepeaterRowSignature($row)` is a deterministic sha1 (recursive `ksort`, strips ACF `acf_row_*` transient keys, handles nested arrays). `SharedGlobalsDescriptorFactory::buildRepeaterSubfieldDescriptor()` mints descriptors with `container_type='repeater'` and `source.type` branching on family: `acf_collection_field` + `render.context='query_collection'` for reference leaves (matches `AcfReferenceCollectionResolver::supports`), `acf_repeater_subfield` + `render.context='field'` for scalar leaves (matches `AbstractAcfResolver::isRepeaterSubfieldSource`). `parent_field_selector` uses the repeater's KEY so ACF key-based lookup handles group-nested repeaters. Source embeds `row_index` + `expected_row_signature` + all normal per-family metadata. `resolveRepeaterLeafFamilyConfig()` supports 10 families: text-family (5), choice-family (4), link, wysiwyg, image, color_picker, true_false, relationship, post_object. `AbstractAcfResolver::verifyExpectedRowSignature()` compares descriptor's signature against a freshly-computed one and returns structured `{ok:false, code:'concurrent_reorder_detected', message}` on mismatch; hook called inside `writeRepeaterSubfieldValue`; legacy descriptors without a signature skip the check (backwards-compat). **Vertical (both checkouts)**: `parseParentChain()` shared walker; `parentChainIsGroupOnly` refactored; new `parentChainImmediateRepeaterInfo()` returns repeater info when the leaf's immediate parent is a repeater AND every ancestor above is a group (MVP: rejects repeater-in-repeater and flexible-in-chain). `getControls()` runs `maybeExpandRepeaterRecord()` — when a record is a repeater-nested candidate + `get_field($repeater_key, 'option', false)` returns non-empty rows, emits one parent record (dedup'd across sibling subfield curation entries; `meta.role='repeater_parent'`, `meta.childCount=N`, `status='unsupported'` so drawer today renders it muted) + N leaf records (`source.role='repeater_leaf'`, `source.row_index`, `source.expected_row_signature`, `source.repeater_field_name`+`_key`, `parentPublicId` linking to parent). Row display label heuristic picks first non-empty scalar subfield (truncated 40 chars) or falls back to `Row N`. Empty repeaters emit nothing (R5.6 unsupported behavior preserved). `buildDescriptor()` routes repeater leaves through `buildRepeaterLeafDescriptor()` → lifts parent + subfield ACF field objects → delegates to DBVC factory. `buildValueSummary()` routes repeater leaves through `extractRepeaterLeafValue()` → lifts row + subfield value → delegates to family static builder. `ControlRecord::fromArray` currently drops the `parentPublicId` key (silently) — R5.7-b will add the field for tree rendering. **Interim UI**: drawer surfaces N+1 flat records (Option-A-like); fully functional (Open + edit + Save works with row-signature integrity). **Validation**: `VisualEditorControlCenterR57ATest` → **18/60 OK**. `VisualEditorVerticalProviderR51B` → **44/148 OK** (was 39/105 — +5 R5.7-a new). `VisualEditor(*)` subset → **372 / 2850 OK** (was 349 / 2714 — +23 new). Drawer jsdom **44 preserved** (no drawer JS/CSS touched — interim UI uses existing flat renderer). Media-manager **42/42**. Agent docs **54/443/0** (unchanged). PHP lint clean on every touched file. `diff -q` confirms both Vertical checkouts identical. See E-114 (spike) + E-115 (implementation) + release plan §R5.7-a. **Live-site coverage after R5.7-a**: 398+ of 400 (399+ Vertical records total; exact count depends on how many rows the maintainer configured in the `menus` repeater). **R5.7-b (2026-09-02)**: drawer tree UI lands on top of R5.7-a's backend. `ControlRecord` gains public `$parentPublicId` + case-preserving `sanitizeMetaKey` (fixes silent lowercasing of `childCount`/`unlocksAt` in the safe projection). `ControlRegistry::collectValidRecords` runs a new private `flattenParentChildOrder($sorted)` DFS pass — children emit immediately after their parent even against label-alpha sort. `ControlCenterListController` bumps `viewModelVersion` 2→3. `brand-control-center-app.js` gains `state.expandedRows` (persisted via `LS_KEY_EXPANDED_ROWS`) + `state.searchAutoExpandedRows` (transient). Tree helpers: `isRepeaterParent`, `isTreeChild`, `isRowExpanded`, `toggleTreeRow`, `buildParentChildIndex`, `recomputeSearchAutoExpansion` (Notion-pattern ancestor walk + parent-label-match self-expand). `filteredItems` refactored to two-pass tree-aware filter (parents short-circuit past status/priority; leaves hidden when parent collapsed; parents with 0 visible children drop away). `renderRow` extended with `is-parent`/`is-child`/`is-expanded` classes + ARIA `role=treeitem` + `aria-level=1|2` + `aria-expanded` on parents; `renderTreeDisclosure` `<button>` with rotating chevron + i18n aria-label; `renderTreeChildCountChip` in the action cell in place of Open. New `handleTreeKeydown` root listener (ArrowRight expand / ArrowLeft collapse on parents; ArrowUp/Down move focus between visible rows). `createTableWrap` gives the table `role="tree"` + `aria-label`. New CSS: `.__row.is-parent`, `.__row.is-child` (indent + faint hairline connector), `.__row-disclosure` (rotating chevron on `.is-expanded`), `.__row-childcount` (pill), `prefers-reduced-motion` suppression. AssetLoader gains 4 new i18n keys (`controlCenterTreeExpand`/`Collapse`/`ChildCountOne`/`ChildCountMany`). **Validation**: `VisualEditorControlRegistry` → **16/78 OK** (was 11/44 — +5 R5.7-b new). `VisualEditor` subset → **377/2871 OK** (was 372/2850 — +5 net). Drawer jsdom **51 pass** (was 44 — +7 R5.7-b new). Media-manager **42/42**. Agent docs **54/443/0** (1 discovery id rotated for `dbvc_visual_editor_control_registry_invalid` from ControlRegistry line shifts; re-mapped). PHP lint clean on every touched file. **Live-site UX after R5.7-b**: R5.7-a's N+1 flat repeater rows now fold into 1 compact parent row that expands to reveal per-menu leaves — the intended Option C-1 tree render. Zero new REST routes, zero new mutation authority, zero new mutation contracts. See E-116 + release plan §R5.7-b. **R5.7-c (2026-09-02)**: pure docs slice — release notes + real-browser QA plan. `BRAND-CONTROL-CENTER-RELEASE-NOTES-AND-ROLLBACK.md` title bumped to `(R3-D + R5.7)`; new R5.7 block appended covering per-slice "What shipped", feature-gates cross-referenced to specific test names, side-effects+boundaries, deferred residuals (R5.7-C-2/3/4 + multi-level nesting + full Notion keyboard nav + real-browser QA), 7-step rollback runbook (staged: UI-only revert of R5.7-b preserves R5.7-a save integrity + Option-A-flat fallback; full revert returns to R5.6 muted state), verification snapshot pinning all test counts. New `qa/R5-7-TREE-BROWSER-QA-REPORT.md` mirrors R3-DRAWER-BROWSER-QA-REPORT.md shape — header marks "PENDING — awaiting live-site verification", 22-item checklist covering drawer regression + R5.7-a interim-state verification via DevTools network + R5.7-b tree render + click-toggles-expansion + keyboard nav (ArrowRight/Left/Up/Down) + leaf Open + edit+save persists to correct row + concurrent-reorder detection scenario + reduced-motion + table role=tree + localStorage persistence + search auto-expand ancestors + parent-label-match self-expand + row-only-carries-data-public-id invariant + single-polite-live-region + viewModelVersion=3 + 4 regression checks (397 pre-R5.7 records + Shared Globals popover + Media Manager + list-endpoint version). **No code changes** — all test counts stay identical to E-116 (`VisualEditor(*)` **377/2871 OK**, drawer jsdom **51 pass**, media-manager **42 pass**, agent docs **54/443/0**). See E-117 + release plan §R5.7-c + QA report. **Live-site QA is the sole residual gate** for R5.7 (matches R3-D residual shape that closed 2026-08-29). |
| R6 | Frontend Site Manager Workspace | Not started |  |  |  |

## R0 checklist

- [x] Branch, status, recent commits, and uncommitted work recorded
- [x] Current Visual Editor architecture mapped
- [x] Existing media scanner/catalog evidence mapped
- [x] Featured-image/image/gallery read/write paths traced
- [x] Non-rendered descriptor strategy verified (confirmed absent generically; narrow R2 bridge required)
- [x] Eligible entity matrix completed
- [x] Media field/path matrix completed
- [x] Performance baseline recorded (enumeration/applicability only; full R1 scanner baseline remains gated)
- [x] Shared Globals/option support matrix completed
- [x] VerticalFramework evidence and exact paths recorded
- [x] Corrected R1/R2 planning sequence reconciled (does not authorize implementation)
- [x] Risks, decisions, rollback, and test gaps updated

### R0 stop line

- Production code changed: **No**
- DB/runtime writes performed: **No**
- R1 implementation authorized: **No**
- Reconciled Git checkpoint: **clean/synchronized `codex/visual-editor-linked-posts-plan` at `5db4b40` when this documentation refresh began**
- Known validation limitations: **6 deterministic PHP failures out of 684 tests; full JavaScript lint did not complete**
- Next crossing line: explicit user authorization for R1-A after refreshing Git status/HEAD and confirming the inherited test/lint baseline

## R1 Media Manager scan/report

### Policy/catalog

- [x] Object eligibility policy implemented/reused
- [x] Post type/taxonomy exclusions verified
- [x] ACF location/field catalog implemented/reused
- [x] Featured-image eligibility verified
- [x] Nested/conditional policy documented
- [x] Capability filtering covered

### R1-A review stop line

- Production scope: **default-off setting, policy, catalog, raw-value classification, focused tests, and required docs/agent-reference maintenance only**
- Explicitly absent: **scanner, snapshot, REST/AJAX, frontend assets/UI, descriptors, and mutation**
- Focused validation: **5 tests/65 assertions for R1-A; combined Visual Editor focus 12 tests/80 assertions; touched PHP syntax clean**
- Full comparison: **689 PHP tests/7,186 assertions with the same six inherited failure identities and no new failure**
- Remaining validation limitation: **full JavaScript lint did not complete at the inherited checkpoint and was not rerun because R1-A touches no JavaScript**
- Recorded crossing line: **R1-B was explicitly authorized after review of this policy/catalog contract and is now complete for review**

### Scan/session

- [x] Minimal bounded coordinator and separate snapshot implemented (no compatible existing scanner/session found)
- [x] Start/progress/next/complete states
- [x] User/site-bound snapshot
- [x] Expiry/cancel/retry behavior
- [x] Duplicate/stale response handling
- [x] Summary counts and finding identity
- [x] Representative performance measured

### R1-B review stop line

- Production scope: **bounded candidate provider, supported-value scanner, exact group-only traversal, deterministic opaque identity/fingerprints, compressed transient snapshot store, coordinator lifecycle/revision/config invalidation, performance metrics, focused tests, and docs maintenance only**
- Explicitly absent: **REST/AJAX controller/routes, public list/search/filter/pagination, row expansion/revalidation, descriptors, frontend assets/UI, and mutation**
- Focused validation: **5 tests/106 assertions for R1-B; combined R1-A/R1-B 10/171; combined current Visual Editor focus 17/186; touched PHP syntax clean**
- Representative chunk: **20 entities, 60 findings, 4.661 ms, 24 queries, zero additional allocated/peak memory pages at reported granularity, 4,983-byte compressed snapshot**
- Full comparison: **694 PHP tests/7,302 assertions with the same six inherited failure identities and no reproducible new failure; an initial extra Phase 4 failure passed in isolation, passed with its 32-test class, and disappeared on the clean full rerun**
- Agent documentation: **54 curated records; 408 discovered surfaces; zero unmapped**
- Remaining validation limitation: **R1-B changes no JavaScript; the inherited full JavaScript lint command still has no completed result**
- Recorded crossing line: **R1-C was explicitly authorized after review of the scanner/snapshot contract and is now complete for review**

### Read model/UI

- [x] Search/filter/sort/pagination
- [x] Row expansion revalidation
- [x] Safe display records and opaque references
- [x] UI requirements brief
- [x] Claude static mockup delivered/reviewed
- [x] Accepted/adapted/rejected design decisions recorded
- [x] D4A contract/state/responsive/interaction corrections accepted
- [x] Feature-gated toolbar entry and responsive shell implemented
- [x] API/state controller, request identity, state adapter, and stale-response suppression implemented
- [x] Production scrollable laptop/desktop table implemented
- [x] Table loading/empty/no-match/list-error/append-error states
- [ ] Keyboard and supported laptop/desktop QA; additional touch/mobile/responsive work tabled by D-036
- [ ] Bricks Builder isolation
- [ ] Feature flag/fallback/release notes

### R1-C review stop line

- Production scope: **protected active-mode start/latest/list/next/retry/cancel/group routes, safe lifecycle/result projections, bounded search/filter/sort/opaque-cursor retrieval, list-time object permission/status rechecks, single-owner row rescanning, focused tests, and required docs/agent-reference maintenance only**
- Explicitly absent: **frontend assets/table, descriptor issuance, Media Library selection/upload, content mutation, journal writes, cache invalidation, and R2 actions**
- Focused validation: **6 tests/417 assertions for R1-C; combined R1-A/R1-B/R1-C and current Visual Editor instrumentation 23/603; touched PHP syntax clean**
- Full comparison: **700 PHP tests/7,723 assertions with exactly the same six inherited failure identities and no new failure**
- Agent documentation: **54 curated records; 415 discovered surfaces; zero unmapped**
- Security boundary: **browser payloads contain no owner IDs/subtypes, ACF object IDs, field keys/names/selectors/paths, empty/configuration fingerprints, raw values, descriptors/tokens, or mutation actions**
- Remaining release gates: **production frontend shell/table, shared subtle-text contrast correction, browser/accessibility/Builder QA, large-site/payload profiling, feature fallback/release evidence**
- Remaining validation limitation: **R1-C changes no JavaScript; the inherited full JavaScript lint command still has no completed result**
- Recorded crossing line: **R1-D was explicitly authorized for Claude static mockups and frontend-table translation against the frozen R1-C view model**

### R1-D authorization, static direction, and production crossing line

- Authorization: **R1-D approved by the user on 2026-08-15; Claude completed the separately reviewed D1-D4 static-mockup sub-phases**
- Context optimization: **the minimal launcher delegates to one self-contained context capsule and one R1-C-safe fixture; broader package/source reads are exception-only**
- Production scope at D4A handback: **No; the delivered files are static documentation/mockup artifacts and add no production frontend asset, component, route, descriptor, or mutation code**
- Claude D1-D4 state: **complete; 15-file read-only static mockup delivered under `docs/ui-mockups/dbvc-visual-editor/r1-media-manager/` and reviewed by Codex**
- Review decision: **D4A accepted as qualified static visual direction; production must use the authoritative read model and a narrow module, not copy the fixture or local mockup script**
- D4A corrections: **complete—contract-valid sort/pagination/timestamp/expanded fields; explicit backend-to-view state adapter; distinct expansion 404 versus provider-unavailable states; reachable 390x844/375x667/320x568 layout; corrected announcements/count copy/touch targets/ARIA/order; seven refreshed screenshots and reconciled active docs**
- D4A validation: **30/30 structural checks, `node --check`, seven true-viewport captures, and package 46/46 integrity passed. The original default view exposed one serious shared-token contrast violation; after the authoritative token correction and screenshot refresh, axe-core 4.11.0 returned 33 passing rule groups/0 violations/38 incomplete for `index.html` and 22 passes/0 violations/0 incomplete for `states.html`. No assistive-technology, Safari/Firefox, real-device, or authenticated production-integration pass is claimed.**
- Accessibility qualification: **the confirmed failures trace to the shared production `--dbvc-ve-color-text-subtle` token; RK-036 owns its production correction. RK-035 remains open for broader production accessibility evidence.**
- Recorded crossing line: **production translation slice 1 was authorized for the feature-gated toolbar entry/shell boundary and is now complete for review**

### R1-D production slice 1 review stop line

- Production scope: **feature-gated Media Manager toolbar entry; separate scoped CSS/JavaScript shell; localized feature/config strings; non-modal close/Escape lifecycle; trigger `aria-expanded` and focus restoration; inherited active-mode and Builder asset guards**
- Explicitly absent: **scan/list/group requests, API/state controller, result rows/table, descriptors, Media Library actions, content mutation, journal writes, and cache invalidation**
- Focused validation: **4 PHPUnit tests/32 assertions; PHP and JavaScript syntax clean; disabled and Builder asset gates covered**
- Isolated browser validation: **1440x900, 1440x600, 390x844, and 320x568 geometry/focus pass; no horizontal overflow; 44px short/mobile close target; shell axe 13 passes/0 violations at every viewport**
- Shared contrast correction: **authoritative and mirrored subtle token now use the 60% mix; post-correction axe reports 0 violations on both mockup pages, while 38 incomplete default-view contrast nodes keep broader accessibility evidence open**
- Full comparison: **704 PHP tests/7,755 assertions with the same six inherited failures; repository `npm run lint` completed with stale browser-data warnings; agent docs passed 54 curated records/415 discovered surfaces/0 unmapped**
- JavaScript qualification: **the repository lint script does not include raw Visual Editor assets; touched files pass direct Node syntax checks and the isolated Chromium harness**
- Viewport priority: **preserve the proven responsive baseline, but pause additional mobile-friendly layouts, responsive cards, touch refinements, real-device optimization, and mobile-specific QA; current implementation targets normal laptops and desktops per D-036**
- Recorded crossing line: **R1-D production slice 2 was authorized by the user on 2026-08-16 and is complete for review**

### R1-D production slice 2 review stop line

- Production scope: **shared API-client methods for latest/start/list/next/retry/cancel; first-open resume; bounded query normalization; copied safe state; generation/revision identity; explicit backend/no-scan/error/stale mapping; server-authorized action visibility; out-of-order/older-revision suppression; lifecycle-only status/actions**
- Explicitly absent: **result rows/table, cursor append rendering, row expansion, descriptors, Media Library actions, content mutation, journal writes, cache invalidation, and additional responsive/mobile work**
- Focused JavaScript validation: **5 state/route tests pass, covering current-scan resume, exact request identity/query, all backend-state mappings, latest 404, revision conflict, and slower-response suppression**
- Isolated browser validation: **1440x900 and 1280x720 latest/start/next/list flow; exact nonce and identity; safe list retained without row DOM; geometry and Escape/focus pass; shell axe 13 passes/0 violations at each viewport**
- Full comparison: **704 PHP tests/7,755 assertions with exactly the same six inherited failures; repository lint including all touched Visual Editor JavaScript and the focused state test completed with stale browser-data warnings only; agent docs passed 54 curated records/415 discovered surfaces/0 unmapped**
- Next implementation line: **R1-D production slice 3—server-driven laptop/desktop result table for search/filter/sort, replacement/cursor append, loading/empty/error state, and preserved query/scroll state; no expansion yet and no responsive cards/mobile refinement**

### R1-D production slice 3 review stop line

- Production scope: **semantic internally scrollable laptop/desktop table over safe R1-C rows; server-driven search/entity/field/six-sort query; first-page replacement; opaque-cursor append with group-reference de-duplication; explicit loading/no-findings/no-match/list-error/append-error/retry states; preserved query and append scroll**
- Explicitly absent: **row expansion/group calls, descriptors, Media Library actions, content mutation, journal writes, cache invalidation, and additional responsive/mobile work**
- Focused JavaScript validation: **8 state/DOM tests pass for safe row projection, request identity, state/error mapping, search/filter/sort replacement, cursor append/de-duplication, scroll retention, and stale suppression**
- JavaScript static validation: **the new module and both new test files pass targeted `wp-scripts` lint with stale browser-data warnings only; the aggregate repository command did not complete during a bounded rerun, so no full-lint pass is claimed**
- Focused PHP validation: **4 tests/37 assertions pass for feature/Builder asset gates, toolbar/focus/read-only seams, safe table markers, and scoped contrast/styles**
- Isolated browser validation: **2 Playwright tests pass at 1440x900 and 1280x720 for 15-to-25 row paging, append scroll, server replacement/ARIA sort, exact nonce/identity/query/cursor, no expansion control, bounded geometry, Escape/focus, and axe WCAG A/AA with zero violations**
- Full comparison: **704 PHP tests/7,760 assertions with exactly the same six inherited failure identities and no slice-3 regression**
- Evidence boundary: **browser responses are mocked; authenticated WordPress runtime, large-scale profiling, assistive technology, and cross-browser evidence remain R1-E work**
- Next implementation line: **R1-D production slice 4—lazy row expansion and pending/current/changed/resolved-or-changed/provider-unavailable/request-error presentation over the existing server-owned group route; no descriptor or mutation bridge**

### R1-D production slice 4 review stop line

- Production scope: **nonce-authenticated group client; current scan/generation/revision plus opaque group identity; one-row lazy expansion; independent pending/error sequencing; safe normalized current/changed/resolved-or-changed/provider-unavailable field projections; accessible row button and labeled detail region; collapse/new-row stale suppression; preserved list and table scroll**
- Explicitly absent: **descriptor hydration, Media Library choose/upload, content mutation, journal writes, cache invalidation, full-page/table reload logic, and additional responsive/mobile work**
- Focused JavaScript validation: **10 state/DOM tests pass for group identity, all field status families, unknown-key stripping, forced false mutation actions, one-row behavior, late-response suppression, isolated group errors, table queries/paging, and backend-state mapping**
- JavaScript static validation: **targeted and aggregate repository `wp-scripts` lint plus direct Node syntax pass with stale Baseline/Browserslist data warnings only**
- Focused PHP validation: **4 tests/41 assertions pass for feature/Builder asset gates, toolbar/focus/read-only seams, group-client/expansion markers, absence of `wp.media`/write actions, and scoped styles**
- Isolated browser validation: **2 Playwright tests pass at 1440x900 and 1280x720 for exact group identity, expanded status/ARIA/region/collapse behavior, no assignment action, retained paging/scroll/geometry/focus, and expanded/collapsed axe WCAG A/AA with zero violations**
- Full comparison: **704 PHP tests/7,764 assertions with exactly the same six inherited failure identities and no slice-4 regression**
- Evidence boundary: **browser responses are mocked; authenticated WordPress runtime, large-scale profiling, assistive technology, and cross-browser remain R1-E gates**
- Next implementation line: **R1-E hardening; after that R2-A descriptor bridge, R2-B native Media Library choose/upload in the loaded table context, and R2-C field-level save plus targeted no-reload finding/count reconciliation**

### R1-E initial hardening checkpoint

- Production correction: **restore focus to the current replacement row button after loading/success/collapse table-body rerenders without moving the results scroller; suppress the disclosure transition under reduced-motion preference**
- Keyboard/browser validation: **Enter expands and Space collapses at 1440x900 and 1280x720; row focus remains stable; expanded/collapsed axe remains zero violations; reduced-motion computed transition is `0s`**
- Static/automated validation: **10 jsdom tests pass including focus continuity; focused R1-D PHP passes 4 tests/43 assertions; targeted and aggregate lint pass with stale dependency-data warnings; agent docs and package integrity pass after reconciliation**
- Current broad comparison: **704 PHP tests/7,764 assertions immediately before this frontend-only correction, with exactly the same six inherited failure identities**
- Still open: **authenticated active-site REST/table proof, complete candidate traversal/raw-read performance where fixtures permit, assistive-technology review, Safari/Firefox, and final release/fallback evidence**

### R1-E synthetic scale and no-auto-scan checkpoint

- Added `VisualEditorMediaManagerR1ETest` with ordinary frontend enqueue/no-scan proof and 100/500/2,000-group compressed snapshot/read/payload measurements.
- Focused R1-E passes 2 tests/50 assertions; combined R1-A through R1-E passes 22/681.
- Full PHP comparison passes all new coverage while retaining exactly the inherited six failures: 706 tests/7,816 assertions.
- Combined 2,000-group evidence: 25.425 ms; 0 additional WordPress queries; 6,291,456 allocated-memory bytes; 120,475 stored bytes; 24,833 response bytes; 50 returned rows.
- Boundary: synthetic groups share one eligible owner to isolate snapshot/list/payload scale. Complete candidate enumeration, owner-specific capability/raw ACF reads, authenticated REST/browser transport, AT, and cross-browser work remain open.
- Runtime provenance was refreshed read-only: active site `dbvc-codexchanges.local`, active plugin checkout, `vertical` child theme over Bricks. The persistent Visual Editor option is off and Media Manager option is on; the available browser session is logged out. No option, session, or credential state was changed.
- Next implementation line: **continue R1-E with authenticated runtime and representative scale checks; do not cross into R2 descriptor or mutation authority until the remaining release gate is reviewed**

### R1-E automated semantic and fallback checkpoint

- Production semantics: **the dialog references visible title/description; the results heading and focusable scroll region have stable names; list/group loading exposes polite atomic status and busy state; and the entity-specific expansion heading persists across loading, success, and error**
- Announcements: **the existing stable live region reports field-check start, completion status counts, and request failure without exposing descriptor/owner/path targets**
- Focused validation: **11 jsdom tests, 4 R1-D PHP tests/47 assertions, targeted Media Manager lint, and 6 Playwright cases across Chromium/Firefox/WebKit at 1440x900/1280x720 pass; expanded/collapsed axe remains zero confirmed WCAG A/AA violations**
- Broad comparison: **combined R1-A through R1-E passes 22/685; full PHP runs 706/7,820 with the same six inherited failures; the latest aggregate repository lint rerun did not complete and is not promoted to a pass**
- Fallback: **the existing default-off and Builder gates omit the Media Manager module/entry, ordinary frontend enqueue creates no scan, and R1 has no content/data migration or write path**
- Evidence boundary: **automated DOM/ARIA/axe/browser-engine evidence is not real assistive-technology or real Safari proof. Authenticated WordPress runtime, VoiceOver or equivalent, real Safari, complete candidate traversal/raw reads, and aggregate lint completion remain open**
- Next implementation line: **finish or explicitly accept the remaining R1-E runtime/AT/browser/performance/lint gates before crossing into R2-A descriptor issuance**

### R1-E closeout checkpoint (2026-08-16)

- Re-verified focused evidence before trusting older counts: 11/11 Media Manager jsdom tests, targeted Media Manager lint (stale Baseline/Browserslist warnings only), and 6/6 Playwright cases across Chromium/Firefox/WebKit at 1440x900/1280x720.
- Runtime provenance refreshed read-only (E-050): active site `dbvc-codexchanges.local`, `bricks`+`vertical` theme, DBVC plugin active, Media Manager option ON. The persistent Visual Editor option is now ON (drift from the previously recorded OFF; recorded, not reverted per D-043). No option/login/content/LocalWP state changed.
- Live REST auth-enforcement proven unauthenticated (E-050): all seven Media Manager routes registered; `scans/latest`, tampered scan/group refs, and POST `scans` each return HTTP 401 `rest_forbidden` with no data and no snapshot created — auth enforced before resource resolution.
- Complete candidate-traversal/raw-read scale added (E-051): new `test_complete_candidate_traversal_and_raw_reads_scale_without_field_definition_n_plus_one` drives the real provider/scanner/store pipeline to completion across 100 and 300 live owners, proving per-owner raw reads constant at 2, applicability evaluated once per candidate, max 50 candidates and <=1 source query per chunk, and per-candidate DB cost falling ~1.25 -> ~0.83 as owners triple (no N+1). Focused R1-A-R1-E now passes 23 tests/1,127 assertions.
- Full PHP comparison (E-052): 707 tests/8,262 assertions with exactly the six inherited failures (+1 test/+442 assertions over 706/7,820 is the new traversal test; no new regression).
- Aggregate lint: one bounded attempt on 2026-08-16 ran ~11 minutes without completing and was stopped; not promoted to a pass (RK-032).
- Accepted residual R1-E gates: authenticated active-site REST/table **data** behavior (no authorized session available), real VoiceOver/assistive technology, real Safari (WebKit engine is not Safari), large-list browser responsiveness, and a completing aggregate JavaScript lint run.
- Fallback/rollback unchanged: default-off asset/entry gates, no-auto-scan, and no content/data migration remain the R1 rollback contract.
- Next crossing line: **R1 sign-off (or explicit acceptance of the residual gates above) before R2-A descriptor-bridge issuance. No descriptor/Media Library/mutation work performed in this closeout.**

## R2 Media Manager remediation

### R2-A descriptor bridge review stop line

- Production scope: **`MediaFindingDescriptorBridge` plus a protected `POST .../scans/{scan_ref}/groups/{group_ref}/findings/{finding_ref}/descriptor` route that exchanges one opaque finding for one fresh standard `EditableDescriptor`. It resolves the owner/field only from the user/site-bound snapshot, revalidates snapshot identity/owner status/capability/field applicability/field family/empty value, routes to exactly one existing resolver family (featured/ACF image/ACF gallery), and persists the descriptor via the narrow `EditableRegistry::persistDetachedDescriptor()`. The response returns only opaque token/session ids plus safe labels/status.**
- Explicitly absent: **Media Library selection/upload, staged selection, `wp.media`, value hydration, content mutation, journal writes, cache invalidation, and any exposure of owner ids/field keys/selectors/ACF object ids/paths/fingerprints.**
- Focused validation: **11 R2-A tests/200 assertions (three writable families and correct single-resolver routing, server-resolved selector/group-path carriage, no-raw-target projection, user-bound session isolation, tampered/malformed refs, stale generation/revision, expired snapshot, populated-after-scan `resolved`, changed-evidence `changed`, unpublished/deleted owner `unavailable`); combined R1-A-R1-E plus R2-A 34/1,327; touched PHP syntax clean.**
- Full comparison: **718 PHP tests/8,462 assertions with exactly the same six inherited failures (+11 tests/+200 assertions is R2-A; no new regression).**
- Agent documentation: **54 curated records; 416 discovered surfaces (+1 for the new route); 0 unmapped; the shifted session-compression hook and the eight Media Manager routes were remapped in `manifest.json`.**
- Recorded crossing line: **R2-B native Media Library choose/upload in the loaded table context is the next slice and is not authorized by this slice.**

### R2-B media-library selection review stop line

- Production scope: **the `dbvc-ve-media-manager__detail-panel` gains a capability-gated `assign-media` control per still-`missing` field. Activating it calls the R2-A bridge and, on a `writable` descriptor, opens the native `wp.media` frame — single image for featured/ACF image, multiple ordered for ACF gallery — reusing the same standard frame config as `overlay-app.js`. The selection is staged client-side (unsaved) with an `Unsaved selection` badge, thumbnail preview, `Replace`/`Clear`, and a live announcement.**
- Explicitly absent: **any field save, mutation, expected-empty precondition, journal write, cache invalidation, no-reload reconciliation, or exposure of the descriptor token/session or raw targets in the DOM. `overlay-app.js` is untouched.**
- Focused validation: **jsdom 16 tests (11 prior + 5 R2-B: single-select staging, gallery multi-select staging, non-writable notice with no frame, clear-selection, and no control when `wp.media` is unavailable); targeted `lint:visual-editor-media-manager` clean; R1-D read-only invariant updated (staged `wp.media`, still no save) + R2-A focused = 15/252.**
- Full comparison: **718 PHP tests/8,467 assertions with exactly the same six inherited failures (+5 assertions is the updated R1-D invariant; no new regression). Agent docs 54/416/0 (no new surface).**
- Recorded crossing line: **R2-C field-level save (expected-empty precondition, attachment/MIME/cardinality validation, journal/cache, targeted reread, no-reload row/count reconciliation) is the next slice and is not authorized by this slice.**

### R2-C field-level save review stop line

- Production scope: **`MediaAssignmentService` + `POST .../findings/{finding_ref}/assignment` save the staged selection. It re-runs the R2-A revalidation as the expected-empty precondition, mutates through the shared `MutationService` (resolver save, journal/audit, cache invalidation), and rereads via `expandGroup`. The client reconciles the expanded field, the row's missing count, and the scan summary from the reread and marks a fully resolved row in place — with no list/scan reload.**
- Gate satisfied: **a field populated after scan is blocked with `409 media_assignment_stale` and never overwritten; the write target is always the freshly server-resolved descriptor.**
- Focused validation: **`VisualEditorMediaManagerR2CTest` 7 tests/81 assertions (three-family save + reconcile, expected-empty block, non-image rejection, empty-value rejection, stale-generation block); 3 new jsdom cases (19 total): no-reload reconciliation, save conflict retains selection, saving state; R1-D read-only invariant updated; combined Media Manager PHP 41/1,413.**
- Full comparison: **725 PHP tests/8,550 assertions with exactly the same six inherited failures (+7 tests/+83 assertions is R2-C; no new regression). Agent docs 54/417/0 (the new assignment route registered and remapped).**
- Recorded crossing line: **R2-D verified UX states (media modal open, unsaved, save in progress, saved, changed-since-scan, validation error, resolved) is the next slice; R2-E is production hardening. Neither is authorized by this slice.**

### R2 code-review open items (raised 2026-08-16)

Self-review of the R2-A/R2-B/R2-C implementation. None are happy-path correctness defects (the expected-empty gate and saves are proven by tests); these are simplification, performance, and robustness improvements.

| # | Area | Finding | Disposition |
|---|------|---------|-------------|
| CR-1 | Simplification (+minor perf) | R2-A/R2-B minted, persisted, and round-tripped an opaque descriptor `{token, sessionId}` that the R2-C save never consumes (it re-resolves server-side); `persistDetachedDescriptor` + the client token/session were dead state. | **Resolved 2026-08-16** — removed persistence and the token/session round-trip; the bridge returns only `{input, family, expectedState}`. |
| CR-2 | Performance | The same owner is rescanned twice per save (`resolveFinding` precondition + `expandGroup` reconciliation) plus the resolver's old-value read. | **Deferred** — reuse a single rescan for both the precondition and the reconciliation. |
| CR-3 | Correctness/UX (edge) | A successful write whose `expandGroup` reread fails returns `row: null`; the client clears the selection and shows the field as still missing with no "saved" signal. | **Deferred** — return an explicit "saved, refresh to confirm" state on reread failure. |
| CR-4 | Robustness/security (low) | No maximum gallery cardinality on the assignment endpoint; `attachmentIds` was unbounded. | **Resolved 2026-08-16** — cap enforced server-side in `MediaAssignmentService`. |
| CR-5 | Performance (minor) | `reconcileAfterSave` re-rendered the whole results table for a single-field save instead of patching the affected row. | **Resolved 2026-08-16** — targeted patch of the collapsed row, expanded panel, and summary; no full-table rebuild. |
| CR-6 | Cosmetic (minor) | The client scan-summary decrement is non-authoritative and is reset on the next list fetch. | **Deferred** — acceptable as a local estimate; revisit in R2-E. |
| CR-7 | Diff hygiene | `lint --fix` reformatted the whole of `api-client.js` (2-space → tabs), a large cosmetic diff on a file outside the lint set. | **Deferred** — optionally revert to 2-space to keep the diff focused; functionally verified by jsdom. |

### R2 checklist

- [x] Finding-to-descriptor bridge (R2-A: server-authoritative, revalidated, no client target authority)
- [x] Existing image/media-frame integration reused (R2-B: native `wp.media`, staged unsaved, no write)
- [x] Field-level save through the existing family contract (R2-C, expected-empty enforced)
- [x] Journal/audit and cache invalidation on save (R2-C, via `MutationService`)
- [x] Targeted finding revalidation and no-reload row/count reconciliation (R2-C)
- [x] Verified UX states (R2-D: opening, unsaved, upload-unavailable, saving, saved/verified, changed, validation-error, resolved)

### R2-D verified UX states review stop line

- Production scope: **presentation/state refinements over R2-A/B/C in `media-manager-app.js`/`media-manager.css` plus a `canUpload` bootstrap flag. Nine states: media-modal-open (`Opening…`), image/gallery unsaved, upload-unavailable hint, save-in-progress, saved/verified (`Saved` chip + `Resolved` row badge), changed-since-scan (polite refresh), validation error (assertive alert, selection retained), and entity resolved marked in place.**
- Explicitly absent: **any new REST route, mutation authority, descriptor/session change, mobile-specific layout, or responsive-floor change (D-036).**
- Focused validation: **jsdom 23 tests (4 new R2-D: saved/verified, validation-error alert, opening state, upload-unavailable hint); R1-D read-only invariant intact; targeted lint clean; mockup `COMPONENT-NOTES.md` §11 catalogs the states.**
- Full comparison: **726 PHP tests/8,564 assertions with exactly the same six inherited failures (R2-D added only jsdom coverage). Agent docs 54/417/0 (no new surface).**
- Recorded crossing line: **R2-E production hardening (security/stale/attachment/permission tests, journal/cache verification, laptop/desktop browser+keyboard QA, repeated-expansion performance, current-page DOM/reload QA, feature isolation, release notes, rollback) is the next slice and is not authorized by this slice.**
- [ ] Existing gallery media-frame integration reused
- [ ] Upload capability behavior
- [ ] Draft/unsaved selection state
- [ ] Featured-image save
- [ ] ACF image save
- [ ] ACF gallery save
- [ ] Expected-old-value/stale conflict behavior
- [ ] Journal/audit/cache integration
- [ ] Targeted finding revalidation
- [ ] Counts/row update behavior
- [x] Same-entity `Save Row` deferred from initial R2 (D-010)
- [ ] No row-save endpoint/action introduced in initial R2
- [ ] Cross-entity bulk save absent
- [ ] Media modal focus/layering QA
- [ ] Feature flag/fallback/release notes

## R3 Registry-backed Brand Control Center

- [x] Registry/provider contract (R3-A)
- [x] Validation/duplicates/failure handling (R3-A)
- [x] Manual Approved Field Selection curation tool (R3-BX, temporary kill-switch admin page + JSON/MD export)
- [x] Shared Globals compatibility provider (R3-B)
- [x] Backend + descriptor factory extraction + two REST routes (R3-C-1)
- [x] Drawer UI translation from accepted mockup (R3-C-2)
- [x] Fresh descriptor opening — backend (R3-C-1)
- [x] Fresh descriptor opening — frontend wire-up (R3-C-2)
- [x] Existing main panel/save behavior (R3-C-2, via absorb-descriptor event bridge)
- [x] Diagnostics/tests/fallback (R3-D)
- [x] Kill-switch registration guarantee locked down (R3-D)
- [x] Bricks Builder exclusion for drawer assets locked down (R3-D)
- [x] Release notes + rollback runbook (R3-D)

## R4 Expanded Global & Brand Control Center

- [ ] Categories/grouping/sorting
- [ ] Search/filters
- [ ] Safe value summaries/statuses
- [ ] Empty/loading/error/unavailable/inspect-only states
- [ ] Claude mockup and design decision record
- [ ] Responsive/accessibility/performance
- [ ] Shared Globals fallback

## R5 ACF option-family support

### R5.1

- [ ] text
- [ ] textarea
- [ ] url
- [ ] email
- [ ] number
- [ ] range

### R5.2

- [ ] checkbox
- [ ] select
- [ ] radio
- [ ] button_group
- [ ] link
- [ ] wysiwyg

### R5.3

- [ ] image
- [ ] gallery

### R5.4

- [ ] post_object
- [ ] relationship
- [ ] taxonomy

For every family:

- [ ] exact field key/canonical option owner
- [ ] options read/write/stale tests
- [ ] current family editor reused
- [ ] acknowledgement/journal/cache/reload
- [ ] regression across current owner types
- [ ] unsupported configurations fail closed

## R6 Frontend Site Manager Workspace

- [ ] Current Go To Object behavior mapped/reused
- [ ] Object policy and bounded navigation
- [ ] Desktop persistent drawer
- [ ] **Tabled by D-036:** small-screen slide-over and additional responsive/mobile workspace work
- [ ] Current object and routes
- [ ] Review Fields integration
- [ ] Media Manager integration
- [ ] Global & Brand Control Center integration
- [ ] Main panel coexistence
- [ ] Mode preservation
- [ ] Focus/Escape/layering
- [ ] Claude mockup and design decision record
- [ ] Fallback to current toolbar/popovers
- [ ] Performance/accessibility/builder isolation

## Per-release sign-off template

```text
Release:
Status:
Branch/commit/change scope:
Feature flag:
Automated test commands/results:
Browser/device QA:
Accessibility QA:
Performance evidence:
Security review:
Known limitations:
Rollback procedure:
Approved by/date:
```
