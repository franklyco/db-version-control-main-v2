# Evidence Log

Record concrete repository evidence. Do not record assumptions as facts.

| ID | Date | Repository | Path/symbol/commit | Evidence | Implication | Release |
|---|---|---|---|---|---|---|
| E-113 | 2026-09-01 | Vertical (both checkouts) + DBVC tests | **Vertical (cross-repo, both checkouts, byte-identical via `cp` + `diff -q`)** `themes/vertical/functions/features/dbvc-visual-editor/includes/class-vf-vertical-control-provider.php` — `resolveStatus()` refactored from a series of per-family return branches into a single evaluate-family-then-gate-ancestry flow: (1) compute `$is_unlocked_family` boolean by checking each family gate in turn (R5.1 text-family, R5.2 choice/link/wysiwyg/true_false, R5.3 image, R5.2+color_picker, R5.4 relationship/post_object); (2) short-circuit `unsupported` if not an unlocked family; (3) short-circuit `available` if the record is top-level (`strpos($name, '>') === false`); (4) delegate to `parentChainIsGroupOnly($field_key)` for nested records — `available` iff every intermediate ACF parent is `type='group'`. The R5.4 branch's family-specific ancestry call is gone; the gate is now applied uniformly. Private static helper renamed from `hasGroupOnlyAncestry` to `parentChainIsGroupOnly` — original name reflected its R5.4-only introduction but the R5.6 broadening makes the family-agnostic name accurate. Docblock updated to reflect the new "unlocked family AND (top-level OR group-only nested)" contract. Family-agnostic body byte-identical (walks `acf_get_field` parent chain, requires every intermediate `type='group'`, returns false when ACF isn't loaded or field lookup fails). `tests/phpunit/VisualEditorVerticalProviderR51BTest.php` (+8 test methods / +14 assertions): (a) 5 existing nested-family tests (`test_r51_text_family_record_maps_to_available_status` dataProvider, `test_r52_family_record_maps_to_available_status` dataProvider, `test_r52_color_picker_record_maps_to_available_status`, `test_r53_image_record_maps_to_available_status`, `test_r52_true_false_now_flips_to_available`) now seed a group-only ACF chain via new `seedGroupOnlyChain($leaf_key, $leaf_type)` helper so the R5.6 gate passes without changing the family-gate assertion intent; (b) 3 previously-implicitly-passing nested-unsupported tests (`test_r54_nested_reference_record_stays_unsupported`, `test_build_descriptor_returns_null_for_unsupported_record`, `test_build_value_summary_returns_null_for_unsupported_record`) now explicitly seed a repeater-in-chain fixture via new `seedRepeaterInChain($leaf_key, $leaf_type)` helper and updated docblock comments — replaces stale "R3-C-1 factory doesn't yet emit group_write_path" reasoning with the current-correct "gate rejects repeater intermediates"; (c) new `test_repeater_in_chain_blocks_every_unlocked_family` matrix `@dataProvider` `unlockedFamilyMatrixProvider` (16 cases covering every currently-unlocked (unlocks_at, field_type) pair: R5.1 text/textarea/url/email/number; R5.2 select/checkbox/radio/button_group/link/wysiwyg/true_false; R5.3 image; R5.2+color_picker color_picker; R5.4 relationship/post_object) — asserts each pair returns `unsupported` when the leaf's ACF ancestry contains a repeater intermediate. Class docblock extended with an R5.6-specific paragraph explaining the shared parent-chain gate. Two new helpers appended at file bottom next to `makeProviderWithRecords`. **Rename traceability**: `grep -rn 'hasGroupOnlyAncestry'` across BOTH plugin repo AND both Vertical checkouts → zero hits (private static, no external callers to migrate) | **Live-site impact today: zero** — a full survey of the 397 already-Available records shows every nested unlocked record has group-only ancestry, so no record's status flips from Available → Unsupported. This is a purely defensive slice: any FUTURE curation record nested inside a Repeater or Flexible-Content field will now stay `unsupported` at the drawer surface (no Open button) instead of surfacing an Open button whose save would silently land on the wrong meta key (via `writeGroupedFieldValue` inheriting `writeAcfValue`'s behavior for the leaf's own key while the ACF row structure lives elsewhere). The DBVC `SharedGlobalsDescriptorFactory` also carries the latent bug this defends against — its `walkGroupChain` helper emits `group_write_path` without type-checking intermediates; the DBVC-side factory rejection (defense-in-depth layer #2 mentioned in the R5.6 plan) is deliberately deferred to avoid duplicating the check across two sites, since the Vertical resolveStatus gate now runs unconditionally before any descriptor mint. **Architecturally**: this normalizes the "nested unlocked records must have group-only ancestry" contract across every family — the R5.4 reference-collection gate is no longer a family-specific exception. R5.7 (repeater/flexible-content nested actual capability) is now the only remaining unlock frontier for records inside a non-group container. **Validation**: `vendor/bin/phpunit --filter "VisualEditorVerticalProviderR51B"` → **39 / 105 OK** (was 31 / 91 pre-slice — +8 new). `vendor/bin/phpunit --filter "VisualEditor"` → **349 / 2714 OK** (full VisualEditor subset green). `node --test tests/visual-editor-brand-control-center-state.test.cjs` → **44 pass** (unchanged — R5.6 touched no drawer JS/CSS). `node --test tests/visual-editor-media-manager-state.test.cjs` → **42 pass** (preserved). PHP lint clean on Vertical provider + test file. `diff -q` confirms both Vertical checkouts identical. No content mutated; no persistent WP option toggled outside test setUp/tearDown; no git operations either repo | Defensive hardening completes the "every nested unlocked record must have group-only ancestry" contract uniformly across every R5.x family. The unlock frontier is now cleanly split into two orthogonal axes: (1) unlocked family + top-level → available; (2) unlocked family + nested + every ancestor is a group → available; (3) unlocked family + nested + any ancestor is a repeater/flexible/other → unsupported (the R5.7 frontier). Any future R5.x family addition inherits the ancestry gate for free — no per-family duplication. R5.7 (repeater/flexible subfield support) is the only remaining unlock frontier that could move a record from Unsupported to Available. R5.5 (date_picker) and R5.later (vf_palette) planning docs remain independent of R5.6 | R5/R5.6 |
| E-112 | 2026-09-01 | DBVC + Vertical | **DBVC** `addons/visual-editor/src/Registry/Providers/SharedGlobalsDescriptorFactory.php` (extended the relationship / post_object branch — the same one R3-C-1 shipped — to walk the ACF parent chain via the existing `walkGroupChain($field)` helper R5.1-b introduced for text-family. When the chain is non-empty (leaf nested inside one or more Group fields), the descriptor now emits `source.group_write_path` (chain from first-parent-below-root DOWN to leaf) + `source.is_grouped_field=true` + a root `source.field_selector`/`field_selector_raw` (pointing at the outermost group's name) + `path.rootFieldName`/`rootFieldKey`. Top-level relationship / post_object records see an empty chain and fall through to the R3-C-1 shape byte-identical — verified by a regression test that pins `field_selector === field_name` + `path.rootFieldName === field_name` + `source` lacks `group_write_path` and `is_grouped_field` for a top-level fixture. `AcfReferenceCollectionResolver::save()` → `writeCollectionValue()` → `writeAcfValue()` → `writeGroupedFieldValue()` (Media Manager R2-F RK-013 pattern) routes the group-nested save through the parent group's own `update_value`); **Vertical (cross-repo, both checkouts, byte-identical)** `themes/vertical/functions/features/dbvc-visual-editor/includes/class-vf-vertical-control-provider.php` (`resolveStatus()` signature widened to accept `$field_key` too; R5.4 branch's blanket `strpos($name, '>') === false` guard replaced with a two-tier check — top-level records short-circuit as before (no ACF hit), nested records call a new private static `hasGroupOnlyAncestry($field_key)` that lifts the leaf via `acf_get_field()` and walks `parent` up until either a `group_XXX` field-group boundary OR a non-`type='group'` intermediate (repeater / flexible / other), returning true only when every intermediate is a group. Nested reference records with group-only chains flip `available`; nested records with a repeater/flexible/other in the chain stay `unsupported`. Fail-safe: `hasGroupOnlyAncestry` returns false when ACF isn't loaded or the field lookup fails — the record stays unsupported, never crashes); `tests/phpunit/VisualEditorControlCenterNestedReferenceTest.php` (new, 3 tests / 24 assertions — top-level regression guard (no group_write_path emitted; field_selector === field_name; path.rootFieldName === field_name); single-level group-nested (group_write_path has 1 entry pointing at the leaf, field_selector points at root group, path.rootField* points at root group, is_grouped_field=true); multi-level group chain (3 levels deep: outer group → inner group → leaf; group_write_path walks [inner, leaf] with root at chain[0] living in field_selector). Includes a global `acf_get_field()` stub that reads from `$GLOBALS['vfe_test_acf_fields']` populated per-test so the walker sees synthetic chain fixtures); `tests/phpunit/VisualEditorVerticalProviderR51BTest.php` (+2 nested-reference cases via the same `acf_get_field` stub pattern — group-only ancestry flips available; repeater-in-chain stays unsupported. Also gained a `tear_down()` that unsets `$GLOBALS['vfe_test_acf_fields']` so state doesn't leak between tests. R51B setUp resets the fixture map to `[]` so the pre-existing "unsupported short-circuit" tests continue to get null from `acf_get_field` and stay unsupported as before); **also fixed a latent bug in the R5.1-b `buildSharedFieldDescriptor`** (text-family path): before this slice its `walkGroupChain` invocation had no type-check on intermediate parents either, so a text field nested inside a repeater would emit `group_write_path` incorrectly. Group-only test coverage now proves the shape is correct; a repeater-nested text field would exhibit the same wrong-meta-key bug we're guarding reference collections against. Since the current 182 R5.1-b text records all work in production, no R5.1-b field is actually repeater-nested; but a documented follow-up gate should be added in a separate slice (see `Follow-ups` in the release doc) | **1 curated Vertical record potentially unlocks** — the specific `settings_nav_menus>menus>menu_posts` record. Whether it actually flips to Available depends on the ACF field-group configuration for `menus`: if it's a group, `hasGroupOnlyAncestry` returns true and the record flips available; if it's a repeater (my a-priori guess), `hasGroupOnlyAncestry` returns false and the record stays unsupported. Both outcomes are correct — this slice's contract is "unlock what's safe; leave the rest for a dedicated repeater-subfield slice." **Combined with R5.1 (182) + R5.2 (155) + R5.2+color_picker (20) + R5.3 (37) + R5.4 (1) + true_false (2), the drawer now has 397–398 editable Vertical rows out of the 400 curated (99.25–99.5% depending on the actual ACF structure of the one nested-reference record)**. The remaining 2 records (`later` / no `unlocks_at`) are not tagged for any current slice. **Architecturally**: this closes the wire-through arc for every R5.x family that had a resolver / panel controller ready. Any nested field where the intermediate chain is group-only now works uniformly across every family via `walkGroupChain` + AbstractAcfResolver's `writeGroupedFieldValue`. Nested-inside-repeater support is a separate future slice that needs `writeRepeaterSubfieldValue` semantics + row-index tracking + likely a new `acf_repeater_subfield` source.type variant. **Validation**: `vendor/bin/phpunit --filter "VisualEditorControlCenterNestedReference"` → **3 / 24 OK**. `vendor/bin/phpunit --filter "VisualEditor(Control\|SharedGlobals\|Vertical\|Resolver)"` → **182 / 642 OK** (was 177 / 614 pre-slice — +5 new: 3 factory + 2 Vertical nested). `node --test tests/visual-editor-brand-control-center-state.test.cjs` → **44 pass** (unchanged — slice touched no drawer JS/CSS; the R4-C-1b relationship chip already renders group-nested reference records the same way as top-level). `node --test tests/visual-editor-media-manager-state.test.cjs` → **42 pass** (preserved). `composer agent-docs:refresh && composer agent-docs:check` → **54 / 443 / 0** (unchanged — no new hook / REST / i18n; no discovery id rotated). PHP lint clean on both Vertical checkouts + the DBVC factory. `diff -q` confirms both Vertical checkouts identical. No content mutated; no persistent WP option toggled outside test setUp/tearDown; no git operations either repo | Nested reference-collection support closes the wire-through R5 arc for every existing resolver + panel controller. Any future Vertical curation record whose intermediate chain is group-only now flips available automatically. **The one remaining specific record** (`settings_nav_menus>menus>menu_posts`) will unlock on live if `menus` turns out to be a group; if it's a repeater it stays correctly-unsupported. **The final unsupported curation tier — records nested inside a repeater or flexible field** — remains a genuinely-separate slice with new primitives (`writeRepeaterSubfieldValue` for reference collections, `acf_repeater_subfield` source.type routing, row-index tracking). That is the last known-safe unlock frontier for the R5 arc. Follow-up gate for the R5.1-b text-family path (which shares the same `walkGroupChain` and could exhibit the same repeater-nested-write bug if a repeater-nested text record ever ships) noted for a small hardening slice | R5/Nested reference |
| E-111 | 2026-09-01 | DBVC + Vertical | **DBVC** `addons/visual-editor/src/Resolvers/AcfTrueFalseResolver.php` (new — extends `AbstractAcfResolver`, `name()='acf_true_false'`, `supports()` gates on `source.field_type === 'true_false'`; `validate()` accepts null / empty / bool / int `0-1` / string `'0'/'1'/'true'/'false'/'on'/'off'/'yes'/'no'` and rejects everything else; `sanitize()` normalizes to ACF's canonical `1` / `0` integer; `save()` delegates to `writeAcfValue()` which inherits R5.1-b group-nested support via `writeGroupedFieldValue()`; new public static `coerceToBool($value)` reused by the summary builder + panel controller to keep truthy-shape interpretation identical across layers); `addons/visual-editor/src/Resolvers/ResolverRegistry.php` (registered `AcfTrueFalseResolver` in the `$instances` list next to `AcfColorPickerResolver`); `addons/visual-editor/src/Registry/Providers/SharedGlobalsControlProvider.php` (`supportedFieldTypes()` widened by `'true_false'`; new public static `buildBooleanSummary($value)` — delegates to `AcfTrueFalseResolver::coerceToBool` so truthy interpretation stays single-sourced, returns `{family:'boolean', value:bool, label:__('On'|'Off')}` — never null (a boolean field always renders a state); buildValueSummary gains true_false branch); `addons/visual-editor/src/Registry/Providers/SharedGlobalsDescriptorFactory.php` (new `true_false` branch delegating to `buildSharedFieldDescriptor()` with `{resolver:'acf_true_false', input:'true_false', source_context:'toolbar_shared_global_option_boolean'}`); `addons/visual-editor/assets/js/overlay-app.js` (added `case 'true_false':` to `createFieldController` switch → new `createBooleanController(value)` — checkbox `<input type="checkbox">` + live-updating "On"/"Off" label span; getValue returns `1`/`0` matching ACF canonical storage; `coerceBooleanValue` client-side helper accepts the same truthy shapes the PHP resolver does so the panel picks up an existing 1/0/true/false value consistently); `addons/visual-editor/assets/js/brand-control-center-app.js` (`renderValueSummaryChip` gains a `family === 'boolean'` branch — on/off pill with `is-on` variant class + label text + title tooltip); `addons/visual-editor/assets/css/control-center.css` (new `.__value-boolean` pill styling with `.is-on` variant using success-tone `color-mix`); `addons/visual-editor/src/Assets/AssetLoader.php` (2 new panel i18n keys: `panelBooleanOn = "On"`, `panelBooleanOff = "Off"` — shared between the panel controller and the drawer chip fallback); **Vertical (cross-repo, both checkouts, byte-identical)** `themes/vertical/functions/features/dbvc-visual-editor/includes/class-vf-vertical-control-provider.php` (`resolveStatus()` R5.2 branch widened by `true_false` — curation JSON tags the 2 boolean records with `unlocks_at="R5.2"`; `buildDescriptor()` `$supported` widened by `true_false`; `buildValueSummary()` gains true_false branch delegating to DBVC's `buildBooleanSummary` static); `tests/phpunit/VisualEditorControlCenterTrueFalseTest.php` (new, 15 tests / 34 assertions — resolver name + supports gate + `@dataProvider` matrix of 7 truthy inputs + 8 falsy inputs (bool/int/string variants covering all common ACF value shapes) + sanitize normalization + validate accept/reject + provider whitelist + buildBooleanSummary true/false/null cases + factory `true_false` descriptor shape + provider integration); `tests/visual-editor-brand-control-center-state.test.cjs` (+2 boolean chip jsdom cases: on-state renders `is-on` class + label, off-state renders muted chip without `is-on`); `tests/phpunit/VisualEditorVerticalProviderR51BTest.php` (renamed `test_r52_true_false_stays_unsupported` → `test_r52_true_false_now_flips_to_available` and inverted the assertion; the 2 "unsupported short-circuit" fixtures re-based onto nested-relationship (`settings_nav_menus>menus>menu_posts`) since true_false now flips to available); `tests/phpunit/VisualEditorControlCenterR51ATest.php` + `VisualEditorSharedGlobalsControlProviderTest.php` (skip fixtures re-based onto `taxonomy` — a real ACF type outside every supported list — since true_false is no longer the "still-unsupported" example); `docs/agents/manifest.json` (extension-point discovery id `dbvc_visual_editor_restricted_option_group_roots` rotated from line shifts of the new resolver registration: `d25ce1dd → 3b65b39c`, re-mapped). **Also included: nested reference-collection investigation** — traced `AcfReferenceCollectionResolver::save()` → `writeCollectionValue()` → `writeAcfValue()` (which routes through `writeGroupedFieldValue` when `is_grouped_field=true`). Group-only nesting would work if the factory emitted `group_write_path` on the relationship branch. **BUT** the specific remaining record `settings_nav_menus>menus>menu_posts` almost certainly has `menus` as a repeater (menu configs are typically repeaters in ACF); a repeater-subfield write needs `writeRepeaterSubfieldValue`, not `writeGroupedFieldValue`, so a partial factory extension would ship broken for the actual record. Distinguishing group-only vs repeater-nested chains at descriptor-mint time needs proper ACF introspection — that's a dedicated slice, not a simple guard toggle. Deferred rather than shipping a partial that fails on the actual record | true_false — **new resolver primitive #2 in the R5 arc** (after R5.2+color_picker). Follows the same "new resolver + new panel controller + wire through factory/summary/chip/Vertical" pattern R5.2+color_picker established. Panel side needed a **real new controller** (`createBooleanController` with a checkbox + live-updating label) — unlike R5.2+color_picker where the native `<input type="color">` reused `createInputController`. **2 curated group-nested Vertical true_false records flip Unsupported → Available** on the maintainer's live site. **Combined with R5.1 (182) + R5.2 (155) + R5.2+color_picker (20) + R5.3 (37) + R5.4 (1), the drawer now has 397 editable Vertical rows out of the 400 curated (99.25%)**. The 3 remaining unsupported records: 1 nested reference-collection (needs the factory-side repeater/group-detection extension flagged above) + 2 curated `later` / no-`unlocks_at` records not tagged for any current slice. **Cross-layer coercion consistency**: the same "truthy shape" definition lives in `AcfTrueFalseResolver::coerceToBool` (server sanitize + summary), `buildBooleanSummary` (server chip payload), and `coerceBooleanValue` (client panel controller) — three parallel implementations covering the same bool / int / string / on/off/yes/no domain, kept aligned via the shared canonical spec in the resolver's docblock. **Validation**: `vendor/bin/phpunit --filter "VisualEditorControlCenterTrueFalse"` → **15 / 34 OK**. `vendor/bin/phpunit --filter "VisualEditor(Control\|SharedGlobals\|Vertical\|Resolver)"` → **177 / 614 OK** (was 152 / 564 pre-true_false — +25 new: 15 in true_false test + 1 Vertical R5.2 case + refactored fixtures across 3 pre-existing skip tests re-based onto still-unsupported types). `node --test tests/visual-editor-brand-control-center-state.test.cjs` → **44 pass / 44 tests** (was 42 — +2 boolean chip cases). `node --test tests/visual-editor-media-manager-state.test.cjs` → **42 pass** (preserved). `composer agent-docs:refresh && composer agent-docs:check` → **54 / 443 / 0** (1 discovery id rotated from ResolverRegistry line shifts; re-mapped in manifest). PHP lint clean on every touched file. `diff -q` confirms both Vertical checkouts identical. No content mutated; no persistent WP option toggled outside test setUp/tearDown; no git operations either repo | true_false takes coverage to **99.25% (397/400)**. Only 3 curated records still unsupported: 1 nested reference-collection (needs factory extension with repeater/group detection — deferred to a dedicated slice) + 2 tagged `later` / no unlocks_at (not intended for any current slice). **This closes the R5 arc for every family that had a curated record with a defined unlocks_at tag**. Any next unlock work is either the nested reference-collection factory extension (touches R3-C-1's relationship factory branch — moderate risk, needs ACF introspection to detect group-vs-repeater) or entirely new families not in the current curation. The R5 pattern proven across 6 slices (R5.1-a/1b, R5.2-a/2b, R5.2+color_picker, R5.3, R5.4, true_false) can be summarized as: (1) add resolver if new; (2) widen `supportedFieldTypes`; (3) add factory branch; (4) add `build{Family}Summary` static helper; (5) add chip renderer branch; (6) Vertical `resolveStatus` + `buildValueSummary` delegation; (7) mirror both Vertical checkouts | R5/true_false + Nested-reference deferral note |
| E-110 | 2026-09-01 | DBVC + Vertical | **DBVC** `addons/visual-editor/src/Resolvers/AcfColorPickerResolver.php` (new — extends `AbstractAcfResolver`; `name()='acf_color_picker'`; `supports()` gates on `source.field_type === 'color_picker'`; `validate()` accepts empty / null / recognized color shapes (`#rgb`, `#rrggbb`, `#rrggbbaa`, `rgb(...)`, `rgba(...)`) and rejects everything else; `sanitize()` normalizes hex to lowercase 7-char (3-char expands to 6-char) and passes rgb(a) through `sanitize_text_field`; `save()` delegates to `writeAcfValue()` which inherits R5.1-b group-nested support via `writeGroupedFieldValue()`; new public static `isRecognizedColorString($string)` used by validate() and reusable from tests); `addons/visual-editor/src/Resolvers/ResolverRegistry.php` (registered `AcfColorPickerResolver` in the `$instances` list next to AcfChoiceResolver so `resolvers['acf_color_picker']` is available at descriptor-resolution time); `addons/visual-editor/src/Registry/Providers/SharedGlobalsControlProvider.php` (`supportedFieldTypes()` widened by `'color_picker'`; new public static `buildColorSummary($value)` — validates recognized color shapes, normalizes hex to lowercase 7-char, expands 3-char to 6-char, preserves rgb(a) via `sanitize_text_field`, returns `{family:'color', hex}` or null; `buildValueSummary()` dispatch gains color_picker branch delegating to the static); `addons/visual-editor/src/Registry/Providers/SharedGlobalsDescriptorFactory.php` (new `color_picker` branch delegating to `buildSharedFieldDescriptor()` with `{resolver:'acf_color_picker', input:'color', source_context:'toolbar_shared_global_option_color'}` — group-nesting inherits from R5.1-b `walkGroupChain`); `addons/visual-editor/assets/js/overlay-app.js` (added `case 'color':` to the `createFieldController` switch — delegates to the existing `createInputController('color', value)` which produces a native `<input type="color">`; **zero new panel controller code**); `addons/visual-editor/assets/js/brand-control-center-app.js` (`renderValueSummaryChip` gains a `family === 'color'` branch — 12×12 rounded swatch with `background-color` set via `style.setProperty` on server-validated hex + monospace hex label + full hex as `title` tooltip); `addons/visual-editor/assets/css/control-center.css` (new `.__value-color`, `.__value-color-swatch` (12×12, rounded, thin border), `.__value-color-hex` (monospace, ellipsis)); **Vertical (cross-repo, both checkouts, byte-identical)** `themes/vertical/functions/features/dbvc-visual-editor/includes/class-vf-vertical-control-provider.php` (`resolveStatus()` gains R5.2+color_picker branch — flips to available when `unlocks_at === 'R5.2+color_picker'` AND `field_type === 'color_picker'`; `buildDescriptor()` `$supported` widened by `color_picker`; `buildValueSummary()` gains color_picker branch delegating to DBVC's `buildColorSummary` static); `tests/phpunit/VisualEditorControlCenterR52ColorPickerTest.php` (new, 15 tests / 40 assertions — resolver name + supports gate + `@dataProvider` matrix of 6 recognized color shapes + 5 rejected shapes (including `javascript:` scheme, named colors, HTML tags) + sanitize normalization + validate accept/reject + provider whitelist + buildColorSummary shape / hex normalization / short-hex expansion / rgba preservation / null cases + factory `color_picker` descriptor shape + provider integration); `tests/phpunit/VisualEditorVerticalProviderR51BTest.php` (+1 R5.2+color_picker Vertical case; two "unsupported short-circuit" fixtures re-based onto `true_false` since color_picker/R5.2+color_picker now flips to available); `tests/phpunit/VisualEditorControlCenterR51ATest.php` + `VisualEditorSharedGlobalsControlProviderTest.php` (skip fixtures re-based onto `true_false` since color_picker is no longer the "still-unsupported" example); `tests/visual-editor-brand-control-center-state.test.cjs` (+1 R5.2+color_picker jsdom case — swatch backdrop set via style.setProperty, hex label, tooltip); `docs/agents/manifest.json` (1 extension-point discovery id rotated by line shifts from the new resolver registration: `dbvc_visual_editor_restricted_option_group_roots` `a6ea5380 → d25ce1dd`, re-mapped) | R5.2+color_picker — **first R5 slice to add a genuinely new resolver primitive**. Every prior R5.x slice was a wire-through of an existing resolver; R5.2+color_picker ships the new `AcfColorPickerResolver` (extends the same `AbstractAcfResolver` base every other ACF resolver uses, so group-nested writes inherit for free). Panel controller side needs **zero new code** — the existing `createInputController` factory produces a native `<input type="color">` from the `'color'` inputType alias, and the switch case in `createFieldController` is one line. Every other primitive is a reuse: `mutation.contract='shared_field'` (whitelisted since R5.1-a's routing work); `walkGroupChain` for group-nested (R5.1-b); public static summary builder pattern (R5.1..R5.4). **20 curated group-nested Vertical color_picker records flip Unsupported → Available** on the live site (the R3-BX curation JSON tags them `unlocks_at="R5.2+color_picker"`). **Combined with R5.1 (182) + R5.2 (155) + R5.3 (37) + R5.4 (1), the drawer now has 395 editable Vertical rows out of the 400 curated (98.75%)**. **Data-safety**: the color-summary chip renderer sets `background-color` via `style.setProperty` (never through an unescaped inline style attribute at eval time) and only on server-validated hex/rgba shapes; the browser CSS parser rejects unknown values as a final layer. `AcfColorPickerResolver::isRecognizedColorString()` explicitly rejects `javascript:`-scheme values, named colors, and HTML-tag-shaped inputs. **Validation**: `vendor/bin/phpunit --filter "VisualEditorControlCenterR52ColorPicker"` → **15 / 40 OK**. `vendor/bin/phpunit --filter "VisualEditor(Control\|SharedGlobals\|Vertical\|Resolver)"` → **152 / 564 OK** (was 129 / 519 pre-R5.2+color_picker — +23 new). `node --test tests/visual-editor-brand-control-center-state.test.cjs` → **42 pass / 42 tests** (was 41 — +1 color chip case). `node --test tests/visual-editor-media-manager-state.test.cjs` → **42 pass** (preserved). `composer agent-docs:refresh && composer agent-docs:check` → **54 / 443 / 0** (one extension-point discovery id rotated from line shifts due to new resolver registration in ResolverRegistry.php; re-mapped in manifest). PHP lint clean on every touched file. `diff -q` confirms both Vertical checkouts identical. No content mutated; no persistent WP option toggled outside test setUp/tearDown; no git operations either repo | R5.2+color_picker proves the "add a new resolver primitive" pattern works end-to-end with the R5.x factory / summary / Vertical wiring already in place: one new resolver class + one panel switch-case + one factory branch + one summary helper + one chip renderer + one Vertical resolveStatus branch. **Live-site: refresh the drawer and 20 previously-Unsupported color_picker rows (all in Brand palette groups) should now show Open** — click Open and the browser's native color picker opens right in the editor panel, save persists as a lowercase 7-char hex string through MutationService via AcfColorPickerResolver, and the R4-D-1 save-status-strip acknowledges the save. **Only two families remain unsupported in the curated set**: `true_false` (2 records — needs a boolean controller + resolver) and nested reference-collection (1 record — needs factory extension). **Live-site coverage: 395 of 400 curated Vertical records editable (98.75%)** | R5/R5.2+color_picker |
| E-109 | 2026-09-01 | DBVC + Vertical | **DBVC** `addons/visual-editor/src/Registry/Providers/SharedGlobalsControlProvider.php` (extracted the R4-A instance-method relationship / post_object summary walk into a new public static `buildReferenceSummary($value, $family)` — takes both the raw value AND the expected family string so a caller can't accidentally route a text value through the reference walker (family gate rejects non-`relationship|post_object`). Body is byte-identical to the R4-A instance code (normalize-to-post-ids → walk first 3 titles via get_the_title → collapse empty). New private static helpers `normalizeToPostIdsList($value)` + `coerceOnePostIdValue($item)` — static twins of the instance methods `normalizeToPostIds` + `coerceOnePostId`; the instance methods still exist for backward compatibility. Instance `buildValueSummary()` reference-collection tail now delegates to `self::buildReferenceSummary($value, $field_type)` so the existing R4-A tests keep passing byte-identically); **Vertical (cross-repo, both checkouts)** `themes/vertical/functions/features/dbvc-visual-editor/includes/class-vf-vertical-control-provider.php` (`resolveStatus()` signature widened to accept `$field_name` so the R5.4 branch can gate on nesting; gains R5.4 branch that returns `'available'` when `unlocks_at === 'R5.4'` AND `field_type ∈ [relationship, post_object]` AND the curated `field_name` does NOT contain `>` (top-level only — R3-C-1's reference-collection factory branch does not emit `group_write_path` for reference collections, so nested paths would save to the wrong meta key). `mapRecord()` passes `$field_name` into the new signature. `buildDescriptor()` `$supported` whitelist widened by `relationship` + `post_object`. `buildValueSummary()` gains reference-collection branch delegating to DBVC's new `SharedGlobalsControlProvider::buildReferenceSummary($value, $field_type)`. Mirrored byte-identically to both Vertical checkouts via `cp` + `diff -q`); `tests/phpunit/VisualEditorVerticalProviderR51BTest.php` (+3 R5.4 cases via `@dataProvider r54FamilyProvider` — top-level post_object + top-level relationship both map to available + nested reference stays unsupported); `tests/phpunit/VisualEditorControlCenterR52Test.php` (+4 R5.4 static-helper cases — walks first-3 titles across 5 real WP post fixtures; family gate rejects non-reference family; null cases for empty/false/null; ACF associative-array shape coercion) | R5.4 — Vertical parity for post_object + relationship. **1 curated Vertical record flips Unsupported → Available on the live site**: the top-level `office_manager` post_object on `site-settings`. The other R5.4 record — `settings_nav_menus>menus>menu_posts` (relationship nested inside a repeater) — deliberately STAYS unsupported: R3-C-1's reference-collection factory branch does not emit `group_write_path`, so a save to a nested reference collection would land on the wrong meta key. Nested reference-collection support is a separate later slice (needs the factory reference-collection branch to walk parent + emit `group_write_path` the way `buildSharedFieldDescriptor` already does for R5.1-b text-family). Zero new REST routes, zero new mutation authority, zero new resolvers, zero new mutation contracts. The AcfReferenceCollectionResolver + shared_relationship_collection / shared_post_object_collection contracts have shipped since R3-B; R5.4 wires the Vertical side. **Combined with R5.1 (182) + R5.2 (155) + R5.3 (37), the drawer now has 375 editable Vertical rows out of the 400 curated** — R5.4 adds 1 rather than 2 because of the nested-repeater guard. **Extraction rationale**: `buildReferenceSummary` becomes the fifth public static summary builder alongside `buildTextSummary` / `buildSingleChoiceSummary` / `buildMultiChoiceSummary` / `buildLinkSummary` / `buildWysiwygSummary` / `buildImageSummary` — every family the drawer supports now has a single-source-of-truth static shape builder Vertical + any future provider can reuse. **Validation**: `vendor/bin/phpunit --filter "VisualEditor(Control\|SharedGlobals\|Vertical)"` → **129 / 519 OK** (was 122 / 499 pre-R5.4 — +7 R5.4 new: 3 Vertical R5.4 cases + 4 buildReferenceSummary DBVC static-helper cases). `node --test tests/visual-editor-brand-control-center-state.test.cjs` → **41 pass** (unchanged — R5.4 touched no drawer JS/CSS; the relationship chip R4-C-1b already renders both `relationship` and `post_object` families). `node --test tests/visual-editor-media-manager-state.test.cjs` → **42 pass** (preserved). `composer agent-docs:refresh && composer agent-docs:check` → **54 / 443 / 0** (unchanged — no new hook or REST surface, no i18n keys). PHP lint clean on both Vertical checkouts + the DBVC file. `diff -q` confirms both Vertical checkouts identical. No content mutated; no persistent WP option toggled outside test setUp/tearDown; no git operations either repo | R5.4 closes the "existing resolver families with Vertical parity" arc. **Every family the R4-A backend contract + R4-C drawer supports is now wired end-to-end for the Vertical provider** except color_picker (needs new AcfColorPickerResolver + panel controller), true_false (needs new boolean primitives), and nested reference collections (needs factory extension). **Live-site coverage after R5.1 + R5.2 + R5.3 + R5.4**: **375 of 400 Vertical records now editable** (93.75%). Refresh the drawer and the `office_manager` row + all previously-supported R5.1/2/3 rows are ready to Open + edit + save | R5/R5.4 |
| E-108 | 2026-09-01 | DBVC + Vertical | **DBVC** `addons/visual-editor/src/Registry/Providers/SharedGlobalsControlProvider.php` (`supportedFieldTypes()` widened by 1 R5.3 type: `'image'`; `mapFieldFamily()` explicitly maps `'image'` → `'image'` (unlike R5.2 families which fall into `'other'`, image gets its own ControlRecord whitelist slot); `buildValueSummary()` dispatch gains an `image` branch delegating to new public static helper `buildImageSummary($value)` + private `coerceAttachmentId($value)` helper that collapses ACF's three value shapes (bare int / numeric string / associative `[ID|id]` array / WP_Post-object with `->ID`) onto a positive integer id. `buildImageSummary` guards on `get_post($id)` truthiness, then calls `wp_get_attachment_image_src($id, 'thumbnail')` for the URL and prefers `get_attached_file` → `wp_get_attachment_metadata['file']` for the canonical filename via `basename()`. Empty / unresolvable / non-attachment id → null); `addons/visual-editor/src/Registry/Providers/SharedGlobalsDescriptorFactory.php` (new `image` branch on the `build()` dispatcher: `resolver='acf_image'`, `ui.input='media_reference'` matching the R2-B/R2-F frontend `createMediaReferenceController`, `source_context='toolbar_shared_global_option_image'`. Everything else — including group-nesting via `walkGroupChain()` — inherits from R5.1-b); `addons/visual-editor/assets/js/brand-control-center-app.js` (`renderValueSummaryChip` gains an `image` branch — creates `.__value-image` container, appends 24×24 `<img loading="lazy" width="24" height="24" alt="">` for `thumbUrl` (uses `setAttribute` per jsdom quirk where property-set `img.loading = 'lazy'` doesn't reflect as an attribute), appends `.__value-image-filename` `<span>` for filename, sets `title` = filename OR thumbUrl for hover disambiguation. Empty filename + empty thumbUrl → null → empty slot); `addons/visual-editor/assets/css/control-center.css` (new `.__value-image` inline-flex container + `.__value-image img` (24×24, rounded, object-fit:cover, subtle placeholder bg via `color-mix`) + `.__value-image-filename` with ellipsis truncation. All inherit parent `.__value-summary max-width:200px` cap); **Vertical (cross-repo, both checkouts)** `themes/vertical/functions/features/dbvc-visual-editor/includes/class-vf-vertical-control-provider.php` (`resolveStatus()` gains R5.3 branch — returns `'available'` when `unlocks_at === 'R5.3'` AND `field_type === 'image'`; `buildDescriptor()` `$supported` whitelist gains `'image'`; `buildValueSummary()` gains image branch delegating to DBVC's `SharedGlobalsControlProvider::buildImageSummary($value)`. Mirrored byte-identically to both Vertical checkouts via `cp` + `diff -q`); `tests/phpunit/VisualEditorControlCenterR53Test.php` (new, 10 tests / 28 assertions — whitelist + mapFieldFamily + factory image descriptor shape + buildImageSummary coercion matrix across int/numeric-string/associative-array shapes with real WP attachment fixtures created via `self::factory()->attachment->create_object()` + null cases for zero/empty/false/non-existent-id + provider integration); `tests/phpunit/VisualEditorVerticalProviderR51BTest.php` (+1 R5.3 image-record-maps-to-available case; the "build descriptor unsupported short-circuit" fixture re-based onto color_picker/R5.2+color_picker since image/R5.3 now flips to available); `tests/visual-editor-brand-control-center-state.test.cjs` (+2 R5.3 jsdom cases: image chip renders thumbnail + filename + title tooltip; image chip renders filename alone when thumbUrl is empty) | R5.3 — image unlock. **37 curated group-nested Vertical image records flip Unsupported → Available** on the maintainer's live site. Zero new REST routes, zero new mutation authority, zero new resolvers, zero new mutation contracts. The AcfImageResolver has shipped since R2 and MutationContractService already whitelisted `shared_field`; R5.3 wires the drawer's factory branch + frontend chip. Panel controller (`createMediaReferenceController`) reuses the same `wp.media` frame factory the Media Manager owns — no TinyMCE-style lazy-load surprise. Group-nesting inherits from R5.1-b's `walkGroupChain()` — `AbstractAcfResolver::writeGroupedFieldValue` writes THROUGH the parent group's own `update_value` (Media Manager R2-F RK-013 pattern), so group-nested option-page image saves persist to the correct prefixed meta key. **Combined with R5.1 (182) + R5.2 (155), the drawer now has 374 editable Vertical rows out of the 400 curated**. Every Vertical family the R4-A backend supports is now editable except color_picker (20 records — needs new AcfColorPickerResolver), post_object/relationship (2 records — R5.4 Vertical parity), and true_false (2 records — needs boolean controller). **Validation**: `vendor/bin/phpunit --filter "VisualEditorControlCenterR53"` → **10 / 28 OK**. `vendor/bin/phpunit --filter "VisualEditor(Control\|SharedGlobals\|Vertical)"` → **122 / 499 OK** (was 111 / 468 — +11 R5.3 new). `node --test tests/visual-editor-brand-control-center-state.test.cjs` → **41 pass / 41 tests** (was 39/39 pre-R5.3 — +2 R5.3 chip cases). `node --test tests/visual-editor-media-manager-state.test.cjs` → **42 pass** (preserved — R5.3 is Control-Center-scoped). `composer agent-docs:refresh && composer agent-docs:check` → **54 curated / 443 discovered / 0 unmapped** (unchanged — no new hook or REST surface, no i18n keys added). PHP lint clean on both Vertical checkouts + the two DBVC files touched. No content mutated; no persistent WP option toggled outside test setUp/tearDown; no git operations either repo | R5.3 completes the "existing resolver families" R5 arc. Every family that had an existing resolver in `src/Resolvers/` (`AcfTextResolver`, `AcfChoiceResolver`, `AcfLinkResolver`, `AcfWysiwygResolver`, `AcfImageResolver`) is now wired through the drawer's factory + chip. Remaining R5 slices require NEW primitives: R5.2+color_picker needs `AcfColorPickerResolver` + a color-picker panel controller; R5.4 needs Vertical-side parity for the R3-B post_object/relationship path (2 curated records, trivial); true_false needs a new boolean controller + resolver (2 records). None of those primitives exist yet — each is a genuinely new slice rather than a wire-through slice. **Live-site: refresh the drawer and 37 previously-Unsupported image rows should now show Open**, load into the main editor panel with a Media Library picker (via the same shared `wp.media` frame factory the Media Manager uses), and save through MutationService with the R4-D-1 save-status-strip acknowledging the save | R5/R5.3 |
| E-107 | 2026-09-01 | DBVC + Vertical | **DBVC** `addons/visual-editor/src/Registry/Providers/SharedGlobalsControlProvider.php` (`supportedFieldTypes()` widened by 6 R5.2 types: `select`, `checkbox`, `radio`, `button_group`, `link`, `wysiwyg`; `buildValueSummary()` dispatch widened with 5 new branches — single-choice (select/radio/button_group), multi-choice (checkbox), link, wysiwyg — each delegating to 5 new public static helpers `buildTextSummary`, `buildSingleChoiceSummary`, `buildMultiChoiceSummary`, `buildLinkSummary`, `buildWysiwygSummary` so Vertical can reuse them without duplicating logic; `mapFieldFamily()` documented that R5.2 families fall into ControlRecord's `'other'` bucket since the whitelist has no per-family choice/link/wysiwyg slot); `addons/visual-editor/src/Registry/Providers/SharedGlobalsDescriptorFactory.php` (refactored `build()` dispatcher — text-family path now flows through a config-bag pattern rather than hardcoded values; new choice / link / wysiwyg branches all delegate to the same `buildSharedFieldDescriptor()` with per-family `{resolver, input, source_context, ui_options}` config. Choice branch picks `ui.input='checkbox_group'` for checkbox and `'select'` for select/radio/button_group; ACF `choices` map projected onto `ui.options = [{value,label}]` via new private `projectAcfChoices()` helper (sanitized value + label; empty values dropped). Link branch: `ui.input='link'`, `resolver='acf_link'`. Wysiwyg branch: `ui.input='richtext'`, `resolver='acf_wysiwyg'`. All emit `mutation.contract='shared_field'`, `kind='scalar'` — same contract as R5.1-a text-family; group-nesting inherits from R5.1-b `walkGroupChain()`); `addons/visual-editor/assets/js/brand-control-center-app.js` (`renderValueSummaryChip` gains three new family branches — `choice` (single-label chip when `summary.label` present; multi-count `<strong>N</strong> selected` chip with first-3-labels title tooltip when `firstLabels[]` present); `link` (title falling back to url + trailing `↗` glyph + full url as title tooltip); `wysiwyg` (stripped preview + subtle `· N words` suffix + full preview as title). All follow the same `title`-tooltip + `textContent`-only render invariants R5.1-a established); `addons/visual-editor/assets/css/control-center.css` (new `.__value-choice`, `.__value-choice strong`, `.__value-link`, `.__value-link-glyph`, `.__value-wysiwyg`, `.__value-wysiwyg-words` selectors — all inherit the compact one-line ellipsis pattern the parent `.__value-summary` `max-width:200px` enforces); `addons/visual-editor/src/Assets/AssetLoader.php` (2 new i18n keys — `controlCenterValueChoiceSelected = "selected"` for multi-choice count suffix, `controlCenterValueWysiwygWords = "· {count} words"` for wysiwyg word-count suffix); **Vertical (cross-repo, both checkouts)** `themes/vertical/functions/features/dbvc-visual-editor/includes/class-vf-vertical-control-provider.php` (`resolveStatus()` extended with an R5.2 branch — returns `'available'` when `unlocks_at === 'R5.2'` AND `field_type ∈ [select,checkbox,radio,button_group,link,wysiwyg]`; `true_false` (2 curated records) intentionally stays unsupported until a boolean controller ships. `buildDescriptor()` `$supported` whitelist widened to include the 6 R5.2 types. `buildValueSummary()` refactored to delegate to DBVC's static summary builders — text/choice/link/wysiwyg all flow through `\Dbvc\VisualEditor\Registry\Providers\SharedGlobalsControlProvider::build{Text,SingleChoice,MultiChoice,Link,Wysiwyg}Summary()` — choice branch lifts the ACF field object via `get_field_object($field_key, 'option', false, false)` to source the choices map. Mirrored byte-identically to both Vertical checkouts via `cp` + `diff -q`); `tests/phpunit/VisualEditorControlCenterR52Test.php` (new, 20 tests / 90 assertions — supportedFieldTypes widening; factory choice/link/wysiwyg shapes via `@dataProvider choiceFieldTypesProvider`; relationship-path regression guard; buildValueSummary shapes for single-choice / multi-choice / link / wysiwyg with truncation + edge cases; provider integration for R5.2 families); `tests/phpunit/VisualEditorVerticalProviderR51BTest.php` (extended with a new R5.2 `@dataProvider r52FamilyProvider` across 6 types + a true_false-stays-unsupported case; the two "unsupported short-circuit" cases were re-based onto image/R5.3 and color_picker/R5.2+color_picker fixtures since wysiwyg/R5.2 now flips to available); `tests/visual-editor-brand-control-center-state.test.cjs` (+4 R5.2 jsdom cases — single-choice chip, multi-choice count+tooltip chip, link chip with glyph + url tooltip, wysiwyg chip with stripped preview + word-count suffix) | R5.2 (a + b) — choice / link / wysiwyg unlock. **~155 curated group-nested Vertical records flip from `status='unsupported'` → `status='available'`** on the maintainer's live site (49 select + 27 checkbox + 16 radio + 6 button_group + 27 link + 30 wysiwyg = 155; the 2 true_false records defer). **Combined with R5.1's 182 records**, the drawer now has **337 editable Vertical rows** out of the 400 curated. Zero new REST routes, zero new mutation authority, zero new resolvers, zero new mutation contracts. Every save routes through MutationService via the existing AcfChoiceResolver / AcfLinkResolver / AcfWysiwygResolver under the `shared_field` contract; group-nesting inherits from R5.1-b's factory extension; every gate the R3-D two-part kill switch enforces still applies. **Panel-controller alignment**: `createSelectController` reads `ui.options = [{value,label}]` (factory projects ACF `choices`); `createCheckboxGroupController` same for multi-select; `createLinkController` reads the descriptor's URL/title/target from the resolver payload; `createRichTextController` handles TinyMCE lazy-load (R5.2-b's highest-risk piece — jsdom cannot exercise TinyMCE but the descriptor shape is correct and MutationService pipeline is unchanged). **Cross-panel coexistence preserved**: R3-C-2 D-063 (drawer + main panel coexist visibly) still holds; R4-D-1 save-status-strip fires the same way for R5.2 saves. **Validation**: `vendor/bin/phpunit --filter "VisualEditorControlCenterR52Test"` → **20 / 90 OK**. `vendor/bin/phpunit --filter "VisualEditor(Control\|SharedGlobals\|Vertical)"` → **111 / 468 OK** (was 84 / 358 pre-R5.2 — +27 R5.2 new: 20 in R52Test + 7 in VerticalProvider R5.2 extension). `node --test tests/visual-editor-brand-control-center-state.test.cjs` → **39 pass / 39 tests** (was 35/35 — +4 R5.2 chip cases). `node --test tests/visual-editor-media-manager-state.test.cjs` → **42 pass** (preserved). `composer agent-docs:refresh && composer agent-docs:check` → **54 / 443 / 0** (unchanged — 2 new i18n keys added; no new hook or REST surface). No content mutated; no persistent WP option toggled outside test setUp/tearDown; no git operations either repo. PHP lint clean on both Vertical checkouts + the two DBVC files touched | R5.2 delivers the middle chunk of the R5 unlock. **Live-site coverage after R5.2**: R5.1 (182) + R5.2 (155) = **337 of 400 Vertical records now editable** in the drawer. Remaining: R5.2+color_picker (20, needs a NEW color_picker resolver — no `AcfColorPickerResolver` in Resolvers/), R5.3 (37 image records), R5.4 (2 post_object/relationship — Vertical parity for the R3-B path), true_false (2, needs a NEW boolean controller). The 5-family value-summary chip vocabulary the drawer supports (text/choice/link/wysiwyg + relationship/post_object from R4-C-1b) covers every family the Vertical curation JSON contains except color_picker + image + true_false. R5.2's config-bag refactor of the factory dispatcher establishes the pattern R5.3 / R5.4 will reuse — each per-family branch is now a ~15-line dispatch call rather than a duplicated descriptor build. Real-browser QA on the live site is one drawer-refresh + Open-click away | R5/R5.2 |
| E-106 | 2026-09-01 | DBVC + Vertical | **DBVC** `addons/visual-editor/src/Registry/Providers/SharedGlobalsDescriptorFactory.php` (extended `buildSharedFieldDescriptor()` with a new private `walkGroupChain($leaf_field)` helper — walks the ACF `parent` chain via `acf_get_field()` from the leaf up until it hits a `group_XXX` field-group boundary, returning `[ [key:root_group_key, name:root_group_name], ..., [key:leaf_key, name:leaf_name] ]` when nested or `[]` when top-level. When the chain is non-empty, `buildSharedFieldDescriptor()` sets `source.field_selector = root_field_name`, `source.field_selector_raw = root_field_name`, `source.group_write_path = array_slice($chain, 1)`, `source.is_grouped_field = true`, and `path.rootFieldName / rootFieldKey` to the ROOT group's identity while `source.field_name` / `field_key` / `leaf_field_name` / `leaf_field_key` remain the LEAF identity — mirrors the Media Manager R2-F RK-013 group_write_path pattern that AbstractAcfResolver's `writeGroupedFieldValue` + `readGroupedFieldValue` already handle. Top-level shared globals text fields see an empty chain and fall through to the unchanged shape); **Vertical (cross-repo)** `themes/vertical/functions/features/dbvc-visual-editor/includes/class-vf-vertical-control-provider.php` (`mapRecord` now computes `$status` via new private `resolveStatus($unlocks_at, $field_type)` helper — returns `'available'` when `unlocks_at === 'R5.1'` AND `field_type ∈ [text,textarea,url,email,number]`; every other record stays `'unsupported'`. Source projection widened with `field_type` + `unlocks_at` so downstream `buildDescriptor` / `buildValueSummary` gate on them without re-reading the curation JSON. `buildDescriptor()` — for `record->status === 'available'` records only: gates on `function_exists('get_field_object')` fail-safe; lifts the leaf ACF field object via `get_field_object($field_key, 'option', false, false)` (key-based lookup handles both top-level and group-nested paths); guards against ACF returning a group container instead of the leaf (`resolved_type` must be one of the R5.1 text-family types); falls back to using the curated `field_name` when ACF's leaf `name` is empty (skipping when the curated name uses `>` group delimiters SharedGlobalsDescriptorFactory does not consume); delegates to `\Dbvc\VisualEditor\Registry\Providers\SharedGlobalsDescriptorFactory::build()`. New private `descriptorFactory()` lazy accessor + new `setDescriptorFactory()` public seam for test injection. `buildValueSummary()` mirrors DBVC's SharedGlobalsControlProvider text-family summary shape — gates on `record->status === 'available'` + text-family `field_type` + `function_exists('get_field')`, reads via `get_field($field_key, 'option', false)` (key-based lookup unwraps group-subfield prefixed meta), returns `{family:'text', preview (mb_substr at 32 chars), charCount, truncated}` with empty / non-scalar → null. Mirrored byte-identically to both Vertical checkouts — `/Users/rhettbutler/Documents/LocalWP/dbvc-codexchanges/…` and `/Users/rhettbutler/Documents/LocalWP/frameworkflo-live/…` — via `cp` + `diff -q` verification); `tests/phpunit/VisualEditorVerticalProviderR51BTest.php` (new, 9 tests / 34 assertions — loads the Vertical class file via a guarded `require_once` with `markTestSkipped` fallback for environments without the theme checkout, then covers: mapRecord flips text-family R5.1 records to `available` across the 5-type `@dataProvider`; non-text-family R5.1 records stay unsupported even with a deliberate curation mismatch; text-family records tagged for a later R5.x slice stay unsupported; buildDescriptor returns null for unsupported records; buildValueSummary returns null for unsupported records. The buildDescriptor + buildValueSummary happy paths need real ACF and are exercised at runtime on the live site — this test set covers the pure-PHP claims) | R5.1-b — Vertical cross-repo unlock. **Second R5.1 sub-slice** and the one that produces the user-visible impact: **182 R3-BX-curated Vertical text-family records tagged `unlocks_at="R5.1"` flip from `status='unsupported'` → `status='available'`** on the maintainer's live site, going from "Unsupported" button to "Open" button. **All 182 are group-nested** (per the curation JSON's `>`-delimited field paths); the DBVC-side `walkGroupChain()` extension emits the `group_write_path` + `is_grouped_field` source keys that `AbstractAcfResolver::writeGroupedFieldValue` requires to write THROUGH the parent group's own `update_value` (Media Manager R2-F RK-013 pattern proven at group-nested image scale). **Zero new REST routes**, zero new mutation authority, zero new resolvers, zero new mutation contracts. Every save still routes through `MutationService` via `AcfTextResolver` under the `shared_field` contract; every gate the R3-D two-part kill switch enforces still applies. **Cross-repo delegation architecture**: Vertical instantiates `SharedGlobalsDescriptorFactory` directly (public + stateless per R3-C-1) rather than duplicating the descriptor shape. The single-factory invariant holds — R5.2 / R5.3 / R5.4 slices will extend the same factory with their per-family branches and Vertical inherits automatically. **Validation**: `vendor/bin/phpunit --filter "VisualEditorVerticalProviderR51B"` → **9 tests / 34 assertions OK** (with a graceful skip fallback for CI without the theme). `vendor/bin/phpunit --filter "VisualEditor(Control\|SharedGlobals\|Vertical)"` → **84 tests / 358 assertions OK** (was 75/324 pre-R5.1-b — +9 R5.1-b new). `node --test tests/visual-editor-brand-control-center-state.test.cjs` → **35 pass / 35 tests** (unchanged — R5.1-b touched no drawer JS or CSS). `node --test tests/visual-editor-media-manager-state.test.cjs` → **42 pass** (preserved). `composer agent-docs:refresh && composer agent-docs:check` → **54 curated / 443 discovered / 0 unmapped** (unchanged — no new hook or REST surface). PHP lint clean on both Vertical checkouts + the DBVC factory. `diff -q` confirms both Vertical checkouts identical after the mirror. No content mutated; no persistent WP option toggled outside test setUp/tearDown; no git operations either repo | **R5.1 is now fully landed** (R5.1-a DBVC + R5.1-b Vertical). The pattern R5.1 established — "widen SharedGlobals whitelist + add factory family branch + Vertical resolveStatus gate for the same family + one delegation call in Vertical::buildDescriptor" — is the template R5.2 / R5.2+color_picker / R5.3 / R5.4 will follow, each unlocking their family (color_picker: 20 records; wysiwyg: 30; select/radio/checkbox/button_group/true_false: ~100; link: 27; image: 37; relationship/post_object: 2). R5.1-b's group-nesting factory extension is reusable by every future R5.x slice too — the Media Manager R2-F RK-013 pattern is now proven on option-page shared_field writes. Real-browser verification is one Open click away on the maintainer's site once the drawer is refreshed | R5/R5.1-b |
| E-105 | 2026-08-31 | DBVC | `addons/visual-editor/src/Registry/Providers/SharedGlobalsControlProvider.php` (getControls whitelist swapped from the hard-coded `['relationship', 'post_object']` to `self::supportedFieldTypes()` which returns the union of relationship + post_object + text + textarea + url + email + number; `fieldFamily` on the emitted record now runs through the new `self::mapFieldFamily($field_type)` helper that collapses text/textarea/url/email/number onto the `'text'` bucket — matches the `ControlRecord::fromArray` whitelist so the drawer's filter chip + chip render see a single family. `buildValueSummary` widens the same accepted-types check to `supportedFieldTypes()` and branches inside: text-family reads the option value via the existing `optionValueResolver` seam, trims + sanitizes, computes preview (mb_substr at 32 chars) + `charCount` + `truncated`; empty / non-scalar values collapse to null so the drawer renders no chip. Relationship / post_object summary path is byte-identical to R4-A. Two new public static helpers `supportedFieldTypes()` + `mapFieldFamily($field_type)` at the bottom of the class); `addons/visual-editor/src/Registry/Providers/SharedGlobalsDescriptorFactory.php` (`build()` opens with an in_array guard on the R5.1-a text-family types and delegates to a new private `buildSharedFieldDescriptor()` helper for those; relationship + post_object falls through to the unchanged R3-C-1 path so its shape stays byte-identical. `buildSharedFieldDescriptor()` mints an `EditableDescriptor` with `source.type='acf_field'`, `source_context='toolbar_shared_global_option_field'`, no `reference_*` metadata, `ui.input='textarea'` for textarea else `'text'`, `resolver.name='acf_text'`, `mutation.contract='shared_field'`, `kind='scalar'`, `target='field'`, `renderContext='field'`, `reloadAfterSave=true` — matches the resolver-support contract AcfTextResolver already ships and the shared_field entry MutationContractService already whitelists as writable); `addons/visual-editor/assets/js/brand-control-center-app.js` (`renderValueSummaryChip` gains a `family === 'text'` branch: emits `.__value-text` `<span>` with `title` = full preview + `textContent` = preview; when `summary.truncated` is true, appends a `.__value-text-count` `<span>` carrying the `controlCenterValueTextTruncated` template `"({count})"`. Empty preview collapses to no chip. relationship / post_object branch is untouched); `addons/visual-editor/assets/css/control-center.css` (new `.__value-text` + `.__value-text-count` selectors — text-color + ellipsis + subtle count suffix); `addons/visual-editor/src/Assets/AssetLoader.php` (1 new i18n key `controlCenterValueTextTruncated = "({count})"` — template consumed by the client-side chip renderer); `tests/phpunit/VisualEditorControlCenterR51ATest.php` (new, 15 tests / 124 assertions covering all three surfaces: static-helper matrix; provider whitelist widening + fieldFamily collapse; factory `shared_field` descriptor shape via `@dataProvider textFamilyTypesProvider` across text/textarea/url/email/number; factory relationship-path invariant preservation; buildValueSummary short-text / long-text-with-truncation / empty-value / non-scalar-value); `tests/phpunit/VisualEditorSharedGlobalsControlProviderTest.php` (renamed `test_non_relationship_or_post_object_field_is_skipped` → `test_unsupported_field_type_is_skipped`; changed the fixture from `type=text` (which R5.1-a widens to accept) to `type=color_picker` (R5.2 territory, still skipped) so the still-skipped promise stays covered); `tests/visual-editor-brand-control-center-state.test.cjs` (+2 R5.1-a jsdom cases: text chip renders preview + title + no suffix on non-truncated response; text chip renders `.__value-text-count` suffix on truncated response); `docs/agents/manifest.json` (rotated the `dbvc_visual_editor_control_center_description` extension-point discovery id `5d884d1f → c6dfb57c` from the line-shift produced by the two new static helpers added at the bottom of SharedGlobalsControlProvider) | R5.1-a — DBVC-side text-family unlock for the Brand Control Center. **First R5-series slice**; DBVC-only (Vertical cross-repo R5.1-b is a follow-on). Zero new backend routes, zero new mutation authority, zero new resolvers, zero new mutation contracts — the AcfTextResolver has supported text/textarea/url/email/number since long before R3, and `shared_field` has been whitelisted in `MutationContractService::isWritable` since the same slice. R5.1-a is a pure "wire the existing pipeline to the drawer" slice. **What ships end-to-end**: any Shared Globals option field whose ACF type is text/textarea/url/email/number now (a) renders in the drawer with `status='available'`, `fieldFamily='text'`, and a description if the provider filter emits one; (b) lazily hydrates a text preview chip via the R4-A batch value-summaries endpoint; (c) opens on click into the existing main editor panel via the R3-C-1 open route + the R4-D-1 save-status-strip listener → panel save routes through MutationService via AcfTextResolver → drawer strip acknowledges the save. **No Vertical rows unlock in R5.1-a** — that's R5.1-b (183 records tagged `unlocks_at="R5.1"` in the R3-BX curation JSON, 182 shippable in R5.1 after excluding the 1 date_picker record). **Validation**: `vendor/bin/phpunit --filter "VisualEditorControlCenterR51A"` → **15 tests / 124 assertions OK**. `vendor/bin/phpunit --filter "VisualEditor(Control\|SharedGlobals)"` → **75 tests / 324 assertions OK** (was 60/200 — +15 R5.1-a new; 1 pre-existing R3-B test renamed + refactored to use a still-unsupported family since the old `type=text` fixture now surfaces a real record). `node --test tests/visual-editor-brand-control-center-state.test.cjs` → **35 pass / 35 tests** (was 33/33 pre-R5.1-a — 2 new cases). `node --test tests/visual-editor-media-manager-state.test.cjs` → **42 pass** (preserved — R5.1-a is Control-Center-scoped). `composer agent-docs:refresh && composer agent-docs:check` → **54 curated / 443 discovered / 0 unmapped** (1 hash rotated on the description filter due to line-shift from the two new static helpers; re-mapped in manifest). No content mutated; no persistent WP option toggled outside test setUp/tearDown; no git operations either repo | R5.1-a proves the "wire an existing resolver through the drawer" pattern that R5.2 / R5.2+color_picker / R5.3 / R5.4 will reuse — each per-family slice becomes a matter of (1) whitelist the field type in SharedGlobalsControlProvider, (2) branch inside SharedGlobalsDescriptorFactory to build the appropriate contract-shaped descriptor for that resolver, (3) extend buildValueSummary for the family, (4) extend renderValueSummaryChip. **Visible user impact on the maintainer's site**: currently 1 Shared Globals field is configured (a relationship), so no new rows appear yet. R5.1-b is the slice that flips 182 Vertical rows from `unsupported` → `available` — likely the maintainer's next request | R5/R5.1-a |
| E-104 | 2026-08-31 | DBVC + Vertical | **DBVC** `addons/visual-editor/assets/js/overlay-app.js` (new `dispatchPanelSaved(token)` helper next to `dispatchControlCenterEvent` — gated behind `isControlCenterEnabled()` matching the existing dispatch policy; kill-switch-off callers dispatch nothing. Fires `dbvc:visual-editor:panel:saved` with detail `{token}` on both `handleSave` success paths — composite save at ~line 12603 and non-composite at ~line 12684. Placed AFTER the panel-state / status-bar updates and BEFORE the `shouldReloadAfterSave` `window.location.reload` so the drawer has one tick to render its confirmation before a page reload cuts it off); `addons/visual-editor/assets/js/brand-control-center-app.js` (new `state.activeToken` — stored in `openRow` alongside `activePublicId` so the panel:saved listener can gate on matching saves only; a save from the Shared Globals popover carries a different token and is intentionally ignored. New `state.saveStatusStrip` + `state.saveStatusTimer` — transient strip populated by the listener, cleared by a 2500ms fade timer. New `SAVE_STATUS_STRIP_MS = 2500` module constant. New `handlePanelSaved(event)` — reads `detail.token`, gates on `token === state.activeToken` AND `state.activePublicId` non-empty; populates strip with `labelForPublicId(state.activePublicId)`, schedules the fade timer (clears any pending timer first so back-to-back saves reset the fade), calls `renderSaveStatusStrip()`, and announces via the single polite live region (same announcer path R3-C-2 already owns — the strip carries `aria-hidden="true"` to prevent AT double-announcement). New `renderSaveStatusStrip()` — inserts / removes the `.__save-status-strip` at the top of the table wrap; on empty state, removes the node from the DOM. `close()` extended to clear the fade timer, null the strip state, and clear `activeToken` so a reopen starts clean. `renderList()` calls `renderSaveStatusStrip()` after `renderRefreshOverlay(wrap)` so a filter change / view-mode flip mid-fade preserves the confirmation. `publicState()` gains `activeToken` + `saveStatusStrip` for test observability. Document listener wired in `mount()` — one listener, added alongside the existing control-center:toggle / :close listeners); `addons/visual-editor/assets/css/control-center.css` (new `.__save-status-strip` selector — sticky `top: 0`, success-green background, `animation: dbvc-ve-cc-save-status 2500ms ease forwards` with a 0%/8%/80%/100% keyframe fade; reduced-motion block suppresses the animation entirely, matching the drawer's other transition-based effects); `addons/visual-editor/src/Assets/AssetLoader.php` (1 new i18n key `controlCenterSaveStatus = "Saved {label}."` — same string used for both the visual strip and the polite live-region announcement); `tests/visual-editor-brand-control-center-state.test.cjs` (+3 R4-D-1 jsdom cases: strip renders on matching token with `aria-hidden="true"` + correct label; foreign-token event is ignored + no strip DOM node created; strip DOM + state clear after the fade timer elapses) | R4-D-1 — save-status-strip. **The first R4-D sub-slice** and the first R4 slice that touches `overlay-app.js` in any way. Reuses D-064's fail-soft posture: the strip is scoped to descriptors the drawer opened AND the Control Center feature is enabled — every other code path (Shared Globals popover saves, media-manager saves, all overlay-app saves when the CC kill-switch is off) is transparently unaffected. **Contract preserved**: no new REST routes, no new mutation authority; `dispatchPanelSaved` is a UI event dispatch only. Single polite live region rule holds — the announcer is authoritative, the strip is `aria-hidden`. Forbidden `data-*` invariant untouched (the strip's `data-dbvc-ve-control-center-save-status-strip="1"` is UI-scoped). Reduced-motion suppresses the fade animation. Kill switch operationally unchanged. **Cross-panel coexistence preserved**: the strip listener gates on `state.activeToken`, so a viewer with the drawer open who also uses the Shared Globals popover to edit a DIFFERENT descriptor sees NO strip in the drawer for that save (matches R3-C-2 D-063 coexistence). **Validation**: `node --test tests/visual-editor-brand-control-center-state.test.cjs` → **33 pass / 33 tests** (was 30/30 pre-R4-D-1 — 3 new cases). `node --test tests/visual-editor-media-manager-state.test.cjs` → **42 pass** (baseline preserved — the overlay-app.js addition is scoped to Control Center-enabled contexts, media-manager save flow is untouched). `vendor/bin/phpunit --filter "VisualEditor(Control\|SharedGlobals)"` → **60 tests / 200 assertions OK** (unchanged — R4-D-1 touched no backend). `composer agent-docs:refresh && composer agent-docs:check` → **54 curated / 443 discovered / 0 unmapped** (unchanged — 1 new i18n key added; no new hook or REST surface). No content mutated; no persistent WP option toggled outside test setUp/tearDown; no git operations either repo. **Note on R4-A live-site bug fix**: an unrelated fix landed the same day in the Vertical theme (`themes/vertical/functions/features/dbvc-visual-editor/dbvc-visual-editor.php:107` + `.../includes/class-vf-vertical-control-provider.php:265`) — both callsites of `vf_field_context_get_entry_primary_purpose()` were passing a string field name where the function requires the resolved entry array. Fixed by adding a `vf_field_context_get_entry_for_runtime_field($field_name, 'option')` catalog lookup before the purpose call. Fixed in both Vertical checkouts (dbvc-codexchanges + frameworkflo-live mirror). This was a pre-existing R4-A cross-repo landing bug that only surfaced on the first live-site drawer request (all R4-A tests are PHPUnit against DBVC in isolation; the Vertical hook was untested against a real WP context) | R4-D-1 delivers the last cross-panel affordance the R4 mockup pinned. **All 7 pinned R4-B decisions + the save-status-strip are now honored end-to-end**, meaning the drawer's UI is functionally complete against the mockup. R4-D-2 (real-browser QA at 1440×900 + 1280×720 with the 400-row registry, long-label truncation calibration, release-notes/rollback update) is the only R4 work remaining before R5.x per-family editing factories. The R4-A live-site bug fix that landed alongside proves the value of routing R4-A cross-repo work through this kind of real-browser exercise — a jsdom + PHPUnit-only slice missed a real TypeError in the Vertical filter callback | R4/R4-D-1 |
| E-103 | 2026-08-31 | DBVC | `addons/visual-editor/assets/js/brand-control-center-app.js` (added `DEFAULT_QUERY.provider = 'all'` axis; new module constants `VIEW_MODES`, `LS_KEY_VIEW_MODE`, `LS_KEY_EXPANDED_GROUPS`; new state `viewMode` ('category'\|'provider', default 'category') + `expandedGroups` (object map of currently-EXPANDED groupKeys — groups default to COLLAPSED, so the persisted list only records viewer deviations from that default and stays valid even after new fixture groups appear); new localStorage helpers `readStoredString`/`writeStoredString` (try/catch-wrapped for private-window safety), `loadStoredViewMode` (validates against `VIEW_MODES`), `loadStoredExpandedGroups` (guards a bad JSON payload), `persistExpandedGroups`; new `setViewMode(mode)` — validates, snapshots the active row's publicId BEFORE the re-render for focus continuity, persists to localStorage, restores focus on the same publicId's Open button in the new layout; new `toggleGroup(groupKey)` — flips the map entry (adds when expanding, deletes when collapsing back to default), persists, re-renders; new `clearSearchInput()` — clears the input + fires a `loadControls({reason:'query'})` round-trip; new provider helpers `providerIdFromPublicId` (splits on `:`), `providerLabel` (Shared Globals / Vertical / fallback title-case), `providersFromItems` (deduped provider tab entries), `tabAxisForViewMode`/`tabEntriesForViewMode`/`tabLabelForViewMode` (view-mode dispatchers); `ensureRoot()` extended to rehydrate `viewMode` + `expandedGroups` from localStorage on first open; new view-toggle segmented control (`role="tablist"` with `aria-label="Category view"` for a11y disambiguation vs the category tablist below) in `createHeader()` between the title-block and close button, with two `role="tab"` buttons carrying `data-view-mode` + `data-dbvc-ve-control-center-action="set-view-mode"`; new `renderViewToggle()` keeps `aria-selected` + `tabIndex` in sync; new `search-wrap` DOM (leading `.__search-icon` glyph + trailing `.__search-clear` button carrying `data-...-action="clear-search"`) that replaces the R3-C-2 bare input; `renderFilters()` extended to toggle `.__search-clear` visibility based on `state.query.search === ''`; new `handleClick` branches for `set-view-mode`, `toggle-group`, and `clear-search`; existing `select-tab` branch reads `data-tab-slug` with `data-category` / `data-provider` fallbacks (R3-C-2 tests remain compatible); `handleKeydown` extended with `ArrowLeft`/`ArrowRight` nav when focus is inside the view-toggle tablist — the arrow key advances the segmented control AND moves focus to the newly-selected button; `renderTabs()` + `createTabButton()` are now view-mode aware — tabs carry both `data-tab-slug` (generic) and `data-category` / `data-provider` (axis-specific); `selectTab()` routes into `state.query.category` or `state.query.provider` depending on `viewMode`; `itemMatchesFilters()` branches on `viewMode` — provider mode filters on `providerIdFromPublicId(item.publicId)`; `clearFilters()` preserves BOTH `category` and `provider` axes; **`createTableWrap()` no longer creates the R3-C-2 single `<tbody>` sentinel** — rows now live inside per-group `<tbody class="__group">` elements built by `renderList()`; new `clearTableTbodies(wrap)` helper; new `buildGroupsFromVisible(visible)` — keys groups `{providerId}::{group}` (fallback 'other'), preserves first-appearance order (matches R4-A `sortKey ASC → label ASC → publicId ASC` global sort); new `renderGroupTbody(group)` — emits the tbody with `data-group-key` + `data-provider-id` + `is-collapsed` when the group is not in `state.expandedGroups`; new `renderGroupHeaderRow(group, isExpanded)` — colspan-2 disclosure row with `.__group-toggle` (`aria-expanded` + `aria-label` for expand/collapse), chevron icon, title, count badge; `renderList()` overhauled — clears ALL tbodies via `clearTableTbodies`, then either renders panel-state OR iterates `buildGroupsFromVisible` and appends one tbody per group; `registerVisibleValueSummaryTargets(wrap)` signature simplified — takes wrap only, queries rows at the wrap level so per-group tbodies are picked up; dead `clearRows(tbody)` helper removed; `publicState()` extended with `viewMode` + `expandedGroups` for test observability); `addons/visual-editor/assets/css/control-center.css` (new `.__view-toggle` + `.__view-toggle-option` selectors — segmented control with `aria-selected="true"` bg swap, focus-visible ring; new `.__search-wrap` + `.__search-icon` + `.__search-clear` selectors — icon positioned absolute at input left, clear button at input right with hover/focus states; new `.__group-header` + `.__group-header-cell` + `.__group-toggle` + `.__group-toggle-icon` (chevron rotates 90deg on `[aria-expanded="true"]` via a 160ms transition) + `.__group-title` + `.__group-count` selectors; new `.__group.is-collapsed .__row { display: none }` rule — semantic tree stays intact; new `@media (prefers-reduced-motion: reduce)` block suppressing chevron rotation + segmented-control transitions); `addons/visual-editor/src/Assets/AssetLoader.php` (11 new i18n keys — `controlCenterViewToggleLabel`, `…ViewByCategory`, `…ViewByProvider`, `controlCenterGroupExpand`, `…GroupCollapse`, `…GroupControlsCount`, `…GroupUnnamed`, `controlCenterProviderShared`, `…ProviderVertical`, `…ProviderUnknown`, `controlCenterClearSearch`); `tests/visual-editor-brand-control-center-state.test.cjs` (+5 R4-C-2 jsdom cases: view-mode toggle flips + `aria-selected` reflects state; localStorage seed is read on first open + write path updates localStorage on flip; group headers render one-per-`record.group`, collapsed by default, group-key + count badge correct; toggling a group persists in localStorage AND removes when collapsed back to default (deviation-only persistence); row-focus continuity survives a view-mode flip); `docs/dropins/dbvc-visual-editor-brand-controls-guide/releases/R4-EXPANDED-GLOBAL-BRAND-CONTROL-CENTER.md` (§R4-C-2 checkpoint added); `addons/visual-editor/docs/handoffs/DBVC_R4C_RESUME_PROMPT.md` archived to `addons/visual-editor/docs/handoffs/archive/DBVC_R4C_RESUME_PROMPT.md` (R4-C fully landed) | R4-C-2 — visual polish + view-mode toggle + collapsible group headers + search-wrap DOM + invariant sweep. **R4-C now fully landed** (R4-C-1a + R4-C-1b + R4-C-2). The drawer's DOM is now the mockup's target shape: header segmented view-mode toggle, per-tab collapsible group headers keyed `{providerId}::{group}`, per-viewer `localStorage` persistence for both view mode and expanded-groups deviations, search-wrap with leading icon + clear button, per-record description line, per-row lazy relationship/post_object chip via IntersectionObserver → batch POST. **All 7 pinned R4-B decisions are honored end-to-end**: (1) persistent search input (R3-C-2 preserved + R4-C-2 clear button); (2) flat tab strip (unchanged shape, view-mode-aware slugs); (3) collapsible group headers within each category (R4-C-2 tbody-per-group); (4) description as muted second line (R4-C-1a); (5) per-family value-summary chips (R4-C-1b relationship/post_object; other families → R5.x); (6) Shared Globals folded into the same category system + view-mode toggle (R4-C-2 provider-mode tabs); (7) view-mode toggle in drawer header with localStorage persistence (R4-C-2). **Invariant sweep**: reduced-motion suppresses chevron rotation + segmented-control transitions; row-focus continuity survives view-mode flips; single polite live region rule preserved (announcer stays authoritative; view toggle + group headers are visual affordances); `data-public-id` remains the sole client-authoritative row token (new attributes `data-view-mode`, `data-group-key`, `data-tab-slug`, `data-provider`, `data-provider-id` are all UI-scoped, none carries a target authority the save pipeline reads). **Cross-realm test observation**: two assertions swapped from `deepStrictEqual` to `JSON.stringify` comparison — jsdom's plain-object prototype differs from Node's plain-object prototype, which `deepStrictEqual` treats as a mismatch. Documented inline in the tests. **Zero backend edits** — R4-A backend contract remains stable, kill switch untouched, no new REST routes, no new mutation authority. **Validation**: `node --test tests/visual-editor-brand-control-center-state.test.cjs` → **30 pass / 30 tests** (was 25/25 pre-R4-C-2 — 5 new cases). `node --test tests/visual-editor-media-manager-state.test.cjs` → **42 pass** (baseline preserved). `vendor/bin/phpunit --filter "VisualEditor(Control\|SharedGlobals)"` → **60 tests / 200 assertions OK** (unchanged from R4-A landing — R4-C-2 touched no backend). `composer agent-docs:refresh && composer agent-docs:check` → **54 curated / 443 discovered / 0 unmapped** (unchanged — 11 new i18n keys added; no new hook or REST surface). No content mutated; no persistent WP option toggled outside test setUp/tearDown; no git operations either repo | **R4-C is complete.** The R3 drawer + R4-A backend + R4-B mockup + R4-C-1a/1b/2 implementation loop is closed — the production drawer consumes the entire R4-A backend contract and translates every mockup DESIGN-DECISION into shipping code. Follow-ons: R4-D (Shared Globals transition + hardening — real-browser QA at 1440×900 + 1280×720 with a 400-row registry, long-label truncation calibration, save-status-strip that dispatches from `overlay-app.js` on successful save, release-notes/rollback update) and R5.x (per-family editing factories that flip Vertical rows from `unsupported` to `available`). R4-C-2's tbody-per-group structural refactor establishes the pattern any future R4-D structural change should follow (semantic tree preserved via `display:none`, not `removeChild`). Fresh resume prompt for R4-D should ship alongside the archival of `DBVC_R4C_RESUME_PROMPT.md` | R4/R4-C-2 |
| E-102 | 2026-08-31 | DBVC | `addons/visual-editor/assets/js/brand-control-center-app.js` (new `state.valueSummaries` — publicId → summary\|null\|'loading' cache, lifetime-scoped; new `state.valueSummaryObserver` — lazy IntersectionObserver rooted on `.__table-wrap` with `threshold: 0`; new `state.valueSummaryPending` — dedup queue; new `state.valueSummaryFlushTimer` — 50ms debounce; new `ensureValueSummaryState()` + `ensureValueSummaryObserver(wrap)` + `teardownValueSummaryObserver()` helpers — the observer is created lazily on first row registration and disconnected in `close()` (which also empties the cache so a reopen re-hydrates); new `handleValueSummaryIntersect(entries)` — unobserves each intersecting row immediately, short-circuits any publicId already cached (`loading`/summary/null), pushes fresh ones into the pending queue, schedules a flush; new `scheduleValueSummaryFlush()` — 50ms debounce; new `valueSummariesUrl()` builder; new `flushValueSummaries()` — takes up to **20** publicIds (D-064 batch cap headroom; server hard-caps at 50), marks them `'loading'`, chains another flush if the queue still has entries, POSTs `{publicIds}` to `/control-center/value-summaries`, merges response into the cache, and calls `patchValueSummarySlot(publicId)` per record for a **surgical DOM patch** (mirrors Media Manager R2-E3's per-row patch pattern — no full `renderList()` so focus + observer registrations survive); network failure fails soft — every publicId in the batch collapses to `null` (empty slot), never re-queued so a broken endpoint cannot create a retry loop; new `patchValueSummarySlot(publicId)` — finds the row's `.__value-summary[data-public-id]` slot and swaps its children in place; new `renderValueSummaryChip(summary)` — per-family dispatcher; **only** `relationship` + `post_object` render a chip in R4-C-1b (`<strong>{count}</strong> connected` with `title="{firstTitles.join(', ')}"`); every other family / `null` returns no chip (empty slot); R5.x owns the remaining chip shapes; new `registerVisibleValueSummaryTargets(wrap, tbody)` — called at the end of `renderList()`, registers only rows whose `data-status="available"` and whose publicId is unhydrated so filter changes + rerenders re-attach the observer without thrashing the endpoint; `renderRow()` gained a `.__value-summary[data-public-id]` slot before the Open button via new `renderValueSummarySlot(item, status, publicId)` — cached summaries render synchronously, non-`available` rows render an empty slot; `close()` extended to call `teardownValueSummaryObserver()` and null-out the caches; `publicState()` extended with `valueSummaries` for test observability; **graceful degrade** — `typeof window.IntersectionObserver !== 'function'` short-circuits the observer creation and rows just render empty slots, never a fallback fetch); `addons/visual-editor/assets/css/control-center.css` (new `.__value-summary` selector — inline-flex chip container with `max-width:200px`, ellipsis truncation, and a `:empty { display: none }` rule so unhydrated / null rows do not claim visual space next to the Open button; new `.__value-relationship` selector — count + label chip); `addons/visual-editor/src/Assets/AssetLoader.php` (1 new i18n key: `controlCenterValueRelationshipConnected` — label suffix for the "N connected" chip); `tests/visual-editor-brand-control-center-state.test.cjs` (new `installIntersectionObserverPolyfill(window)` — minimal IO polyfill exposed on `window.__ioRegistry` so tests can trigger intersections synthetically via `triggerAll()` / `trigger(predicate)`; polyfill honors `unobserve` + `disconnect` so post-first-intersection re-triggers do not fire — matches real browser behavior; +4 R4-C-1b jsdom cases: only `available` rows observed; intersecting rows POST with correct publicIds body; response summaries render as relationship chips + null summaries render empty slots; hydrated summary is not re-requested on subsequent triggers) | R4-C-1b — batch value-summary loader that consumes R4-A's `POST .../control-center/value-summaries` endpoint. **Zero backend edits.** Delivers the mockup COMPONENT-NOTES §3 lazy-hydration contract exactly as pinned in D-064: `IntersectionObserver` root = drawer `.__table-wrap`, only `available` rows observed, batch up to 20 per flush (server cap 50 as headroom), per-record capability recheck happens server-side (`ControlCenterValueSummariesController` already gates via descriptor + `CapabilityManager::canEditDescriptor` per R4-A). **Cache discipline**: per-drawer-lifetime, publicId-keyed, `'loading'` / summary / `null` states; cleared on `close()`; a re-render never re-observes an already-cached row (verified in test 4). **Surgical DOM patch**: only the affected row's `.__value-summary` slot is swapped in place — full `renderList()` never re-runs on a summary response, so focus + observer registrations on sibling rows survive. **Per-family dispatcher**: only `relationship` + `post_object` render for R4-C-1b — every other family and every `null` summary renders an empty slot. R5.x owns the remaining chips. **Graceful degrade**: `IntersectionObserver` unavailable → observer helpers short-circuit and rows render empty slots; no fallback fetch (the drawer is not expected to run in an environment without IO, and a fallback would defeat the lazy-hydration point). **Fail-soft on network error**: batch entries collapse to `null` (empty slot), never re-queued. Single polite live region rule preserved (chip is a persistent visual affordance, not an announcement). Forbidden `data-*` invariant untouched (the new `data-public-id` on the summary slot re-uses the same allowed attribute). Kill switch untouched. **Validation**: `node --test tests/visual-editor-brand-control-center-state.test.cjs` → **25 pass / 25 tests** (was 21/21 pre-R4-C-1b — 4 new cases). `node --test tests/visual-editor-media-manager-state.test.cjs` → **42 pass** (baseline preserved). `vendor/bin/phpunit --filter "VisualEditor(Control\|SharedGlobals)"` → **60 tests / 200 assertions OK** (unchanged from R4-A landing — R4-C-1b touched no backend). `composer agent-docs:refresh && composer agent-docs:check` → **54 curated / 443 discovered / 0 unmapped** (unchanged — one i18n key added; no new hook or REST surface). No content mutated; no persistent WP option toggled outside test setUp/tearDown; no git operations either repo | R4-C-1b delivers the second sub-slice of the R4-C-1a/1b/2 split: the drawer now consumes both R4-A endpoints (the widened list route from R4-C-1a and the batch summaries endpoint from R4-C-1b). The R4-A backend surface is now fully consumed by production code; R4-C-2 is purely presentational (view-mode toggle + collapsible group headers + search-wrap DOM + state gallery + invariant sweep). The Media Manager R2-E3 surgical-DOM-patch pattern is now proven for a second slice, reinforcing the "avoid full re-render on partial data update" idiom for future subsystems. The IntersectionObserver polyfill added to the test harness is reusable by future observer-driven drawer features (R4-C-2's collapsible group headers do not need it, but any future virtualization or lazy image loader would) | R4/R4-C-1b |
| E-101 | 2026-08-31 | DBVC | `addons/visual-editor/assets/js/brand-control-center-app.js` (rewrote `priorityFromItem()` to derive `must`/`should`/`nice` from the `sortKey` prefix `vertical_[1\|2\|3]_*`, dropping the dead pre-R4-A `item.priority` / `item.meta.priority` fallback; `listUrl()` now accepts a `{family,q}` params object and encodes non-empty values as `?family=…&q=…`; `loadControls({reason})` supports `'initial'` / `'query'` / `'retry'`, sets `state.requestStatus = 'loading-refresh'` for non-initial requests when items are already loaded — keeps the current rows visible under the previously-unused `.__refresh-overlay` — and captures `payload.providerErrors` into `state.providerErrors` (resetting `providerErrorsDismissed` only when the errored-provider set changes); `handleInput` search debounce widened from 180ms → **250ms** per the R4 mockup pinned number; `toggleChip` fires an immediate round-trip when the axis is `fieldFamily` (server-side per R4-A), stays client-side for `status` + `priority`; `itemMatchesFilters` drops the search + `fieldFamily` branches (server owns them now); `clearFilters` fires a round-trip only when a server-scoped param (`fieldFamily` or `search`) was cleared; new `renderProviderErrorBanner()` inserts `.__notice--provider-error` (`role="status"`, dismiss button `data-…-action="dismiss-provider-error"`) between the filter strip and the table wrap when the R4-A `payload.providerErrors` map is non-empty and not dismissed; new `renderRefreshOverlay()` adds/removes the dimmed overlay based on `state.requestStatus`; new `dismissProviderErrors()` handler; `renderRow()` wraps the label in a new `.__label-block` and appends a `.__description` `<p>` (`textContent`, never innerHTML) only when `item.description !== ''`; `close()` resets `state.providerErrorsDismissed` so a fresh open re-surfaces still-throwing providers; `publicState()` exposes `providerErrors` + `providerErrorsDismissed` for test observability); `addons/visual-editor/assets/css/control-center.css` (new `.__notice--provider-error` selector — warning-tone banner sitting above the table wrap; new `.__label-block` + `.__description` selectors — muted second line with 2-line `-webkit-line-clamp`); `addons/visual-editor/src/Assets/AssetLoader.php` (widened `controlCenterSearchPlaceholder` to "Search labels, descriptions, owner…" matching the R4-A `q` semantics; 4 new i18n keys — `controlCenterRefreshing`, `controlCenterProviderErrorSingular`, `controlCenterProviderErrorPlural`, `controlCenterProviderErrorDismiss`); `tests/visual-editor-brand-control-center-state.test.cjs` (+7 R4-C-1a jsdom cases + a `sleep()` + `extractQuery()` helpers block: debounced 250ms GET with `?q=`; family chip fires immediate GET with `?family=`; priority chip stays client-side; description renders/omits per record; provider-errors banner renders with count + names; banner dismissal per-drawer-lifetime + reopen re-surfaces; `priorityFromItem` matrix across `vertical_[1\|2\|3\|9]_*` + `shared_*`); `docs/dropins/dbvc-visual-editor-brand-controls-guide/releases/R4-EXPANDED-GLOBAL-BRAND-CONTROL-CENTER.md` (§R4-C rewrite to R4-C-1a/1b/2 sub-slice split + pinned in-slice decisions); `addons/visual-editor/docs/handoffs/DBVC_R4C_RESUME_PROMPT.md` (in-scope section flipped from "proposed / confirm with user" to "confirmed 2026-08-31 as three sub-slices") | R4-C-1a — production drawer consumes the R4-A backend contract. **Mixed filter model** (D-064-a): `family` + `q` are server-side (matches R4-A's list-route params); `status` + `priority` + `category` stay client-side (keeps tab counts cheap, avoids a second unfiltered request). **Priority derivation** (D-064-b): reads the `sortKey` prefix — the pre-R4-A `item.priority` / `item.meta.priority` fallback never populated against real R4-A data. **Fail-soft provider errors**: partial list keeps rendering under a warning-tone banner (the R4-A `providerErrors` map exposes the throwing providers by id; end-user never sees a message string from the provider — banner text is client-authored). **Loading-refresh state**: keeps the current rows visible under the previously-unused `.__refresh-overlay` for any non-initial round-trip (search / family chip / retry-after-partial) so the viewer never loses scan context. **Zero backend changes** — every gate the R3-D two-part kill switch enforces still applies. Single polite live region rule preserved (banner is a persistent visual affordance, not an announcement; the announcer remains authoritative). Forbidden `data-*` invariant untouched (rows still carry only `data-public-id`). **Validation**: `node --test tests/visual-editor-brand-control-center-state.test.cjs` → **21 pass / 21 tests** (was 14/14 pre-R4-C-1a — 7 new cases). `node --test tests/visual-editor-media-manager-state.test.cjs` → **42 pass** (baseline preserved). `vendor/bin/phpunit --filter "VisualEditor(Control\|SharedGlobals)"` → **60 tests / 200 assertions OK** (unchanged from R4-A landing — R4-C-1a touched no backend). `composer agent-docs:refresh && composer agent-docs:check` → **54 curated / 443 discovered / 0 unmapped** (unchanged — AssetLoader i18n additions do not rotate hook or REST hashes). No content mutated; no persistent WP option toggled outside test setUp/tearDown; no git operations either repo | R4-C-1a delivers the smallest contract-consumer slice: the drawer now sends the R4-A `family` + `q` params server-side, renders the R4-A `description` line, surfaces the R4-A `providerErrors` map as a subtle banner, and derives priority from the R4-A `sortKey` prefix. R4-C-1b (batch value-summary loader) can proceed against the same untouched backend; the R4-A `POST .../value-summaries` batch endpoint is the single new API affordance it will consume. Mockup `states.html` coverage advances (loading-refresh dimmed overlay, provider-error banner state); R4-C-2 will translate the remaining state-gallery cells + wire the view-mode toggle + collapsible group headers | R4/R4-C-1a |
| E-100 | 2026-08-30 | DBVC + Vertical | `addons/visual-editor/src/Registry/ControlRecord.php` (+`description`, +`sortKey` public properties, constructor + `fromArray` + `toListItem` all round-trip the pair); `addons/visual-editor/src/Registry/ControlProvider.php` (interface widened with `buildValueSummary(ControlRecord, sessionId): ?array`); `addons/visual-editor/src/Registry/ControlRegistry.php` (`listControls()` accepts new `family` + `q` args; sort now `sortKey ASC → label ASC → publicId ASC` globally rather than per-provider; try/catch on `getControls()` with new `REJECT_PROVIDER_THREW` observer reason + `getProviderErrors()` accessor; new `buildValueSummaryForRecord()` delegator); `addons/visual-editor/src/Registry/Providers/SharedGlobalsControlProvider.php` (emits `description` via `dbvc_visual_editor_control_center_description` filter → ACF `instructions` fallback → empty; emits `sortKey` `shared_{fieldName}`; real `buildValueSummary()` for relationship/post_object returning `{family,count,firstTitles≤3,hasMore}` with option-owned capability recheck; new 4th optional constructor seam `$optionValueResolver`); `addons/visual-editor/src/Rest/Controllers/ControlCenterListController.php` (`viewModelVersion` 1→2, `query` echoes all four params, new `providerErrors` field, `q` clamped at 128 chars); `addons/visual-editor/src/Rest/Controllers/ControlCenterValueSummariesController.php` (new — `POST .../control-center/value-summaries` body `{publicIds}` batch cap 50, per-record capability recheck via descriptor → summary or null); `addons/visual-editor/src/Rest/Routes.php` (wires the new controller under the R3-D two-part kill switch); `addons/visual-editor/src/Bootstrap/Addon.php` (wires the production `optionValueResolver` closure that calls `get_field($name, 'option', false)`); `tests/phpunit/VisualEditorControlCenterR4ATest.php` (new, 19 tests / 60 assertions across all R4-A surfaces); `tests/phpunit/VisualEditorControlCenterRoutesTest.php` (routes test updated for `viewModelVersion=2` + four-key query echo + `providerErrors`); `tests/phpunit/VisualEditorControlRegistryTest.php`, `tests/phpunit/VisualEditorControlCenterProvidersFilterTest.php`, `tests/phpunit/VisualEditorControlCenterRoutesTest.php` (three anon-class provider fixtures gain `buildValueSummary` stubs); Vertical repo `functions/features/dbvc-visual-editor/includes/class-vf-vertical-control-provider.php` (emits `description` via `vf_field_context_get_entry_primary_purpose()` → curation `notes` → empty; emits `sortKey` `vertical_{1\|2\|3\|9}_{fieldName}` mapped from `client_priority`; `buildValueSummary` remains `null`); Vertical repo `functions/features/dbvc-visual-editor/dbvc-visual-editor.php` (new `vf_dbvc_visual_editor_shared_globals_description` filter callback wired to `dbvc_visual_editor_control_center_description` so Shared Globals rows get Field Context primary-purpose text) | R4-A — expanded Brand Control Center backend. Backend contract is complete; frontend (R4-C) will consume `viewModelVersion=2`, the new `family` + `q` params, the `providerErrors` banner data, and the batch value-summaries endpoint. Every R4 mockup DESIGN-DECISIONS pin is honored: (§4) description as muted second line drawn from provider-owned filter; (§5) value summary is an opaque per-family read-model with per-record capability recheck (relationship shape is `{family,count,firstTitles≤3,hasMore}`); (§6) provider errors surface as a subtle banner without shielding a partial list; (§7) sortKey wins over label alpha with deterministic publicId tiebreaker. **Validation**: `vendor/bin/phpunit --filter "VisualEditorControlCenterR4A"` → **19 tests / 60 assertions OK**. `vendor/bin/phpunit --filter "VisualEditor(Control\|SharedGlobals)"` → **41 tests / 140 assertions OK** (R3-A/B/C-1/D + R3-B SharedGlobals + R4-A all together). Full PHPUnit **888 tests, 7 failures** (was 869/7 pre-R4-A — same 7 pre-existing failures across unrelated Bricks / Content Collector / Content Migration / Proposal Diff / Capability Landscape suites; the 19 delta matches the new R4-A suite). No content mutated; no persistent WP option toggled outside test setUp/tearDown; no git operations either repo | R4 backend contract is stable and covered. Frontend integration (R4-C) can proceed against a fixed set of API affordances: `?family=&q=`, `viewModelVersion=2`, `providerErrors` map, and the batch value-summaries endpoint. Cross-repo: Vertical's Field Context primary-purpose now feeds into DBVC Shared Globals row descriptions with zero DBVC changes needed on Vertical's side — the filter contract is the seam | R4/R4-A |
| E-099 | 2026-08-29 | DBVC | `docs/dropins/dbvc-visual-editor-brand-controls-guide/qa/R3-DRAWER-BROWSER-QA-REPORT.md` (new); `releases/R3-REGISTRY-BACKED-BRAND-CONTROL-CENTER.md` §R3-D residual bullet flipped to CLOSED; `releases/BRAND-CONTROL-CENTER-RELEASE-NOTES-AND-ROLLBACK.md` *Residual / deferred* + *Verification snapshot* updated | R3 residual gate closed via real-browser QA against the live LocalWP site (`https://dbvc-codexchanges.local/`, admin `agentadminuser1`, real Chrome via Claude in Chrome). Tested at both supported viewports (D-058: 1440×900 primary, 1280×720 secondary) against a live 400-row registry (1 `SharedGlobalsControlProvider` + 399 `VerticalControlProvider`). All 12 checklist items pass: (1) drawer opens from toolbar `sliders` slot; (2) `aria-expanded` flips false↔true correctly; (3) 400 rows loaded — provider split `{shared_globals:1, vertical:399}`; (4) tab click drops 400→78 with **0 fetch calls** (confirms pinned decision #3 client-side filtering); (5) chip filter fires with `aria-pressed="true"` and 0 fetch calls; (6) label search debounced + client-side; (7) Escape closes drawer + restores focus to toolbar button (`document.activeElement === btn`); (8) **zero** forbidden `data-*` attributes across all 400 rendered rows (`data-owner-id`, `data-field-key`, `data-selector`, `data-path`, `data-descriptor`, `data-token` — schematic §6 invariant 2); (9) exactly 1 polite live region in the drawer shell; (10) drawer + main-editor-panel coexistence — measured geometry at 1440×900: drawer `[0, 32, 480×615]` + panel `[1044, 8, 380×707]` gap 564px; at 1280×720: drawer `[0, 32, 480×435]` + panel `[892, 8, 380×527]` gap 412px; row marked `is-focused-source`, panel `dataState="ready"`; drawer stays visible after Open click (D-063 coexistence); (11) `@media (prefers-reduced-motion: reduce)` CSS rule confirmed present targeting `.dbvc-ve-control-center`; (12) `SharedGlobalFieldsController::handle` response shape unchanged after R3-C-1 extraction — HTTP 200, canonical top-level keys `[descriptors, descriptorHydrations, fields, ok, warnings]`, first field carries the canonical 13-key inventory shape, hydration `mutationContract.contract = "shared_relationship_collection"` with real data (10 connected Case Studies / Posts / Benefits). **Cross-provider verified live**: the Shared Globals record's `POST /control-center/open` response includes real ACF data with 10 connected items on option page `site-settings`. **Follow-ups** (non-blocking): 1 curated Vertical record dropped in `VF_Vertical_Control_Provider::mapRecord` (400 in JSON → 399 registered — likely a slug collision or empty field_key check; small ticket to instrument `mapRecord()` with a `record_dropped` counter); Claude-in-Chrome isolated-world JS listener did not observe the `dbvc:visual-editor:absorb-descriptor` event, but overlay-app.js's own listener clearly did (panel opened, row marked, descriptor absorbed) — production works, test observability is the artifact. **Out of scope per D-058**: real assistive technology (VoiceOver/JAWS/NVDA), mobile/tablet/touch. **Residual carried at the same shape as Media Manager D-049**: real Safari (Claude in Chrome only drives Chrome; WebKit-in-Chromium is not Safari) | R3 core is fully verified end-to-end at real-browser scale. The one residual gate carried into R4 (`R3-D` release notes' *Residual / deferred*) is now CLOSED. Path A (R4 expanded UI) can proceed without R3 residual concerns | R3/Post-R3 (QA) |
| E-098 | 2026-08-29 | DBVC + Vertical | `addons/visual-editor/src/Bootstrap/Addon.php` (new `dbvc_visual_editor_control_center_providers` filter in `register()` inside the kill-switch gate — external providers returned from the filter register on the runtime `ControlRegistry` after `SharedGlobalsControlProvider`; non-`ControlProvider` entries silently skipped); `addons/visual-editor/bootstrap.php` (new `DBVC_Visual_Editor_Addon::get_curation_export_path()` static helper + `dbvc_visual_editor_curation_export_path` filter; deterministic absolute path to the R3-BX curation JSON computed from bootstrap.php's own directory); `tests/phpunit/VisualEditorControlCenterProvidersFilterTest.php` (new, 7 tests / 11 assertions); `docs/agents/manifest.json` (2 new extension-point discovery ids mapped: `0d92c6ae` for the providers filter, `03b25a10` for the curation-path filter; 3 sibling filter hashes rotated by line shifts from the two additions: excluded_post_types `66a24b36`→`8efdf138`, excluded_taxonomies `8563d9d2`→`3b704999`, shared_global_field_names `037e8a46`→`6d6d3290`); Vertical repo `functions/features/dbvc-visual-editor/vertical-control-provider.php` (new, VerticalControlProvider implements `Dbvc\VisualEditor\Registry\ControlProvider`, reads the curation JSON via `DBVC_Visual_Editor_Addon::get_curation_export_path()`, emits 400 records all with `status="unsupported"`, `buildDescriptor` returns null); Vertical repo hook that registers the provider via the new DBVC filter | **Cross-repo VerticalControlProvider slice.** DBVC side: a post-R3 extension point + curation-path helper give external providers a documented, kill-switch-respecting way to register on the runtime `ControlRegistry` without touching addon source. The filter is inert whenever the Brand Control Center is disabled (both parts required per D-063). The extension point inherits the same discovery-only contract: external providers' `buildDescriptor()` may return null (drawer renders `status="unsupported"` rows with no Open button and never calls the open route), and every mutation still routes through the shared `MutationService` pipeline. Vertical side: consumes the filter to project the 400 R3-BX-curated Vertical records onto the R3-A registry — every row is `status="unsupported"` in this MVP because the R5.1/R5.2/R5.2+color_picker/R5.3 descriptor factories that would flip them to `available` are R5-slice work; only the 2 R5.4 records are family-eligible today (and they may be nested groups the current `SharedGlobalsDescriptorFactory` doesn't handle either). **Validation**: `vendor/bin/phpunit --filter "VisualEditorControlCenterProvidersFilter"` → **7 tests / 11 assertions OK**. Full PHP suite unchanged in identity — R3-A / R3-B / R3-C-1 / R3-C-2 / R3-D subsets all preserved. Drawer + Media Manager jsdom baselines preserved (14/42). Agent docs `54 curated / 441 discovered / 0 unmapped` (up 2 from R3-D's 439 — the two new filter surfaces). No content mutated; no persistent WP option toggled outside setUp/tearDown; no git operations either repo | The drawer now populates with 400 real Vertical records across 7 curation categories, exercising the R3-C-2 drawer's category tabs / chip filters / label search at realistic density — the R3 "registry membership never grants edit permission" principle is proven end-to-end at scale. Every row is a roadmap marker: as R5.1 / R5.2 / R5.2b / R5.3 land descriptor factories for their families, VerticalControlProvider flips those records from `unsupported` to `available` with no drawer-side change. The DBVC filter is stable, documented, and reusable by any other theme that wants to register controls on the same registry | R3/Post-R3 (VerticalControlProvider) |
| E-097 | 2026-08-29 | DBVC | `tests/phpunit/VisualEditorControlCenterHardeningTest.php` (new, 7 tests / 18 assertions covering the two invariants R3-D exists to lock down: routes-registration axis of the kill switch, and Bricks Builder isolation for drawer assets); `docs/dropins/dbvc-visual-editor-brand-controls-guide/releases/BRAND-CONTROL-CENTER-RELEASE-NOTES-AND-ROLLBACK.md` (new, sibling to `MEDIA-MANAGER-RELEASE-NOTES-AND-ROLLBACK.md`) | R3-D — production hardening + release notes. **Kill-switch registration coverage** (5 tests): `is_control_center_enabled` requires both parts; both routes register only when both parts are on; both routes absent when the feature switch is off; both routes absent when the master switch is off; mid-session flip from on→off makes both routes unreachable on the next REST-init dispatch (test resets `$wp_rest_server` between passes to force a clean re-init since the REST server caches its routes across tests). **Bricks Builder isolation coverage** (2 tests): with `$_GET['bricks']='1'`, `AssetLoader::enqueue` short-circuits via `FrontendRuntimeGuard::shouldRunFrontend()` → drawer stylesheet + script both stay unenqueued (also asserts media-manager assets stay off, proving both feature branches sit behind the same guard). Also asserts the feature-switch-off enqueue-inhibit path with the edit-mode cookie forced on to isolate the switch gate from the mode-active gate. **Release notes + rollback runbook** mirrors the Media Manager sibling section-for-section: *What shipped* (R3-A / R3-B / R3-C-1 / R3-C-2 / R3-D), *Feature gates & isolation (verified)* with every gate cross-referenced to the exact test that proves it, *Side effects & boundaries* (writes: none from R3 itself; storage: none beyond `OPTION_CONTROL_CENTER_ENABLED`; frontend enqueue footprint: one CSS + one JS + one toolbar button, all Bricks-isolated), *Residual / deferred* (real-browser QA of drawer + panel coexistence at 1440×900 and 1280×720, marked as a residual gate along the Media Manager D-049 shape — jsdom already asserts DOM shape + state machine + security invariants headless; PHPUnit locks the kill switch + Bricks exclusion; the residual is only real-browser fidelity of the coexistence pattern), *Rollback runbook* (6 numbered steps + pre-flip verification recipe: curl the popover route before and after, curl the two Control Center routes to confirm 404 after flip, visit a frontend page to confirm the toolbar slot is absent), *Verification snapshot*. **No new REST routes; no descriptor changes; no drawer state additions; no new persistent option.** Kill switch stays default off. **Validation**: `vendor/bin/phpunit --filter "VisualEditorControlCenterHardening"` → 7 tests / 18 assertions OK. Full PHP suite **861 tests, 7 failures** (was 854/7 pre-R3-D — same 6 inherited + 1 pre-existing dirty-tree `ProposalDiffContractTest`). R3-A / R3-B / R3-C-1 / R3-BX / Media Manager subsets all unchanged. Drawer jsdom 14 pass; Media Manager jsdom 42 pass. Agent docs 54/439/0 (no new discoveries — R3-D added no REST or hook surface). No content mutated; no persistent WP option toggled outside test setUp/tearDown; no git operations | **R3 core is now ship-ready.** Every gate the release notes + rollback runbook promises has a concrete test proving it: the two-part kill switch prevents route registration in either half-off state (5 tests); Bricks Builder never sees the drawer (2 tests); every existing R3-C-1 behavior-level guard (unauthenticated, subscriber, foreign session, malformed publicId, provider-null, empty reference_post_types) already had coverage in `VisualEditorControlCenterRoutesTest` (10 tests); the descriptor factory extraction preserved popover behavior (already asserted in R3-C-1); the drawer's DOM/state/security invariants are asserted headless in `visual-editor-brand-control-center-state.test.cjs` (14 tests). The only residual gate is real-browser QA of the drawer + panel coexistence — deferred as an R3-D residual along the Media Manager D-049 shape, not a blocker on R3 core ship. Remaining follow-ons (R4 expanded UI, cross-repo VerticalControlProvider) are separate slices with their own gates | R3/R3-D |
| E-096 | 2026-08-29 | DBVC | `addons/visual-editor/assets/js/brand-control-center-app.js` (new, ~900 lines; IIFE mirroring `media-manager-app.js`'s lifecycle shape); `addons/visual-editor/assets/css/control-center.css` (new; direct translation of the mockup's production selectors, drops `.dbvc-ve-control-center-mockup__*` scaffolding, uses existing `--dbvc-ve-*` tokens, declares component-local `--dbvc-ve-control-center-*` sizing on `.dbvc-ve-control-center` itself); `addons/visual-editor/assets/css/overlay.css` (one new `:root` token: `--dbvc-ve-z-drawer: 120015`, between panel 120010 and toolbar 120020); `addons/visual-editor/assets/js/overlay-app.js` (`isControlCenterEnabled()` + `dispatchControlCenterEvent()` helpers next to their media-manager twins; new dock toolbar entry after `shared-globals` using the already-registered `sliders` icon; `handleToolbarClick` branch for `action === 'control-center'` with mutual-exclusion vs media-manager; new `bindControlCenterBridge()` called from `mount()` that attaches ONE document listener for `dbvc:visual-editor:absorb-descriptor` and reuses the existing internal `mergeSharedGlobalInventory` + `openToolbarDescriptorPanel` helpers so the panel opens identically from the drawer and from the Shared Globals popover); `addons/visual-editor/src/Assets/AssetLoader.php` (`isControlCenterEnabled()` method; new enqueue branch for `control-center.css` + `brand-control-center-app.js` both depending on `dbvc-visual-editor-overlay`, filemtime-versioned; new `controlCenter: { enabled, restBase }` block in the `wp_localize_script` payload; ~40 new `controlCenter*` i18n keys); `tests/visual-editor-brand-control-center-state.test.cjs` (new jsdom suite, 14 tests); `docs/agents/manifest.json` (one extension-point hash rotated from `784de0da` → `c8ae9455` on `dbvc_visual_editor_media_index_list_enabled` — `AssetLoader::enqueue` line shifts from the new `controlCenter` block above the `apply_filters` call) | R3-C-2 — production drawer that consumes R3-C-1's REST routes. **Three pinned decisions honored verbatim (2026-08-29)**: (1) overlay↔drawer glue is a document event, not a shared global — drawer dispatches `dbvc:visual-editor:absorb-descriptor` with the R3-C-1 payload `{descriptors, descriptorHydrations, token, publicId}` and overlay-app.js's bridge listener re-uses its internal `mergeSharedGlobalInventory` + `openToolbarDescriptorPanel` helpers (same helpers the Shared Globals popover already uses, so the panel behaves identically regardless of entry point); (2) toolbar entry lives in the dock right after `shared-globals` with the same `dbvc-ve-toolbar__button--dock` treatment as the media-manager slot, gated by `isControlCenterEnabled()`; (3) filtering is client-side (tab / status / priority / field-family / search) — the drawer's initial `GET /control-center/controls` returns every visible-to-user record and all filter changes are in-memory. Drawer state machine covers the mockup's §4 states: loading-initial / list / opening (per row, `aria-busy="true"`) / opened (drawer + panel coexist via `is-focused-source` modifier) / open-error (inline row-notice with Dismiss; assertive for 409) / empty / empty-filtered / error (with Retry). Rows carry ONLY `data-public-id` — no `data-owner-id` / `data-field-key` / `data-selector` / `data-path` / `data-descriptor` / `data-token` (schematic §6 invariant 2, jsdom-asserted). Row-focus continuity via `activeElement` snapshot. Single polite `role="status"` region. `@media (prefers-reduced-motion: reduce)` suppresses spinner + slide transitions. Kill switch stays default off (D-063); when off, AssetLoader enqueues nothing, `isControlCenterEnabled()` returns false → toolbar entry hidden, bridge listener not bound, drawer JS never boots. **Validation**: `node --test tests/visual-editor-brand-control-center-state.test.cjs` → **14 pass / 14 tests**. `node --test tests/visual-editor-media-manager-state.test.cjs` → **42 pass** (baseline preserved — earlier resume-file claim of 43 was stale; corrected to 42 here). Full PHPUnit **854 tests, 7 failures** (identical to R3-C-1 — same 6 inherited + 1 pre-existing dirty-tree `ProposalDiffContractTest`). Agent docs `composer agent-docs:refresh` then `composer agent-docs:check` → **54 curated / 439 discovered / 0 unmapped** (unchanged discovered count — R3-C-2 added no new REST routes; one extension-point hash rotated by line shifts and was re-mapped). No content mutated, no persistent WP option toggled, no git operations | R3 is now user-visible behind the two-part kill switch: an authorized user with both switches on sees a `sliders` icon in the Visual Editor toolbar dock, opens the drawer, sees the R3-B Shared Globals controls in a filterable table, clicks Open on a row, and the existing main editor panel opens on that control while the drawer stays visible — same save pipeline, same capability checks, same journaling as the popover. The only R3 slice remaining before ship is **R3-D** (production hardening: full REST-forgery / stale-nonce / Bricks-Builder-exclusion fail-closed test coverage, browser QA of the drawer + panel coexistence, release-notes + rollback runbook mirroring `MEDIA-MANAGER-RELEASE-NOTES-AND-ROLLBACK.md`) | R3/R3-C-2 |
| E-095 | 2026-08-28 | DBVC | `addons/visual-editor/src/Registry/Providers/SharedGlobalsDescriptorFactory.php` (new; stateless — owns `build($sessionId, $pageContext, $field)` + every private helper participating in the descriptor build: `resolveFieldGroupContext`, `resolveFieldGroupKey`, `extractFieldGroupOptionPages`, `resolveOptionPageLabel`, `filterPostTypes`, `resolveFieldPostTypes`, `getDefaultReferencePostTypes`, `normalizeReferenceIds`, `normalizePostTypes`, `resolveReferenceMin`, `resolveReferenceMax`, `createToolbarToken`); `addons/visual-editor/src/Rest/Controllers/SharedGlobalFieldsController.php` (delegates to factory; every extracted private helper removed except `canManageSharedGlobalOptions`, `getConfiguredFieldNames`, `buildFieldInventoryItem` which stay controller-scoped; associative-array key ordering in the extracted `build()` matches the pre-extraction inline emit exactly so JSON output is byte-identical); `addons/visual-editor/src/Registry/ControlProvider.php` (interface widened with `buildDescriptor(ControlRecord, sessionId, pageContext): ?EditableDescriptor`); `addons/visual-editor/src/Registry/Providers/SharedGlobalsControlProvider.php` (implements new method by re-resolving via the field-object seam, re-validating type ∈ {relationship, post_object}, delegating to the factory); `addons/visual-editor/src/Registry/ControlRegistry.php` (new `buildDescriptorForRecord()` looks the provider up in the private map and delegates); `addons/visual-editor/src/Rest/Controllers/ControlCenterListController.php` (new; `GET /dbvc/v1/visual-editor/session/{session_id}/control-center/controls?category=&status=`, returns registry safe projection); `addons/visual-editor/src/Rest/Controllers/ControlCenterOpenController.php` (new; `POST .../control-center/open`, resolves publicId → provider.buildDescriptor → capability recheck → session attach → payload); `addons/visual-editor/src/Rest/Routes.php` (`ControlRegistry` param added; both new controllers registered under `is_control_center_enabled()` gate); `addons/visual-editor/src/Bootstrap/Addon.php` (passes `$this->control_registry` into `Routes` constructor); `tests/phpunit/VisualEditorControlCenterRoutesTest.php` (new; 10 tests / 36 assertions); `tests/phpunit/VisualEditorControlRegistryTest.php` (anon-class `makeProvider` factory gained trivial `return null;` for the new interface method); `docs/agents/manifest.json` (1 REST hash rotated on `SharedGlobalFieldsController` from `02228c99c1991532` → `4c1cbc65dd5b2863`; 1 extension-point hash rotated on `ControlRegistry::observeInvalid` from `2129b02e` → `3a806cb6` due to line shifts from the new registry method; 2 new REST discovery ids mapped: `9ce38d48fbea6244` list, `2c6592da113e4d85` open) | R3-C-1 — Brand Control Center backend and open route. **Extraction** is a lift, not a redesign: descriptor construction was moved out of the popover controller into a stateless factory the new open route also uses, with byte-identical output (a focused extraction-sanity test asserts key descriptor fields for a fixture ACF field). **Interface extension** was chosen over a parallel factory registration surface (rationale captured in the resume file's R3-C-1 "Provider descriptor-factory seam" section; not promoted to its own DECISION-LOG row because it does not go beyond D-063's gate — the kill switch and popover-coexistence policy remain authoritative) — one line per anon class in existing tests, one call site downstream, no synchronization surface between providers and factories. **Two new REST controllers** are session-scoped (matching the Shared Globals popover route's scoping) so the open route's minted descriptor can attach to the same session; both fail closed on the standard axes (edit-mode inactive, session expired, unknown/malformed publicId, provider returns null, `source.reference_post_types` empty, capability denied at open time, session-attach fails). **Kill switch gates the controller registrations only**, not the `ControlRegistry` instantiation (which stays unconditional so R3-B's provider is registered when its own kill-switch branch runs). **No frontend JS/CSS** in this slice — the drawer is R3-C-2. **Validation:** `vendor/bin/phpunit --filter "VisualEditorControlCenterRoutes"` → 10 tests / 36 assertions OK. `--filter "VisualEditorControlRegistry"` unchanged (11/44). `--filter "VisualEditorSharedGlobalsControlProvider"` unchanged (5/28). `--filter "VisualEditorCuration"` unchanged (29/96). `--filter "VisualEditorMedia"` unchanged (115/1962). Full PHP suite **854 tests, 7 failures** (was 844/7 pre-R3-C-1 — identical 6 inherited + 1 pre-existing dirty-tree `ProposalDiffContractTest`). Agent docs `composer agent-docs:refresh` then `composer agent-docs:check` → **54 curated / 439 discovered / 0 unmapped** (up 2 discovered surfaces from R3-B's 437, one for each new REST route). No content mutated; no persistent WP option toggled (only the `dbvc_visual_editor_mode` request cookie set in tests, unset in tear_down); no git operations | R3-C-1 delivers the REST contract R3-C-2 will wire the drawer against, and the extraction gives R3-C-2 exactly one place to instantiate the descriptor factory. The kill switch stays operationally inert (list route + open route are registered but no UI reaches them) until R3-C-2's `assets/js/brand-control-center-app.js` + toolbar wiring land. Popover behavior is guarded by the extraction-sanity test + the descriptor-shape assertions in the new controller tests | R3/R3-C-1 |
| E-094 | 2026-08-28 | DBVC | `addons/visual-editor/src/Registry/Providers/SharedGlobalsControlProvider.php` (new); `addons/visual-editor/bootstrap.php` (`OPTION_CONTROL_CENTER_ENABLED` constant, `is_control_center_enabled()` helper, `ensure_defaults`/`get_all_settings`/`save_settings`/`get_settings_groups`/`get_field_meta` wiring; `SETTINGS_VERSION` 5→6); `addons/visual-editor/src/Bootstrap/Addon.php` (unconditional `ControlRegistry` instantiation, kill-switch-gated `SharedGlobalsControlProvider` registration in `register()`, new `getControlRegistry()` getter, `$this->capabilities` stored for R3-C reuse); `tests/phpunit/VisualEditorSharedGlobalsControlProviderTest.php` (new, 5 tests / 28 assertions); `tests/phpunit/VisualEditorMediaManagerR1ATest.php` (`SETTINGS_VERSION` assertion `5`→`6` with R3-B comment); `docs/agents/manifest.json` (three extension-point hashes rotated by bootstrap.php line shifts: `dbvc_visual_editor_excluded_post_types` → `66a24b36`, `dbvc_visual_editor_excluded_taxonomies` → `8563d9d2`, `dbvc_visual_editor_shared_global_field_names` → `037e8a46`) | R3-B — headless Shared Globals compatibility provider. Adapts the existing `SharedGlobalFieldsController` field-enumeration path onto the R3-A `ControlRegistry` as a **discovery-only** surface: one `ControlRecord` per configured Shared Globals field whose ACF type is `relationship` or `post_object`, `category=globals`, `ownerType=option`, `ownerSubtype=acf_options`, opaque `source={field_name, field_key}` hint (for R3-C's descriptor factory), `meta.badge = "Shared Global"`, and a `visibleTo` closure that reproduces `SharedGlobalFieldsController::canManageSharedGlobalOptions` verbatim (same option-owned probe descriptor, same `canEditDescriptor()` call). Provider constructor accepts `CapabilityManager` plus two callable seams (configured-names resolver + field-object resolver) so tests inject fixtures without loading ACF. Provider is registered under a two-part kill switch (master Visual Editor switch AND the new `OPTION_CONTROL_CENTER_ENABLED`, both default off) — mirrors `is_media_manager_enabled()`. `Bootstrap\Addon::__construct` instantiates `ControlRegistry` unconditionally (empty registry is safe — matches how `EditableRegistry` is wired); `register()` gates only the provider registration. `SharedGlobalFieldsController` stays intact — the existing Shared Globals popover keeps working exactly as it does today; R3-B is parallel discovery, not a replacement. New `getControlRegistry()` getter exposes the same instance for R3-C's REST controllers. **Validation**: `vendor/bin/phpunit --filter "VisualEditorSharedGlobalsControlProvider"` → 5/28 OK. `--filter "VisualEditorControlRegistry"` unchanged (11/44). `--filter "VisualEditorCuration"` unchanged (29/96). `--filter "VisualEditorMedia"` unchanged (115/1962). Full PHP suite **844 tests, 7 failures** (was 839/7 — identical 6 inherited + 1 pre-existing dirty-tree `ProposalDiffContractTest`). Agent docs `composer agent-docs:refresh` then `composer agent-docs:check` → **54 curated / 437 discovered / 0 unmapped** (three extension-point hashes rotated by bootstrap.php line shifts, re-mapped in `manifest.json`). No content mutated; no live WP option toggled outside intentional new defaults; no git operations | R3-B's discovery surface is live behind the kill switch; R3-C can now bind REST controllers to `Addon::getControlRegistry()` and wire its descriptor factory to the opaque `source` hint carried on each `ControlRecord`. The existing Shared Globals popover continues to work independently — the two paths coexist deliberately | R3/R3-B |
| E-001 | 2026-08-14 | DBVC | historical pre-commit branch `codex/visual-editor-linked-posts-plan`; HEAD `78a06ad`; `git status`/diff stats | Initial R0 began with protected dirty Visual Editor and unrelated work; those changes were later committed by the user | Historical dirty-boundary evidence; superseded for current authority by E-028 | R0/All |
| E-002 | 2026-08-14 | DBVC | `addons/visual-editor/src/Bootstrap/Addon.php`; `Addon::register()` | One composition root wires runtime guard, contexts, descriptor registry, resolvers, mutation, journal, cache, assets, hooks, and routes | Add R1 services through current composition root | R1 |
| E-003 | 2026-08-14 | DBVC | `addons/visual-editor/src/Registry/EditableRegistry.php` | Opaque public IDs map to private compressed descriptors in user-bound transient sessions with 8-hour default TTL and keepalive | Reuse conventions, not the store; scan snapshot requires separate blog/user/generation/paging lifecycle | R1/R2 |
| E-004 | 2026-08-14 | DBVC | `PageContextResolver`; `CapabilityManager`; `FrontendRuntimeGuard` | Current Visual Editor knows singular/archive/term contexts, owner-specific capabilities, and builder/frontend exclusions | Reuse context/permission/guard policy | R1 |
| E-005 | 2026-08-14 | DBVC | `addons/visual-editor/src/Rest/Controllers/ObjectSearchController.php` | Navigation search uses public/show-UI post types/taxonomies, configured exclusions, per-object capability, and a capped result query | Reuse policy conventions; do not repurpose capped navigation endpoint as scanner | R1 |
| E-006 | 2026-08-14 | DBVC | `addons/visual-editor/src/Resolvers/PostFeaturedImageResolver.php` | Canonical read is thumbnail ID; writes use set/delete post thumbnail; local image attachment validation exists | R2 can reuse resolver after fresh descriptor; R1 reuses read semantics | R1/R2 |
| E-007 | 2026-08-14 | DBVC | `AcfImageResolver.php`; `AcfGalleryResolver.php`; `AbstractAcfResolver.php` | Raw ACF reads, local image normalization, ordered gallery IDs, `update_field()`, post/term/user/option owners, and render-proven group/repeater/flexible existing rows exist | Media mutation primitives exist; generic off-page path discovery does not | R1/R2 |
| E-008 | 2026-08-14 | DBVC | `addons/visual-editor/src/Save/MutationService.php`; `CompositeSaveController.php` | Single mutation rereads old value but has no expected-old request; batch path has expected-value preflight/compensation for composite contracts | Add narrow expected-empty gate in R2; defer Save Row | R2 |
| E-009 | 2026-08-14 | DBVC | `ChangeJournalRecorder`; `ChangeJournalStore`; `CacheInvalidator`; DBVC activity/sync logger calls | Existing mutation flow journals before/after, audits, and invalidates post/term/user/option caches | No parallel mutation/audit/cache system | R2 |
| E-010 | 2026-08-14 | DBVC | `addons/visual-editor/src/Rest/Routes.php` | Routes exist for session/touch, descriptors, search, Shared Globals, seed, composite save, and save; no scan/progress/results/cancel endpoint | New cohesive Media Manager controller/routes required | R1 |
| E-011 | 2026-08-14 | DBVC | `addons/visual-editor/src/Assets/AssetLoader.php`; `AssetLoader::enqueue()` | Active Visual Editor eagerly calls both `wp_enqueue_editor()` and `wp_enqueue_media()` | R1 adds no new enqueue; package scan-only asset assumption corrected | R1 |
| E-012 | 2026-08-14 | DBVC | `assets/js/overlay-app.js`; `assets/css/overlay.css` | Toolbar/popovers, field-index search/scroll preservation, panel/focus/layering/status, and Media Library frame patterns exist; no large data-grid/virtualization component exists | Extend current vanilla frontend shell with scoped Media Manager table | R1/R2 |
| E-013 | 2026-08-14 | DBVC | `SharedGlobalFieldsController`; `DBVC_Visual_Editor_Addon::get_shared_global_field_names()` | Field-name allowlist defaults to `settings_globals_default_posts`; controller accepts only option-owned relationship/post_object and manually attaches descriptors with owner `option`/`acf_options` | Off-render precedent is narrow and relationship-specific, not a general registry/factory | R2/R3/R5 |
| E-014 | 2026-08-14 | DBVC | `addons/content-migration/mapping-catalog/dbvc-cc-target-field-catalog-service.php` | Recursive domain artifact/catalog includes broad meta/media heuristics and does not provide exact object-specific nested-media support | Do not use as R1 runtime catalog; reuse traversal lessons only | R1 |
| E-015 | 2026-08-14 | DBVC | `includes/Dbvc/AiPackage/AcfDiscoveryService.php` | Normalizes runtime/local JSON groups, nested fields, clones, layouts, return formats, and basic locations for AI packages | Possible later pure-normalizer extraction; not exact per-entity applicability | R1 |
| E-016 | 2026-08-14 | DBVC | `tests/phpunit/VisualEditorElementInstrumentationTest.php`; `scripts/check-ve-flex-image.php`; `scripts/check-ve-gallery-save.php`; VE QA docs | Focused PHP coverage is small/untracked; media scripts/manual probes exist; no dedicated JS/browser harness | Add focused policy/catalog/snapshot/REST tests; do not run mutating probes in R0/R1 baseline | R0/R1/R2 |
| E-017 | 2026-08-14 | Vertical | `admin/class-vf-tools-media-alt-ai.php`; `vf_media_health_state` | Scans attachments for size, alt, orphan parent, and backup usage; persists option state | Not a missing owner-field scanner | R1 |
| E-018 | 2026-08-14 | Vertical | `admin/media-unused-cleanup/class-vf-media-unused-cleanup-scanner.php`; repository | Attachment-first bounded dry run scans references in posts/postmeta/termmeta/options and rechecks selections before destructive work | Reuse stale reinspection/bounded-job ideas only; no code dependency | R1/R2 |
| E-019 | 2026-08-14 | Vertical | `admin/media-inventory/class-vf-media-inventory-scanner.php`; repository/installer | Bounded filesystem directory/file steps and blog-bound custom-table jobs | Useful progress precedent, wrong data/source contract | R1 |
| E-020 | 2026-08-14 | Vertical | `acf-json/field-groups/*.json` | 48 source JSON groups; explicit traversal found 113 image/16 gallery fields: 5 top-level, 105 group-only, 19 repeater; 5 conditional | Strong fixture/catalog evidence, not runtime authority; clone/runtime expansion changes counts | R0/R1 |
| E-021 | 2026-08-14 | Vertical | `functions/plugins/acf/field-context/class-vf-field-context-resolver.php`; catalog; `vf_field_context_get_entry_for_runtime_field()` | Semantic catalog exposes canonical key/name paths, clone/ancestor context, status, and caches; group query criteria do not replace exact location visibility | Reuse as fixture terminology/evidence only; DBVC must remain independent | R1/R5 |
| E-022 | 2026-08-14 | Vertical | `config/context-broker/acf-field-group-manifest-registry.json` | Schema `vf-acf-field-group-agent-context-registry.v1`, source `frameworkflo-live.local`, 23 curated groups | Curated manifest is not exhaustive runtime inventory | R0/R1 |
| E-023 | 2026-08-14 | Vertical | `functions/plugins/acf/acf.php`; `acf_custom_options_setup()` | Registers Site Settings plus capability-varied subpages; nine ACF JSON files with media definitions target option pages | Later Brand Control Center evidence; excluded from Media Manager R1 | R3-R5 |
| E-024 | 2026-08-14 | Vertical | exact search for `dbvc_visual_editor`, `DBVC_Visual_Editor`, `dbvc-ve` outside docs/artifacts | Only a backup export contains DBVC settings; no executable current integration hook found | DBVC ships independently; backup is not authority | R1 |
| E-025 | 2026-08-14 | LocalWP runtime | read-only `/private/tmp/dbvc_media_manager_r0_probe.php` against site ID `4gScrLykQ` | Exact runtime: 220 eligible published posts/CPTs, 234 public terms, 101 empty featured assignments, 48 active groups, 149 image/22 gallery runtime definitions, 2,119 applicable pairs, 612.62 ms enumeration/visibility baseline, no writes | Sizes bounded scan/read model; does not prove raw empty-scan/REST/UI performance | R0/R1 |
| E-026 | 2026-08-14 | ACF runtime source | `advanced-custom-fields`/bundled ACF `acf_get_field_group_visibility()` and `acf_match_location_rule()` | Visibility evaluates active groups and every registered location rule against a supplied screen; custom theme rules participate through filters | Use as exact applicability primitive with runtime definitions and explicit screen args | R1 |
| E-027 | 2026-08-14 | DBVC/package validation | focused PHPUnit; `PACKAGE-CONTENTS.sha256`; manifest validator | Visual Editor instrumentation: 7 tests/15 assertions passed; every checksum passed; 45 manifest entries had zero byte/hash mismatches | R0 documentation is internally consistent; this is not Media Manager feature coverage | R0 |
| E-028 | 2026-08-14 | DBVC | branch `codex/visual-editor-linked-posts-plan`; commit `5db4b4094c0d834b3cf482adb095273387b59dc8`; Git status/divergence | Checkout was clean; HEAD and origin matched with zero divergence when repository reconciliation began | Current package/canonical planning checkpoint; recheck for newer work before implementation | R0/All |
| E-029 | 2026-08-14 | DBVC validation baseline | user-supplied full-suite/lint result; focused R0 PHPUnit result | Full PHP suite has 6 deterministic failures out of 684 tests; full JavaScript lint did not complete; focused Visual Editor instrumentation passed 7 tests/15 assertions | Do not claim a green repository baseline; future slices must use focused checks and distinguish regressions | R0/All |
| E-030 | 2026-08-15 | DBVC | `addons/visual-editor/src/MediaManager/`; `bootstrap.php`; `CapabilityManager`; `tests/phpunit/VisualEditorMediaManagerR1ATest.php` | R1-A adds a default-off setting, published/public post and public-term owner policy with per-object capability/exclusion checks, exact ACF group visibility, request-local definition caching, unconditional direct/group-only media cataloging, unsupported-path counts, and invalid-nonempty value classification. Focused suite passed 5 tests/65 assertions; combined Visual Editor focus passed 12 tests/80 assertions; touched PHP syntax passed. | Policy/catalog gate is implemented without scanner, REST, UI, descriptor, or mutation authority; R1-B remains separately gated | R1 |
| E-031 | 2026-08-15 | DBVC | `vendor/bin/phpunit` | Full comparison ran 689 tests/7,186 assertions with six failures: `BricksAddonPhase11Test::test_i18n_strings_are_rendered_for_phase11_additions`, `BricksAddonPhase7Test::test_disabled_mode_regression_suppresses_submenu_routes_and_jobs`, `CapabilityLandscapeTest::test_prepared_records_identify_cli_and_parity_opportunities`, `ContentCollectorV2Phase29Test::test_phase_twenty_nine_resolved_conflicts_no_longer_block_package_preflight`, `ContentCollectorV2Phase32Test::test_phase_thirty_two_url_qa_reports_field_context_provider_risk_and_reviewed_ambiguity`, and `ContentMigrationPhase37W0SettingsTest::test_phase37_feature_flags_are_registered_with_safe_defaults`. These match the inherited six identities. | R1-A introduced no new PHP failure; the suite is still not green | R1/All |
| E-032 | 2026-08-15 | DBVC | `ScanCandidateProvider`; `MediaScanService`; `ScanSnapshotStore`; `MediaScanCoordinator`; `VisualEditorMediaManagerR1BTest` | R1-B implements bounded post/term source pages, supported raw-value reads, deterministic opaque findings and empty fingerprints, compressed user/blog-bound transients, latest generation, revision/idempotency, atomic short locks, expiry/cancel/retry/config invalidation, safe payload ceilings, summary counts, and performance metrics. Focused R1-B passed 5 tests/106 assertions; R1-A/R1-B passed 10/171; combined Visual Editor focus passed 17/186. | Scanner/snapshot gate is implemented without REST, public read model, row hydration, descriptor, UI, or mutation authority; R1-C remains separately gated | R1 |
| E-033 | 2026-08-15 | DBVC isolated PHPUnit performance fixture | temporary 20-entity R1-B probe against the configured test database | One 20-entity chunk produced 60 findings in 4.661 ms with 24 WordPress queries, zero additional allocated/peak memory pages at PHP's reported granularity, and a 4,983-byte compressed 20-group snapshot | Bounded chunk instrumentation and compact storage are functioning at the default-size cohort; 100/500/2,000-object, REST payload, and frontend render measurements remain R1-E gates | R1 |
| E-034 | 2026-08-15 | DBVC | `vendor/bin/phpunit --do-not-cache-result`; isolated and class reruns; `composer agent-docs:check` | The first full run reported the six inherited failures plus `ContentMigrationPhase4ImportExecutorTest::test_import_executor_execute_is_idempotent_across_reruns`. That extra test passed alone (1/34) and with its complete class (32/568); a clean full rerun produced 694 tests/7,302 assertions with exactly the inherited six failures. Agent docs passed with 54 records/408 surfaces/0 unmapped. | No reproducible new PHP failure was introduced by R1-B; the repository suite remains non-green and the transient extra result is recorded rather than hidden | R1/All |
| E-035 | 2026-08-15 | DBVC | `MediaScanReadModel`; `MediaManagerController`; `Routes`; `Addon`; `VisualEditorMediaManagerR1CTest` | R1-C adds seven feature/base-capability/active-mode-protected REST routes, bounded safe list projection, current list-time permission/status checks, user/site/generation/revision isolation, and one-owner row rescanning. Deep payload assertions exclude internal owner/field/path/fingerprint data; focused R1-C passed 6 tests/417 assertions and combined current Visual Editor focus passed 23/603. | The read/list/row gate is implemented without frontend UI, descriptors, Media Library actions, journal/cache work, or content mutation; R1-D remains separately gated | R1 |
| E-036 | 2026-08-15 | DBVC | `vendor/bin/phpunit --do-not-cache-result` | The R1-C full comparison ran 700 tests/7,723 assertions with exactly the six inherited failure identities: Bricks Phase 11 i18n, Bricks Phase 7 disabled mode, Capability Landscape prepared records, Content Collector Phase 29 conflicts, Content Collector Phase 32 URL QA, and Content Migration Phase 37 settings defaults. | R1-C introduced no new PHP failure; the repository suite remains non-green and the incomplete full-JavaScript-lint baseline remains unchanged | R1/All |
| E-037 | 2026-08-15 | DBVC | `composer agent-docs:refresh`; `composer agent-docs:check` | Generated references include the seven R1-C REST registrations; strict check passed with 54 curated records, 415 discovered surfaces, and zero unmapped | The public REST/add-on/safety reference is synchronized with R1-C | R1/All |
| E-038 | 2026-08-15 | DBVC/package | `MEDIA-MANAGER-CLAUDE-MOCKUP-HANDOFF.md`; minimal Claude prompt; `media-manager-r1c-view-model.json` | R1-D authorization is translated into a progressive context contract: one launcher, one capsule, one current read-only fixture, targeted CSS/image evidence, and separate D1-D4 review stops. The older future-R2 fixture is no longer the default R1 source. | Claude can begin D1 with substantially less duplicated prompt context; no static or production UI is claimed | R1 |
| E-039 | 2026-08-15 | DBVC/mockup | `docs/ui-mockups/dbvc-visual-editor/r1-media-manager/` | Claude delivered 15 read-only D1-D4 files: default and 11-state HTML views, scoped CSS, local-only interaction JS, five screenshots, component notes, wiring schematic, decision record, and handoff aids. Claude reported 30/30 structural checks, passing `node --check`, and no horizontal overflow at 1440/900/390 widths. | Static visual direction exists and preserves the R1 read-only boundary; it is not production integration, accessibility proof, cross-browser evidence, or real-device QA | R1 |
| E-040 | 2026-08-15 | DBVC source/browser review | `MediaScanReadModel`; `MediaManagerController`; `MediaScanCoordinator`; R1-C fixture; mockup `styles.css`/`mockup.js`/wiring docs | Source reconciliation confirmed alphabetical `entity_asc`, false `hasMore` when six rows fit under limit 20, valid `missing_desc`/`scanned_desc`, fingerprint-based expansion freshness, and safe field projections for changed/resolved/provider-unavailable rows. It also identified backend state names and distinct group-404 semantics. Browser interaction checks passed search, expansion, close/reopen, and desktop geometry, but 390x844 measured drawer client height 738 versus scroll height 1264 with `overflow-y: hidden`; sort, legend, results, and footer were clipped. The local script also announced final revalidation before a pending placeholder and used plural copy for one row. | Approve the visual direction with required D4A fixture/state/responsive/interaction corrections before production translation; accessibility, AT, cross-browser, real-device, and frontend integration gates remain open | R1 |
| E-041 | 2026-08-16 | DBVC/mockup D4A handback | Corrected R1-C fixture; `docs/ui-mockups/dbvc-visual-editor/r1-media-manager/`; Claude D4A-1/D4A-2/D4A-3 reports; Codex reconciliation | D4A produced a contract-valid fixture and four expansion projections, explicit backend/view-state and request-error mapping, a single reachable short/mobile drawer scroll body, corrected pending/final announcements and count grammar, 44px narrow/short targets, 13 state cases, and seven refreshed true-viewport screenshots. Structural checks passed 30/30, `node --check` and package 46/46 integrity passed. axe-core 4.11.0 on `index.html` returned 33 passing rule groups and one serious `color-contrast` violation affecting 12 confirmed nodes, all traced to the shared production subtle-text token; additional contrast nodes remained incomplete | D4A is accepted as qualified static direction. Production integration, assistive technology, Safari/Firefox, real-device behavior, `states.html` axe coverage, and the shared-token correction remain separate evidence gates | R1 |
| E-042 | 2026-08-16 | DBVC R1-D production shell and contrast checkpoint | `AssetLoader`; `overlay-app.js`; `media-manager-app.js`; `media-manager.css`; `overlay.css`; `VisualEditorMediaManagerR1DTest`; refreshed mockup | The default-off feature now conditionally enqueues a separate Media Manager shell module inside active Visual Editor mode. Its toolbar entry replaces the disabled overflow placeholder at the gate, and the trigger exposes dialog relationships; open/close, Escape, `aria-expanded`, and trigger-focus restoration are module-owned. Focused PHP coverage passed 4 tests/32 assertions including disabled and Builder asset gates. A same-checkout isolated Chromium harness passed toolbar/shell geometry and focus at 1440x900, 1440x600, 390x844, and 320x568 with no horizontal overflow and zero axe violations in the shell. The full PHP comparison ran 704 tests/7,755 assertions with exactly the inherited six failures; repository `npm run lint` completed with stale browser-data warnings, and direct Node syntax checks passed for the raw Visual Editor assets outside that lint target. Agent docs passed 54 records/415 discovered surfaces/0 unmapped. The shared subtle token was moved to the 60% mix; post-correction axe returned 33 passes/0 violations/38 incomplete for `index.html` and 22 passes/0 violations/0 incomplete for `states.html`; seven screenshots were regenerated | This slice makes no scan/list request, table projection, descriptor, Media Library call, content mutation, journal write, or cache invalidation. The browser harness is not authenticated WordPress runtime, AT, cross-browser, or real-device evidence | R1 |
| E-043 | 2026-08-16 | DBVC R1-D production API/state checkpoint | `api-client.js`; `media-manager-app.js`; `media-manager.css`; `AssetLoader`; `VisualEditorMediaManagerR1DTest`; `tests/visual-editor-media-manager-state.test.cjs`; `composer agent-docs:check` | The API client wraps latest/start/list/next/retry/cancel with nonce and generation/revision identity. The separate state controller maps all five backend states plus no-scan/request-error/stale outcomes, stores copied safe list/query/pagination state, suppresses out-of-order responses, drives action visibility from authoritative state and `canRetry`/`canCancel`, and renders no result rows. Five jsdom state/route tests pass. Isolated Chromium at 1440x900 and 1280x720 passed latest 404, start, next, list identity/query, no-row DOM, geometry, Escape/focus, and shell axe with 13 passes/0 violations at each viewport. The full PHP comparison ran 704 tests/7,755 assertions with exactly the same six inherited failures. Repository lint including all touched Visual Editor JavaScript and the focused state test completed with stale browser-data warnings only. Agent docs passed 54 records/415 discovered surfaces/0 unmapped | Browser evidence uses mocked protected responses and is not authenticated WordPress runtime. Result-table/row expansion, descriptors, Media Library actions, content mutation, journal/cache effects, and additional responsive/mobile work remain absent | R1 |
| E-044 | 2026-08-16 | DBVC R1-D production table checkpoint | `media-manager-app.js`; `media-manager.css`; `AssetLoader`; `VisualEditorMediaManagerR1DTest`; `tests/visual-editor-media-manager-state.test.cjs`; `tests/playwright/visual-editor-media-manager-table.spec.js` | The frontend renders only normalized safe R1-C rows in a semantic internally scrollable table. Search/entity/field/sort remain server-driven; first-page replacement and opaque-cursor append preserve query state, de-duplicate group references, and preserve append scroll. Eight jsdom tests cover safe collapsed DOM and unknown-key stripping, query replacement, all sorts, cursor append, empty/error states, route identity, stale suppression, and backend-state mapping. Four PHPUnit tests/37 assertions keep asset/Builder/read-only/static-contract gates. Two isolated Chromium tests pass at 1440x900 and 1280x720 with 15-to-25 row append, scroll retention, server replacement/ARIA sort, nonce/identity/query/cursor proof, no expansion control, bounded geometry, focus restoration, and axe WCAG A/AA zero violations. The full PHP comparison ran 704 tests/7,760 assertions with exactly the inherited six failures. Targeted `wp-scripts` lint passes for the new module/tests with stale browser-data warnings only; the aggregate repository command did not complete in a bounded rerun. | Mocked browser responses are not authenticated WordPress runtime. Lazy group expansion, descriptors, Media Library actions, mutation/journal/cache effects, large-scale profiling, AT/cross-browser evidence, and additional responsive/mobile work remain outside this slice | R1 |
| E-045 | 2026-08-16 | DBVC R1-D production row-expansion checkpoint | `api-client.js`; `media-manager-app.js`; `media-manager.css`; `AssetLoader`; `VisualEditorMediaManagerR1DTest`; `tests/visual-editor-media-manager-state.test.cjs`; `tests/playwright/visual-editor-media-manager-table.spec.js` | The frontend group client sends the current opaque scan/generation/revision identity and an allowlisted group reference. One-row lazy expansion has independent loading/error state, strips unknown keys, forces descriptor/media actions false, distinguishes group-request failure from successful provider-unavailable projections, and suppresses late responses after collapse or a newer row. Ten jsdom tests pass; focused R1-D PHP passes 4 tests/41 assertions; targeted and aggregate lint plus Node syntax pass with stale browser-data warnings only. Two isolated Chromium tests at 1440x900 and 1280x720 verify group identity, field statuses, ARIA/region/collapse behavior, no assignment action, retained paging/scroll/geometry/focus behavior, and axe WCAG A/AA zero violations in expanded and collapsed states. The full PHP comparison ran 704 tests/7,764 assertions with exactly the inherited six failure identities. | Browser responses are mocked. Authenticated WordPress runtime, assistive technology, cross-browser, large-site performance, and all descriptor/Media Library/mutation effects remain separate gates | R1 |
| E-046 | 2026-08-16 | DBVC initial R1-E keyboard/reduced-motion hardening | `media-manager-app.js`; `media-manager.css`; `VisualEditorMediaManagerR1DTest`; jsdom state test; permanent Playwright table spec | The table renderer now captures the active row disclosure through `body.ownerDocument.activeElement` and restores focus to the replacement safe group-ref button after loading/success/collapse rerenders with `preventScroll`. The disclosure indicator disables its transition under reduced-motion preference. Jsdom focus assertions pass; focused PHP passes 4 tests/43 assertions; targeted and aggregate lint pass with stale dependency-data warnings. Chromium at both supported viewports activates expansion with Enter, collapses with Space, retains row-button focus, verifies `transitionDuration=0s`, and retains expanded/collapsed axe zero violations. | Mocked responses are not authenticated WordPress runtime or screen-reader evidence. Safari/Firefox, assistive technology, large-site profiling, and current active-site integration remain open R1-E gates | R1/R1-E |
| E-047 | 2026-08-16 | DBVC R1-E synthetic scale/no-auto-scan hardening | `VisualEditorMediaManagerR1ETest`; focused and combined R1-A-R1-E PHPUnit | Ordinary frontend Media Manager asset enqueue leaves the current user without a latest scan. Synthetic 100/500/2,000-group compressed snapshots remain below the 5 MiB ceiling and return only a 50-row page. Focused R1-E passes 2 tests/50 assertions; combined R1-A-R1-E passes 22/681. In the combined 2,000-group run: 25.425 ms, 0 additional WordPress queries, 6,291,456 allocated-memory bytes, 120,475 stored bytes, and 24,833 response bytes | Synthetic groups share one eligible owner to isolate snapshot/list/payload cost. Complete candidate traversal/raw ACF reads, authenticated REST/browser transport, AT, and cross-browser proof remain open | R1/R1-E |
| E-048 | 2026-08-16 | DBVC full PHP comparison after R1-E scale coverage | `vendor/bin/phpunit -c phpunit.xml.dist --do-not-cache-result` | Full suite ran 706 tests/7,816 assertions with six failures. The identities remain Bricks Phase 11 i18n, Bricks Phase 7 disabled mode, Capability Landscape prepared records, Content Collector Phase 29 conflict state, Content Collector Phase 32 URL QA state, and Content Migration Phase 37 settings defaults | No new PHP failure was introduced; the repository suite remains non-green | R1/All |
| E-049 | 2026-08-16 | DBVC R1-E automated semantic/fallback hardening | `media-manager-app.js`; `AssetLoader`; R1-D PHP/jsdom/Playwright coverage; full PHP comparison | The dialog now references its visible description; results and the focusable table scroller have stable headings/names; list/group loading exposes status/live/atomic and busy state; every expansion presentation retains an entity-specific heading; and the stable polite live region announces field-check start/completion/failure. Eleven jsdom tests, 4 R1-D PHP tests/47 assertions, targeted lint, and 6 Chromium/Firefox/WebKit viewport cases pass while expanded/collapsed axe stays at zero confirmed violations. Combined R1-A through R1-E passes 22/685; full PHP runs 706/7,820 with the same six failures. The latest aggregate lint rerun did not complete | Automated semantics/browser engines do not prove real screen-reader or real Safari behavior. Default-off/Builder/no-auto-scan/no-migration fallback is retained; authenticated runtime, AT, real Safari, complete traversal/raw reads, and aggregate lint completion remain open | R1/R1-E |
| E-050 | 2026-08-16 | LocalWP runtime / DBVC | read-only `wp_options` SELECT on site `4gScrLykQ` (`dbvc-codexchanges.local`); unauthenticated `curl` of `dbvc/v1` REST index and Media Manager routes | Runtime provenance rechecked read-only: active site `dbvc-codexchanges.local`, `template=bricks`/`stylesheet=vertical`, DBVC plugin active, `siteurl=https://dbvc-codexchanges.local`, MySQL running. `dbvc_visual_editor_media_manager_enabled=1`. The persistent `dbvc_addon_visual_editor_enabled` is now `1` (ON) — a drift from the previously recorded OFF state; no option/login/content/LocalWP state was changed. All seven live Media Manager REST routes are registered; unauthenticated GET `.../scans/latest`, a tampered `.../scans/{ref}` list, a tampered group route, and POST `.../scans` each return HTTP 401 `rest_forbidden` with no data and no created snapshot | The live REST surface and its permission gate are confirmed against real WordPress and enforce authentication before resource resolution. Authenticated active-site REST/table data behavior remains unproven because no already-authorized session is available. The VE-option drift is recorded, not reverted | R1/R1-E |
| E-051 | 2026-08-16 | DBVC isolated PHPUnit fixture | `tests/phpunit/VisualEditorMediaManagerR1ETest.php::test_complete_candidate_traversal_and_raw_reads_scale_without_field_definition_n_plus_one` | A new deterministic, non-mutating test drives the real `ScanCandidateProvider`->`MediaScanService`->`ScanSnapshotStore` pipeline to completion across 100 and 300 live owners at chunk size 50. Every owner is enumerated exactly once and raw-read into three empty findings (featured + ACF image + ACF gallery); per-owner raw ACF reads are constant at 2 and applicability is evaluated exactly once per candidate. Per-candidate DB cost falls from ~1.25 to ~0.83 queries as owners triple (total ~125->~249), max candidates/chunk = 50, max source queries/chunk = 1, wall 41-219 ms, snapshot 15.3-41.0 KB. Focused R1-A-R1-E now passes 23 tests/1,127 assertions | Complete multi-owner candidate traversal and raw ACF read scaling is now measured against live fixtures with no field-definition/capability/permalink N+1 (per-candidate cost decreases with scale). This complements the synthetic 100/500/2,000-group snapshot/read/payload evidence. Authenticated REST/browser transport, real AT, and real Safari remain open | R1/R1-E |
| E-052 | 2026-08-16 | DBVC | `vendor/bin/phpunit -c phpunit.xml.dist --do-not-cache-result`; `npm run lint` | Full suite ran 707 tests/8,262 assertions with exactly the six inherited failure identities (Bricks Phase 11 i18n, Bricks Phase 7 disabled mode, Capability Landscape prepared records, Content Collector Phase 29 conflicts, Content Collector Phase 32 URL QA, Content Migration Phase 37 settings defaults). The +1 test/+442 assertions over the prior 706/7,820 are the new candidate-traversal test. One bounded aggregate `npm run lint` attempt ran ~11 minutes without completing and was stopped; targeted Media Manager lint passes with stale Baseline/Browserslist warnings only | The new coverage introduces no PHP regression; the repository suite remains non-green on the same six inherited identities. Aggregate repository JavaScript lint still has no completed result and remains a recorded caveat, not a pass | R1/All |
| E-053 | 2026-08-16 | DBVC | `addons/visual-editor/src/MediaManager/MediaFindingDescriptorBridge.php`; `EditableRegistry::persistDetachedDescriptor()`; `MediaManagerController` finding-descriptor route; `Routes`/`Addon` wiring; `VisualEditorMediaManagerR2ATest` | R2-A adds a server-authoritative bridge that exchanges one opaque finding for one fresh standard `EditableDescriptor`. It loads the user/site-bound snapshot, validates generation/revision, resolves the owner/field only from the snapshot group/finding, rechecks owner eligibility/status/capability, rescans the single owner to reconfirm applicability and the current empty value by fingerprint, then mints a descriptor for the correct family (`post_featured_image`/`acf_image`/`acf_gallery`) routing to exactly one existing resolver, persisted via the narrow `persistDetachedDescriptor()`. The response exposes only opaque token/session ids plus safe labels/status; no owner id, field key/name/selector, ACF object id, path, or fingerprint leaks. It opens no media frame, hydrates no value, and mutates nothing. Focused R2-A passes 11 tests/200 assertions; combined R1-A-R1-E plus R2-A passes 34/1,327 | The finding->fresh-descriptor bridge is implemented with no client-supplied target authority and fails closed on tamper/expiry/stale/format/changed/permission cases; Media Library selection, upload, and save remain R2-B/R2-C | R2/R2-A |
| E-054 | 2026-08-16 | DBVC | `vendor/bin/phpunit -c phpunit.xml.dist --do-not-cache-result`; `composer agent-docs:refresh`/`check` | Full suite ran 718 tests/8,462 assertions with exactly the same six inherited failure identities; the +11 tests/+200 assertions over 707/8,262 are R2-A. Agent docs were refreshed for the new `.../findings/{finding_ref}/descriptor` REST registration and pass 54 curated records, 416 discovered surfaces (+1), and 0 unmapped; the shifted `dbvc_visual_editor_session_descriptor_compression_level` hook and the eight Media Manager routes were remapped in `manifest.json` | R2-A introduces no PHP regression; the new public REST route is registered and covered in the agent reference. The repository suite remains non-green on the same six inherited identities | R2/All |

| E-055 | 2026-08-16 | DBVC | `media-manager-app.js` (assign controls + wp.media staging); `api-client.js` `mediaManager.descriptor`; `AssetLoader.php` R2-B strings; `media-manager.css`; `visual-editor-media-manager-state.test.cjs`; `VisualEditorMediaManagerR1DTest` | R2-B renders a capability-gated `assign-media` control for each field still `missing`, calls the R2-A bridge on activation, and on a `writable` descriptor opens the native `wp.media` frame (single image / multiple gallery) reusing the same standard config as `overlay-app.js`. Selections stage client-side with an `Unsaved selection` badge, thumbnail preview, and `Replace`/`Clear`; non-writable descriptors surface a notice and never open the frame; clearing and collapse discard the selection. No save, mutation, journal, or cache path is touched, and the descriptor token/session id and all raw targets stay out of the DOM. jsdom passes 16 tests (11 prior + 5 R2-B); targeted `lint:visual-editor-media-manager` is clean; R1-D read-only + R2-A focused pass 15 tests/252 assertions. `overlay-app.js` is unchanged | The loaded table can now choose an existing image or upload a new one for a missing field, staged unsaved, with no content write; field save and no-reload reconciliation remain R2-C | R2/R2-B |
| E-056 | 2026-08-16 | DBVC | `vendor/bin/phpunit -c phpunit.xml.dist --do-not-cache-result`; `composer agent-docs:refresh`/`check` | Full suite ran 718 tests/8,467 assertions with exactly the same six inherited failure identities; the +5 assertions over 8,462 are the R1-D read-only invariant updated to allow staged, unsaved `wp.media` (no new test, no new regression). Agent docs were refreshed for the AssetLoader localized-string change (discovery fingerprint only) and pass 54 curated records, 416 discovered surfaces, and 0 unmapped; R2-B adds no new REST/hook/setting surface | R2-B introduces no PHP regression and no new agent-reference surface; the repository suite remains non-green on the same six inherited identities | R2/All |

| E-057 | 2026-08-16 | DBVC | `MediaAssignmentService`; `MediaFindingDescriptorBridge::resolveFinding`; `MediaManagerController` assignment route; `media-manager-app.js` save/reconcile; `api-client.js` `mediaManager.assign`; `AssetLoader.php` R2-C strings; `media-manager.css`; `VisualEditorMediaManagerR2CTest`; `visual-editor-media-manager-state.test.cjs` | R2-C saves the staged selection through `POST .../findings/{finding}/assignment`. The service re-runs the R2-A revalidation as the expected-empty precondition, mutates through the shared `MutationService` (resolver save, journal/audit, cache invalidation), and rereads via `expandGroup` to reconcile. `VisualEditorMediaManagerR2CTest` (7 tests/81 assertions) proves featured-image/ACF-image/ACF-gallery saves persist and reconcile, a field populated after scan is blocked with `409 media_assignment_stale` and not overwritten, non-image and empty selections are rejected without a write, and a stale generation is blocked. jsdom adds 3 R2-C cases (19 total): save reconciliation with no list/scan reload, a conflict that keeps the staged selection and shows a notice, and the in-progress saving state. R1-D read-only invariant updated (gated save via the dedicated endpoint only; still no direct fetch/generic/composite save); combined Media Manager PHP 41 tests/1,413 assertions; targeted lint clean | Field-level assignment is implemented with the expected-empty gate enforced and proven, reusing the single audited mutation pipeline and reconciling without a table reload; only real-browser save QA remains the residual gate | R2/R2-C |
| E-058 | 2026-08-16 | DBVC | `vendor/bin/phpunit -c phpunit.xml.dist --do-not-cache-result`; `composer agent-docs:refresh`/`check` | Full suite ran 725 tests/8,550 assertions with exactly the same six inherited failure identities; the +7 tests/+83 assertions over 718/8,467 are R2-C plus the R1-D invariant update. Agent docs were refreshed for the new `.../assignment` REST registration and the AssetLoader string change and pass 54 curated records, 417 discovered surfaces (+1), and 0 unmapped; the nine Media Manager routes were remapped in `manifest.json` | R2-C introduces no PHP regression; the new content-mutating route is registered and covered in the agent reference | R2/All |

| E-059 | 2026-08-16 | DBVC | `MediaFindingDescriptorBridge`; `EditableRegistry`; `MediaAssignmentService`; `media-manager-app.js`; `VisualEditorMediaManagerR2ATest`/`R2CTest`; `visual-editor-media-manager-state.test.cjs` | Applied three R2 self-review fixes. CR-1: removed the unused descriptor persistence and `{token, sessionId}` round-trip — the bridge returns only `{input, family, expectedState}`, `EditableRegistry::persistDetachedDescriptor` was deleted, and the client staged selection keeps no token/session; the R2-A test now inspects the minted descriptor via `resolveFinding` and asserts the response has no token, and cross-user binding is proven at the snapshot layer. CR-4: capped gallery assignment cardinality (200) server-side with a rejection test. CR-5: `reconcileAfterSave` now patches only the affected collapsed row, expanded panel, and summary instead of rebuilding the whole table. R2-A 11 tests/204 assertions; R2-C 8 tests/85 assertions; jsdom 19; combined Media Manager PHP 42/1,429; targeted lint clean; agent docs 54/417/0 | The R2 implementation is simpler (no dead persistence/token state), more robust (bounded gallery input), and cheaper to reconcile (targeted DOM patch), with coverage preserved | R2/Review |

| E-060 | 2026-08-16 | DBVC | `media-manager-app.js`; `AssetLoader.php` (`canUpload` + R2-D strings); `media-manager.css`; `visual-editor-media-manager-state.test.cjs`; mockup `COMPONENT-NOTES.md` §11 | R2-D adds verified UX states over R2-A/B/C with no new REST/mutation surface: `Opening Media Library…` while the descriptor request is in flight; unsaved image/gallery selection; an upload-unavailable hint driven by the new `canUpload` bootstrap flag; save-in-progress; a verified `Saved` field chip plus a `Resolved` row badge marked in place; a polite refresh notice for changed-since-scan; and an assertive `role="alert"` validation-error notice that retains the staged selection. jsdom passes 23 tests (4 new R2-D); the R1-D read-only invariant is intact; targeted lint clean | The nine R2-D states are implemented and jsdom-verified with correct ARIA (polite status vs assertive alert), distinct saved/resolved presentation, and honest upload gating; the mockup notes catalog them | R2/R2-D |
| E-061 | 2026-08-16 | DBVC | `vendor/bin/phpunit -c phpunit.xml.dist --do-not-cache-result`; `composer agent-docs:refresh`/`check` | Full suite ran 726 tests/8,564 assertions with exactly the same six inherited failure identities (R2-D added only jsdom coverage, no PHP tests). Agent docs were refreshed for the AssetLoader string/flag change and pass 54 curated records, 417 discovered surfaces, and 0 unmapped; R2-D adds no new REST/hook/setting surface | R2-D introduces no PHP regression and no new agent-reference surface | R2/All |

| E-062 | 2026-08-16 | DBVC | `VisualEditorMediaManagerR2ETest`; `dbvc_visual_editor_audit_event`/`dbvc_visual_editor_invalidate_cache` hooks; `{prefix}dbvc_ve_change_items` | R2-E1 journal/cache verification. A successful media assignment records a `completed` change-journal item (correct `resolver_name` and the new attachment value), fires the audit event exactly once with the empty old value, the new attachment, and the acting user, and fires cache invalidation exactly once for the correct post entity. A failed (non-image) save records a `failed` journal item, fires neither the audit nor the cache-invalidation hook, and leaves the featured image unset. An ACF image save records `resolver_name = acf_image` and invalidates the post cache. 3 tests/39 assertions; combined Media Manager PHP 45 tests/1,468 assertions | The R2-C save path's journaling, auditing, and cache invalidation are proven through the existing services, and a failed save records a failure rather than a phantom success or a stray invalidation | R2/R2-E1 |

| E-063 | 2026-08-16 | DBVC | `MediaScanService` (`$include_assigned`/`buildPreview`); `MediaScanReadModel` (`projectAssignedField`/dedup/`populated` count); `media-manager-app.js` (`normalizeFieldPreview`, `assigned` status); `AssetLoader.php`/`media-manager.css`; `VisualEditorMediaManagerR2FTest` | R2-F Slice 1 (populated-field inventory + bounded preview). Expanding an entity now lists populated fields (`status: assigned`) beside empty findings, each with a sanitized `preview` (thumbnail URL/alt/count); a field empty at scan and now populated is deduped to one entry (resolved, with the merged preview); the snapshot and top-level list/counts stay empty-focused; the preview leaks no field key/selector/owner id/ACF object id/path/raw value. Slice 1 is read-only (`replace: false`, no assign control on populated fields). `VisualEditorMediaManagerR2FTest` 3 tests/29 assertions; jsdom +1 (24 total); combined Media Manager PHP 48 tests/1,504 assertions; R1-D read-only invariant intact; full suite 732/8,639 with the same six inherited failures; agent docs 54/417/0 | The entity media inventory is reported safely (preview-only) without disturbing the top-level missing-media report; thumbnail presentation (Slice 2) and replace (Slice 3) remain | R2-F/Slice 1 |

| E-064 | 2026-08-17 | DBVC | `media-manager-app.js` (`createFieldThumbnail`, `createFieldProjection`); `media-manager.css` (`field-item`/`field-content`/`field-thumb`); `AssetLoader.php` (`mediaManagerThumbCount`); `visual-editor-media-manager-state.test.cjs` | R2-F Slice 2 (thumbnail presentation). Each field renders as `[thumbnail | content]`; `createFieldThumbnail` prefers a staged selection thumbnail, then the populated `preview.url`, then an accent-color placeholder wrap for empty fields, and adds a `+{count}` badge for galleries. Images set `loading="lazy"`/`decoding="async"` via attributes and render only inside the already-lazy detail panel (nothing fetched for collapsed rows). CSS gives the thumb a square `aspect-ratio: 1/1`, rounded, full field-item height, and a `color-mix` accent placeholder. jsdom `R2-F Slice 2` case asserts the thumb/content cells, lazy attributes, the placeholder for the empty field, and the gallery `+2` badge; media-manager lint clean; R1-D read-only invariant intact | The entity inventory now carries a left-aligned lazy thumbnail per field with a single accent placeholder swap point for a future default image, without eager loading or a mobile-specific change | R2-F/Slice 2 |

| E-093 | 2026-08-27 | DBVC / LocalWP runtime | Read-only inspection of `wp_options.dbvc_visual_editor_curation_decisions` via LocalWP's own MySQL socket (`.../Library/Application Support/Local/run/4gScrLykQ/mysql/mysqld.sock`); one-shot script `scratchpad/apply_recommender_include.php` (disposable, not committed) that bootstrapped `wp-load.php` with `DB_HOST` forced to the socket path, iterated `FieldCandidateProvider::getCandidates()` × `FieldCurationRecommender::recommend()`, and wrote `decision=include` + recommender-suggested `category` via `CurationStore::setDecision()` for every candidate whose recommender verdict was `include` and whose current decision was not `ignore` (preserve user intent) or already `include`; `docs/dropins/dbvc-visual-editor-brand-controls-guide/deferred/BRAND-CONTROL-CENTER-ADMIN-OPTIONS-PHASE.md` (new deferred-phase doc); `docs/dropins/dbvc-visual-editor-brand-controls-guide/tracking/DECISION-LOG.md` (D-062 locking the direction) | Two-part event on 2026-08-27. **Part 1 — inspection & bulk apply**: read the persisted curation store to confirm progress. Store held **769 records** (767 defer / 2 include / 0 ignore / 0 undecided; all priorities empty; 749 with no explicit category; 16 Brand / 3 Content / 1 Layout Elements; earliest decided_at 2026-08-27 20:30, latest 20:34 — a bulk-defer sweep). Maintainer directive: "approve to include all of the fields that you labeled/recommended as 'include'." One-shot script applied that. Result: **397 rows flipped defer → include** with recommender-suggested category applied. Zero pre-existing include or ignore decisions were overridden. Final distribution: **400 include / 370 defer / 0 ignore / 0 undecided / 770 total** (walker discovered 770 candidates now, one more than the store held — likely a field that materialized after the earlier group-container fix or an ACF definition change). **Part 2 — deferred-phase decision**: maintainer additionally directed that the R3-BX curation admin surface be **kept forward as a permanent admin governance tool** ("Brand Control Center · admin options") rather than being decommissioned once the seed is exported. Recorded as **D-062** with a full-scope deferred-phase doc (target state, promotion migration story, explicit non-goals for the current tool, deferral triggers, cross-refs). No code touched, no test surface changed, no runtime option (other than the intentional curation decisions) mutated | Maintainer's curation decision set is now bulk-representative of the recommender's include recommendations (52% include / 48% defer distribution); the eventual `VerticalControlProvider` seed export will emit ~400 approved records grouped across all 7 categories, providing realistic fixture density for the R3 mockup and the R3-C production drawer. The tool itself is now direction-locked as a future permanent admin surface, so no future planner has to re-derive whether the tool is throwaway | R3/R3-BX + deferred admin-options phase |
| E-092 | 2026-08-27 | DBVC | `docs/dropins/dbvc-visual-editor-brand-controls-guide/knowledge/EXISTING-SUPPORT-INVENTORY.md` (new); `docs/dropins/dbvc-visual-editor-brand-controls-guide/releases/R3-REGISTRY-BACKED-BRAND-CONTROL-CENTER.md` (added reuse-map link at the top); `addons/visual-editor/docs/handoffs/DBVC_R3_RESUME_PROMPT.md` (added the inventory as required read #1, renumbered the rest) | Adoption inventory doc — captures every already-proven Visual Editor infrastructure piece so future R3-B / R3-C / R3-D / R4 / R5.x / R6 / VerticalControlProvider slices reuse rather than rebuild. Structured in four sections: **§1 ACF family support matrix** (every family × resolver × frontend controller × `input` key in `createFieldController` × option-owned save × R5 slice mapping × status — anchored by file:line refs to the actual resolver `supports()` clauses and the `createFieldController` switch table); **§2 Cross-cutting reusable infrastructure** (descriptor system, mutation pipeline, REST conventions, Bricks Builder exclusion, feature-flag/kill-switch pattern, media-frame factory RK-011, Persistent Media Index R2-H patterns, frontend UI conventions incl. `.dbvc-ve-*` BEM + tokens + row-focus continuity + single polite live region + reduced-motion, admin AJAX + capability + nonce, filesystem export pattern, asset URL + `filemtime()` versioning gotcha with a pointer to E-090 for the exact failure mode, change-journal/audit/cache); **§3 Per-planned-slice adoption checklist** (R3-B / R3-C / R3-D / R4 / R5.1 / R5.2 / R5.3 / R5.4 / R6 / VerticalControlProvider — each with a "reuse" list and a "build new" list); **§4 Known gaps** (repeater/flexible/clone, `color_picker`, `true_false`, `date/time_picker`, `google_map`, `oembed`, `file`, `font-awesome`, `user` field family, batch save, cross-entity Save Row, static Bricks image settings, PNG mockup capture). Not a spec — a snapshot with a maintenance protocol at the bottom. Zero code touched, zero tests touched — pure docs delta | Any agent starting a next slice now has one authoritative "what already exists" doc to consult before designing, eliminating the archaeology overhead we've been paying repeatedly this session. Directly informs the R3-B implementation (which resolvers to reuse), R3-C (which controllers + descriptor factories + z-index token pattern to adopt), R5.1-R5.4 (which family branches to add to the Vertical provider descriptor factory), and the eventual VerticalControlProvider (which JSON→descriptor mapping to write) | R3/All |
| E-091 | 2026-08-27 | DBVC | `addons/visual-editor/src/Curation/FieldCandidateProvider.php` (walker no longer emits `type=group` rows; still recurses into `sub_fields` so every leaf appears with the group's ancestor labels intact); `addons/visual-editor/src/Curation/FieldCurationRecommender.php` (dropped the semantically-incorrect `'group' => 'R5.2'` entry from `FAMILY_UNLOCK_MAP`; groups now fall through to `later`); `tests/phpunit/VisualEditorCurationCandidateProviderTest.php` (replaced `test_group_subfields_are_walked_and_ancestor_labels_captured_from_tabs_and_groups` with `test_group_containers_are_not_emitted_but_their_leaves_are_walked_with_ancestor_labels`; added `test_a_group_with_no_leaves_contributes_zero_candidates`); `tests/phpunit/VisualEditorCurationRecommenderTest.php` (added explicit `deriveUnlocksAt('group') === 'later'` assertion documenting the intentional omission) | R3-BX product cleanup — skip ACF `group` container fields from the curation candidate list. A `group` is a structural namespace that holds subfields; it has no value, no editor, no descriptor factory, no R5 slice that can serve it, so a curation row for a group had nothing to Open once R3-C ships. The maintainer flagged them as noise in the curation table (example: `settings_cards>features` group). The walker still recurses into `sub_fields`, so every editable leaf still surfaces as a normal candidate row with the group's ancestor labels flowing through — no information is lost, only the container row is removed. Curation decisions that were made on group rows before this change stay in `dbvc_visual_editor_curation_decisions` but are silently dropped from future renders and from the JSON export (`CurationExporter` walks candidates × decisions, so a decision keyed to an id no longer emitted as a candidate becomes inert). Validation: `--filter "VisualEditorCuration"` **22 tests / 78 assertions OK** (was 21/76 — two new candidate cases + one recommender assertion). Full PHP suite identity unchanged (six inherited + one pre-existing `ProposalDiffContractTest`); agent docs unchanged (no new surface) | Curation table no longer shows non-actionable group container rows; JSON export cannot include a group as an approved control | R3/R3-BX |
| E-090 | 2026-08-27 | DBVC | `addons/visual-editor/src/Admin/CurationPage.php` (asset URL bugfix + `filemtime()`-based asset versioning + client-side filter DOM: colgroup, row `data-*` attributes, `.dbvc-ve-curation__filters-status` live-count region, `<button type="button">` reset, hidden no-match row; server-side `applyFilters()` removed, replaced with `prepareRows()` that returns every candidate); `addons/visual-editor/assets/js/curation.js` (`bindFilterEvents`, `readFilterState`, `rowMatchesFilter`, `applyFilters`, `resetFilters`, `syncRowFilterAttrs`, debounced label-search input, form-submit hijack); `addons/visual-editor/assets/css/curation.css` (`table-layout: fixed`, `<colgroup>`-driven widths, ellipsis on truncatable cells, `.dbvc-ve-curation__table-wrap` horizontal scroll container, `.dbvc-ve-curation__decision-radios` flex column, `.dbvc-ve-curation__filters-status` styling) | R3-BX post-landing polish. Two bugs + two enhancements, all admin-only, no test surface changes. **Bug 1 (broken asset URL)**: `CurationPage::enqueueAssets` computed the plugin URL via `plugins_url('addons/visual-editor', dirname(__DIR__, 3) . '/db-version-control.php')`. From `src/Admin/CurationPage.php` `dirname(__DIR__, 3)` is `.../addons` (not the plugin root — that's 4 levels up), so WP built the plugin URL as `.../plugins/db-version-control-main/addons` and appending the path produced a doubled `addons/addons/...` URL that 404'd. Fixed by anchoring `plugins_url('', ...)` on the addon's own real `bootstrap.php` at `dirname(__DIR__, 2) . '/bootstrap.php'`. **Bug 2 (browser cache never busted)**: assets were versioned `'0.1.0'` hard-coded, so the browser served cached files forever. Switched to `filemtime()`-based versioning matching the existing `AssetLoader` pattern; falls back to `'0.1.0'` if the file is unreadable. **Enhancement 1 (column-width overflow)**: table now uses `table-layout: fixed` with an explicit `<colgroup>` sizing all 12 columns; wrapped in `.dbvc-ve-curation__table-wrap` for horizontal scroll on narrow admin windows; ellipsis on Label / Field name / Current value / VF context with hover `title="..."` for full text; decision radios in a flex-column div so row heights stay stable. **Enhancement 2 (dynamic filters, no reload)**: server no longer pre-filters the row set — every candidate renders into the DOM with filterable `data-*` attributes (`data-options-page`, `data-field-type`, `data-group-key`, `data-decision`, `data-recommendation`, `data-category`, `data-search`); `curation.js` filter engine matches rows and toggles `hidden` on each; runs on every select change, debounced 180ms on the label search input, re-runs on form submit (`preventDefault`'d — no page reload); reset button is a `<button type="button">` that clears filters client-side; live "Showing X of N candidates" chip in `aria-live="polite"`; when a row's decision or category changes via inline save, the JS updates that row's `data-*` attributes and re-applies filters so the row vanishes immediately if the active filter excludes it. Verification: `--filter "VisualEditorCuration"` **21 tests / 76 assertions OK** (unchanged — tests never exercised URL-param filtering). Curl against the LocalWP site confirmed the correct URL returns 200 while the doubled `addons/addons/...` URL returns 404 (proves the fix hit the exact bug pattern). PHP + JS syntax clean. No new files, no new tests, no changes outside the R3-BX namespace, no test count change | The curation admin page now loads its CSS + JS correctly on every request (auto-cache-busted), has stable column widths under `table-layout: fixed`, and filters instantly without page reloads. Fully operator-usable | R3/R3-BX |
| E-089 | 2026-08-24 | DBVC | `addons/visual-editor/src/Curation/{FieldCandidateProvider,FieldCurationRecommender,CurationStore,CurationExporter}.php` (new); `addons/visual-editor/src/Admin/CurationPage.php` (new); `addons/visual-editor/assets/{js/curation.js,css/curation.css}` (new); `addons/visual-editor/curation/README.md` (new); `addons/visual-editor/bootstrap.php` (`OPTION_CURATION_TOOL_ENABLED`, `is_curation_tool_enabled()`, settings group + meta, `register_admin_curation_page()`, `SETTINGS_VERSION` 4→5); `tests/phpunit/VisualEditorCuration{CandidateProvider,Recommender,Store,Exporter}Test.php` (new); `tests/phpunit/VisualEditorMediaManagerR1ATest.php` (single settings-version assertion updated 4→5 to reflect the legitimate ensure_defaults() bump); `docs/agents/manifest.json` (3 rotated extension-point ids + 1 admin surface + 3 ajax surfaces mapped) | R3-BX — Manual Approved Field Selection curation tool. Parallel phase (not a release gate) that lets the maintainer curate which options-page ACF fields become approved Brand Control Center controls. **Kill-switch gated** (`dbvc_visual_editor_curation_tool_enabled`, default off). Admin submenu **BCC Curation** under Settings → Visual Editor enumerates every options-page-owned ACF field (options-only scope by design — see D-059), rendering a filterable/sortable/bulk-selectable table with columns: label, field name path, type, options page, current value (via `get_field($key,'option')`), VF context (group title › tab labels › instructions), automated recommendation chip (`FieldCurationRecommender` keyword rules → include/ignore/defer/review with reasoning tooltip), category select, decision radio (include/ignore/defer), priority select, notes textarea. Inline saves + bulk actions go through three admin-ajax handlers (`manage_options` + nonce). Export writes `addons/visual-editor/curation/vertical-approved-controls.json` + companion `.md` — include-only records, shaped as the future `VerticalControlProvider::getControls()` seed, with per-record `unlocks_at` derived from field type so the exporter's `unlocks_summary` immediately shows R5 sequencing payoff. Never mutates content: reads ACF metadata + live option values only; writes only to `dbvc_visual_editor_curation_decisions`. `SharedGlobalFieldsController` untouched. `SETTINGS_VERSION` bump 4→5 tripped the R1-A media-manager test's version assertion (updated in place — legitimate bump because a new option participates in `ensure_defaults()`). Focused: `--filter "VisualEditorCuration"` **21 tests / 76 assertions OK**. Registry regression: `--filter "VisualEditorControlRegistry"` **11 OK unchanged**. Media Manager subset: `--filter "VisualEditorMedia"` **115 OK after the settings-version bump correction**. Full PHP suite: **831 tests** (810 + 21 new), same 6 inherited + 1 pre-existing dirty-tree `ProposalDiffContractTest` failure identity — **no new regression**. Agent docs: **54 curated / 436 discovered / 0 unmapped** (3 extension-point ids rotated due to bootstrap.php line-shift; 4 new ids mapped: `admin.menu.2ef573d2f42e` + three `hook.listener.wp_ajax_.*`) | The maintainer can now open Settings → Visual Editor → BCC Curation and manually approve which fields become Brand Control Center controls. Exported JSON is the seed a future `VerticalControlProvider` will consume verbatim; the MD companion is a PR-review-friendly grouping by R5 sequencing bucket. R3-B (Shared Globals compat provider) and R3-C (minimal center UI) are unblocked and independent | R3/R3-BX |
| E-088 | 2026-08-23 | DBVC | `addons/visual-editor/src/Registry/ControlProvider.php` (new); `ControlRecord.php` (new); `ControlRegistry.php` (new); `tests/phpunit/VisualEditorControlRegistryTest.php` (new); `docs/agents/manifest.json` (extension point mapped); tests/phpunit/VisualEditorMediaManagerR1DTest.php (two prior single-line source-string assertions loosened to regexes tolerant of prettier's multi-line call formatting) | R3 Slice R3-A — provider-agnostic control registry foundation. `ControlProvider` is a two-method interface (`getProviderId`/`getControls`). `ControlRecord::fromArray()` coerces loose provider output into a normalized value object, whitelisting `ownerType` to {option, post, term, user}, `fieldFamily` to {text, image, gallery, relationship, post_object, other}, and `status` to {available, inspect_only, unsupported, unavailable} — unknown values default to safe fallbacks (`option`/`other`/`unavailable`) so a misregistration cannot present a control as `available` by accident. `ControlRecord::toListItem()` is the safe list projection — deliberately excludes the internal `source` bag so the browser can never receive the provider's opaque target hint. `ControlRegistry::registerProvider()` rejects duplicate provider ids and invalid ids observably (via `dbvc_visual_editor_control_registry_invalid` — new `do_action` mapped in agent docs). `listControls($args)` filters by category → status → per-user visibility closure, in that order (cheap filters first, per-user visibility last), and returns deterministically sorted output (providerId asc, then id asc within a provider). `getVisibleRecord($publicId)` is the internal bridge that R3-C will use to hand a provider back its own opaque hint at open time; it fails closed on unknown / malformed / visibility-blocked ids. **No `Addon::register` wiring yet** — the registry has no provider to serve until R3-B adapts Shared Globals. **No REST surface, no UI, no mutation path** — the R3 plan's slice R3-A gate is "registry output is deterministic, filtered, and cannot introduce write authority". 11 PHP tests / 44 assertions verify: register + list; duplicate provider id rejected + observed; provider with invalid id rejected; duplicate local id within same provider rejected + observed; same local id across two providers coexists under distinct `publicId`s; malformed records skipped + observed; per-user visibility closure filters at list time (subscriber vs administrator); category/status filters prune before visibility; unknown owner/family/status normalize safely; empty registry lists nothing; `getVisibleRecord` fails closed on unknown/malformed/visibility-blocked ids. Combined PHP suite 810, six inherited failures + one pre-existing dirty-tree `ProposalDiffContractTest::test_entity_drawer_exposes_stable_mode_and_field_selectors` (the working tree's `src/admin-app/index.js` has drifted from the JSON-formatted assertion the test expects — not caused by R3-A, not fixed here per the "preserve dirty tree" constraint). Agent docs 54/432/0. Two R1D single-line source-string assertions (`createToolbarButtonMarkup('media-manager'`, `dispatchMediaManagerEvent('toggle'`) loosened to regexes tolerant of prettier's multi-line call formatting — an incidental fix, not part of R3-A itself | The R3 foundation is in place: providers can register through a documented DBVC extension point, records are normalized + validated + deduplicated, visibility filtering is per-user, and the registry cannot introduce write authority. This unblocks R3-B (adapt Shared Globals as a provider) and R3-C (minimal center UI + open route) | R3/Slice R3-A |

| E-087 | 2026-08-22 | DBVC | `dbvc-codexchanges.local` live site via a paired Chrome extension (Claude for Chrome) driving the user's already-authenticated tab; `assets/js/media-frame-factory.js` (`DBVCVisualEditorMediaFrame.createMediaFrame`/`disposeFrame`); `assets/js/overlay-app.js` + `assets/js/media-manager-app.js` (both routed through the factory since RK-011 Slice 1); `MediaIndexController` `GET .../index` + `POST .../index/expand` (Slice 2b/2c); real Backbone `wp.media` frames | R2-H Slice 2c frontend flip + RK-011 Slice 1/2 single-active-frame discipline — LIVE real-browser evidence. Confirmed on page load: `DBVCVisualEditorBootstrap.active="1"`, `mediaManager.enabled=true`, `supportsWpMedia="1"`, `canUpload="1"`, `window.DBVCVisualEditorMediaFrame` global present (Slice 1 factory in production configuration), `window.DBVCVisualEditorMediaManager` controller present. **Slice 2c flip live**: `DBVCVisualEditorMediaManager.open()` loaded the panel with `source: "index"` (not `scan`), 20 rows via `GET .../index`, dialog heading "20 entities with missing media from the site index" — the durable index is the live read source on this real site. **Slice 2b detached snapshot live**: expanding a `vemx_` list row produced an `expansion.groupRef = "vemg_20fbfb324b6bfe08d9d0"` distinct from the list `itemKey = "vemx_..."` — the working expansion identity is decoupled from the list row key exactly as the client contract specifies. **RK-011 Slices 1 + 2 discipline live on real Backbone frames**: a probe wrapped `window.wp.media` and 3 consecutive `factory.createMediaFrame({previousFrame: current})` calls produced 3 total constructed frames, at any moment **≤1 live**, 2 auto-disposed via the factory's `previousFrame` teardown (idempotent `frame.remove()` chain). A final `factory.disposeFrame(current)` cleaned the last one (final: 4 total constructed across the run, 0 live, 4 disposed, 0 `.media-modal` DOM nodes). **Real Media Library modal rendered**: opening a factory-produced frame showed the actual WP media modal with title flowed through from `options.title`, both "Upload files" and "Media Library" tabs present (upload capability confirmed), backdrop layered over the Visual Editor panel, cancel button labeled from `options.buttonText` — proving the factory produces working frames end-to-end on real WordPress, not just a shape that satisfies jsdom mocks. **Zero content mutations**: no attachment uploaded, no assignment saved, no LocalWP option toggled; every frame was disposed without a select event firing. **Residual D-049 pieces still open** (not blocked, just not run in this session): real Safari (Claude for Chrome only drives Chrome; WebKit engine automation is not Safari-proof), real AT / VoiceOver, a completing aggregate JS lint. jsdom 43, PHP suite 799 with the same six inherited failures, agent docs 54/431/0 (docs-only change) | The R2-H Slice 2c frontend flip AND the RK-011 shared-factory + single-active-frame discipline are now empirically proven against a real WP site with real Backbone `wp.media` frames — the jsdom evidence that these behaviors hold in isolation is confirmed to hold end-to-end. The narrowly-remaining D-049 pieces (Safari, AT, aggregate lint) are documented residuals, not open browser-frame lifecycle uncertainty | R2-H Slice 2c + RK-011 Slices 1–2 live real-browser evidence |

| E-086 | 2026-08-22 | DBVC | `dbvc-codexchanges.local` LocalWP site; `docs/development/local-agent-auth.md` (new reference doc); `~/.config/dbvc-local-agent.env` (per-machine, 600-perm, values outside repo); authenticated Basic-Auth via a WP application password `agentadminuser1:<app-password>`; `MediaManagerController` + `MediaIndexController` routes (`scans`, `scans/latest`, `scans/{ref}/next`, `scans/{ref}/groups/{group_ref}`, `.../descriptor`, `.../assignment`, `index`, `index/expand`) | R2-E4 kill-switch + R2-E2 fail-closed + D-053/R2-H Slice 2b index end-to-end — authenticated live-REST evidence on the real WordPress site (previously blocked as D-049 residual for "no already-authorized session"). **Kill switch (unauth)**: `GET .../scans/latest`, `GET .../index`, `POST .../scans`, and `POST .../scans/vems_forged.../next` all returned `401 rest_forbidden` **before resolution** (no snapshot created). **Auth without VE mode cookie**: `GET .../scans/latest` returned `403 media_manager_mode_inactive` (the mode gate closes even for a live app-password-authenticated administrator). **Auth + VE mode cookie**: `GET .../index` returned 200 with the current serving generation and a real `items` payload (24 scan chunks drained `454/454` candidates and `866` findings; the completion action populated the index via `refreshFromSnapshot`, and the subsequent index list showed 5 rows sorted `entity_asc` — labels "Accepted Insurance", two "Accessibility Auditing"). **No raw-target leak on live data** — every item response was inspected: `ownerId`, `fieldKey`, `selector`, `path` all absent from list items AND from field projections in the expanded response. **Slice 2b detached snapshot proven live**: `POST .../index/expand` with a real `vemx_d1a864ac3c4434481c9361fc` ref returned live detail with a scan ref `vems_h5pnuukjdldyj3oja73i` **DISTINCT** from the full-scan ref `vems_okstcx0rcesdmbttw04c` — proving the per-entity snapshot is detached and does not clobber `loadLatest`. **Fail-closed live**: `.../descriptor` with a forged `vemf_ffffffffffffffffffff` → 404 `media_finding_unavailable`; with a stale generation → 409 `media_scan_generation_mismatch`; `.../assignment` with a non-numeric `attachmentId` → 400 `media_assignment_value_invalid` — none of the three triggered a write. **Query parity live**: `.../index?fieldFamily=acf_image` returned filtered rows and echoed the normalized query. **What this proves**: the R1-E authenticated REST/table-data residual is now empirically closed on this site (previously accepted as documented risk); the R2-E2 stale/permission/malformed-value fail-closed paths are proven live; the Slice 2b detached snapshot contract is proven live; and the D-053 no-raw-target-leak read-time contract holds against real content. **Residual now narrows to browser-side**: real Media Library `wp.media` open/upload/focus, keyboard flows, repeated-open memory/listener profiling over long sessions, real Safari (WebKit engine automation is not Safari-proof), and real AT (VoiceOver etc.). Those still require an interactive browser login and are recorded as remaining D-049 residual. Working tree preserved; no repo mutations; no LocalWP option toggling. The auth env file (`~/.config/dbvc-local-agent.env`) is chmod 600, outside the repo, and per the reference doc must not be committed | The R1-E "authenticated active-site REST and table data behavior against real WordPress responses" residual gate — accepted as documented risk since 2026-08-16 — is now empirically closed. Kill switch, fail-closed, Slice 2b detached snapshot, and D-053 no-leak all proven live against `dbvc-codexchanges.local`. Only browser-side D-049 pieces remain | R1-E/R2-E2/R2-H Slice 2b live REST evidence |

| E-085 | 2026-08-19 | DBVC | `overlay-app.js:929` (`bindMediaFramePrefetchState` read); `overlay-app.js:302`/`:332`/`:738` (`clearViewportPrefetchSchedule`/`state.mediaModalOpen`/`scheduleViewportPrefetch` cross-references); `media-manager-app.js` (searched `prefetch`/`scheduleViewportPrefetch` — none); `WP-MEDIA-FACTORY-CONSOLIDATION-RK-011.md` (Slice 3 section rewritten to close-as-N/A); `RISK-REGISTER` RK-011 row (Slice 3 status recorded); canonical handoff (Slice 3 removed from next-tasks); no code change | RK-011 Slice 3 — investigated and **closed as not applicable**. The overlay's `bindMediaFramePrefetchState` is overlay-internal: it mutates `state.mediaModalOpen` (overlay's own layering flag used at `overlay-app.js:332` to pause the overlay's descriptor prefetch while a media modal is up) and drives `clearViewportPrefetchSchedule` / `scheduleViewportPrefetch` (overlay's descriptor-viewport prefetcher for visible field markers on the page). Grep of `media-manager-app.js` for `prefetch` / `scheduleViewportPrefetch` returns no matches — the Media Manager has no descriptor-marker prefetcher, only its own scan/list lifecycle. Moving the hook into the shared factory would either break the Media Manager (touching overlay-only state) or be a no-op there. The "prefetch parity" premise did not survive reading the actual code. Scope for the shared factory ends at frame configuration + disposal (Slice 1) and single-active-frame discipline (Slice 2). RK-011 is fully mitigated by Slices 1 + 2; D-049 real-browser memory profiling remains the unchanged residual gate. **D-049 empirical validation** — not run: this environment has no already-authenticated session on `dbvc-codexchanges.local`, and the recorded process constraints prohibit logging in, entering credentials, or changing LocalWP state to satisfy a test. Full suite 799 with the same six inherited failures (no code changed); jsdom 43; agent docs 54/431/0 | Closes RK-011 as a work item — a shared prefetch hook is not the right abstraction; the overlay's descriptor prefetch stays overlay-owned by design. The honest close-out documents the finding rather than shipping a phantom change | R2/RK-011 Slice 3 (closed N/A) |

| E-084 | 2026-08-19 | DBVC | `overlay-app.js` (image + gallery controllers: routed through the factory with `previousFrame` chaining, new `destroy()` hook disposes the active frame); `visual-editor-media-manager-state.test.cjs` (Slice 2 discipline case, 43 jsdom total) | RK-011 Slice 2 — overlay adopts the single-active-frame discipline the Media Manager side already enforces (R2-E3). Overlay image controller previously memoized its `mediaFrame` in the closure across opens; overlay gallery controller re-created a fresh frame per open without ever disposing it — both are the same leak pattern R2-E3 closed on the Media Manager side. Both controllers now pass `previousFrame: mediaFrame` to `DBVCVisualEditorMediaFrame.createMediaFrame()` (so re-opens dispose the prior frame), track a single `mediaFrame` reference at controller scope, and add a `destroy()` hook — `destroyActiveController()` (already called by `overlay-app.js:5810` on panel close / re-open / descriptor swap) tears down the frame via `DBVCVisualEditorMediaFrame.disposeFrame`. A factory-level jsdom test simulates the Slice 2 pattern (3 opens with `previousFrame` chaining) and proves ≤1 live frame throughout (2 disposed) and a final `disposeFrame` leaves 0. Zero REST/mutation change; existing 34 jsdom cases + 4 factory cases from Slice 1 + this 1 = 43 pass; media-manager lint clean; combined Media Manager PHP 115 tests; full suite 799 with the same six inherited failures; agent docs 54/431/0. Residual: real-browser memory/listener profiling over long sessions (D-049) | The overlay's image and gallery `wp.media` frames can no longer accumulate across opens or leak past panel close — closes the last known leak on the overlay side that pre-dated the R2-E3 fix on the Media Manager side | R2/RK-011 Slice 2 |

| E-083 | 2026-08-19 | DBVC | `assets/js/media-frame-factory.js` (new); `Assets/AssetLoader.php` (enqueue with dep); `assets/js/overlay-app.js` (image + gallery controllers rewritten to reach `wp.media` through the factory); `assets/js/media-manager-app.js` (`openAssignFrame` rewritten to reach `wp.media` through the factory); `VisualEditorMediaManagerR1DTest` (contract updated to assert factory reach-through instead of the direct `window.wp.media(` call); `visual-editor-media-manager-state.test.cjs` (harness loads the factory + 4 new factory cases) | RK-011 Slice 1 — shared `wp.media` frame factory extracted, behavior-neutral. New file `assets/js/media-frame-factory.js` exposes `window.DBVCVisualEditorMediaFrame.createMediaFrame({mode, title, buttonText, previousFrame})` → `{frame, dispose}` — one construction site for the whole addon. `AssetLoader` enqueues it as a shared dependency of both `overlay` and `media-manager` (same wiring pattern as `api-client.js`). All three previous `wp.media(...)` construction sites (overlay image at ~6942, overlay gallery at ~7328, media-manager `openAssignFrame` at ~3440) now go through the factory; selection handling stays owned by each caller by design (the two modules have different domain models — overlay: full descriptor entry via `mapMediaSelectionToGalleryItem`; media-manager: `{id,url,alt,title}` via `normalizeAttachmentSelection`). The R1-D contract check was updated to assert the factory reach-through (`DBVCVisualEditorMediaFrame`, `createMediaFrame`, `supportsWpMedia`) instead of the raw `window.wp.media(` string, preserving the intent (native Media Library staging, not an ad-hoc uploader) while accurately reflecting the new architecture. Verified: 4 new jsdom cases prove single-image config synthesis, ordered-multiple config synthesis, `previousFrame` disposal (idempotent), and null-frame fallback when `wp.media` is unavailable; existing 38 jsdom cases + 4 new = 42 pass; media-manager lint clean; combined Media Manager PHP 115 tests; full suite 799 with the same six inherited failures; agent docs 54/431/0 (the `dbvc_visual_editor_media_index_list_enabled` extension-point hash refreshed after the AssetLoader edit) | The overlay and the Media Manager stop drifting on `wp.media` construction — the factory is the single source of truth for frame configuration, disposal helper, and feature-detect fallback — with the caller-owned selection handling preserved to keep the two domain models decoupled | R2/RK-011 Slice 1 |

| E-082 | 2026-08-19 | DBVC | `MediaIndexJsonExporter` (new; `exportAll`/`importIfEmpty`); `MediaIndexBuilder` (`dbvc_visual_editor_media_index_build_completed` action on final chunk); `MediaIndexReconciler` (`dbvc_visual_editor_media_index_reconciled` action when a sweep touched rows); `Addon::register` wiring (subscribes exporter to both completion hooks + guarded import on bootstrap); `VisualEditorMediaIndexJsonExportTest` | R2-H Phase 1 Slice 5 — derived JSON mirror in the DBVC sync folder (backup portability). `MediaIndexJsonExporter::exportAll()` writes an envelope `{schema:1, exported_at, source, generation, count, entities}` to `{sync}/visual-editor/media-index.json` — only the SERVING generation is exported (rebuilds-in-flight are internal state), each entity carries identity + cached label/URL + counts + family breakdown + content_hash + indexed_at + is_dirty, and the envelope-level `generation` is authoritative (per-row `index_generation` column is omitted from exported entities). Writes honor `dbvc_is_safe_file_path` and `wp_mkdir_p` and no-op silently on any failure. `MediaIndexBuilder` fires `dbvc_visual_editor_media_index_build_completed($generation, $rebuild_active)` once per build (initial or rebuild swap); `MediaIndexReconciler` fires `dbvc_visual_editor_media_index_reconciled($processed)` when a sweep actually touched rows; `Addon::register` subscribes the exporter to both so the mirror refreshes at completion boundaries without per-invalidator churn. `importIfEmpty()` is the guarded restore path — when the table is empty AND the mirror file exists AND validates (schema match, well-formed `vmig_` generation, non-empty entities), it sets `OPTION_GENERATION` to the file's generation, upserts every row, and marks the builder state complete for that generation (so the scheduler drain does not re-fire immediately after a restore). A no-op when the table has any rows. Two new `do_action`s let integrators observe: `dbvc_visual_editor_media_index_exported($file_path, $count)` and `dbvc_visual_editor_media_index_imported($file_path, $imported)`. Verified (5 tests/41 assertions): export writes a well-formed envelope with only serving-gen rows (a stray non-serving row is filtered) and no per-row `index_generation`; import populates an empty table + marks the build state complete for the imported generation; import is a no-op when the table has rows; a round-trip (export → deleteAll → import) preserves identity + `entity_ref` (refs are HMAC-derived from identity + generation, which is preserved); export does not throw when the safety check filters an unsafe base. Combined Media Manager PHP 115 tests; full suite 799 with the same six inherited failures; jsdom 38; agent docs 54/431/0 (four new `do_action` extension points + two now-discovered option keys mapped). **R2-H Phase 1 complete** | The durable index is now backup-portable — the DBVC sync folder carries a self-describing JSON mirror that hydrates a restored site whose table is empty, closing the last Phase 1 gap in D-053 (custom table is source of truth; JSON is a derived, importable mirror) | R2-H/Slice 5 |

| E-081 | 2026-08-19 | DBVC | `MediaIndexStore` (schema v2, `hasActiveRebuild`/`buildingGeneration`/`activeBuildGeneration`/`beginRebuild`/`completeRebuild`/`abortRebuild`, gen-scoped `entityRowId`/`upsertEntity`, serving-preferred `getEntity`/`getByEntityRef`); `MediaIndexBuilder` (target `activeBuildGeneration`, call `completeRebuild` on drain finish); `MediaIndexInvalidator` (dual-write to serving + building; `onAttachmentDeleted` also marks building dirty); `MediaIndexRebuildController` (new, MM-gated); `Addon::register` wiring; `VisualEditorMediaIndexRebuildTest` | R2-H Phase 1 Slice 4b-2 — topology/exclusion rebuild triggers + atomic swap. `MediaIndexStore` splits "current generation" in two: `currentGeneration()` is the SERVING generation (unchanged for reads); the new `OPTION_BUILDING_GENERATION` holds the BUILDING generation while a rebuild is in flight. Schema v2 extends the unique key to `(entity_type, entity_id, entity_subtype, index_generation)` so a per-entity building row coexists with its serving counterpart mid-rebuild. `beginRebuild()` mints a fresh building generation (dropping any orphaned rows from a prior in-flight rebuild); `completeRebuild()` atomically swaps the serving pointer and prunes every other generation. `MediaIndexBuilder` targets `activeBuildGeneration()` so a rebuild writes into building without touching serving; when its final chunk completes AND a rebuild was in flight, it calls `completeRebuild()` — the swap and prune happen in one step. `MediaIndexInvalidator::reindexEntity` dual-writes into serving AND building during a rebuild so a mid-rebuild save survives the swap; `onAttachmentDeleted` flags both generations dirty for the post-swap reconcile. The new `MediaIndexRebuildController` (MM-gated in `Addon::register`) wires the trigger surface: `acf/update_field_group`, `acf/delete_field_group`, `acf/trash_field_group`, `acf/untrash_field_group`; `update_option_dbvc_visual_editor_excluded_post_types` / `_excluded_taxonomies` (plus add/delete siblings); and a `wp_loaded` (priority 20) topology fingerprint over the sorted set of public+show-UI post types and taxonomies plus current exclusion lists — a drift triggers a rebuild, catching post-type/taxonomy (de)registration without firing on every request. Triggers are non-clobbering: a call while a rebuild is already running is a no-op, and `dbvc_visual_editor_media_index_rebuild_skipped` fires so integrators can observe missed events; `dbvc_visual_editor_media_index_rebuild_started` fires when a fresh rebuild begins. Verified (`VisualEditorMediaIndexRebuildTest`, 4 tests/40 assertions): a rebuild keeps serving the OLD generation until completion (mid-drain reads still return the complete list, and building rows coexist without clobbering serving); each trigger surface begins a rebuild while a concurrent trigger is a no-op; the topology fingerprint primes silently on first call, is stable when nothing changes, drifts on post-type registration → rebuild, and returns to idle after the drain; and the invalidator dual-writes into both generations so a mid-rebuild save survives the swap. Combined Media Manager PHP 110 tests; full suite 794 with the same six inherited failures (a seventh, `ContentCollectorV2Phase8Test`, is order-dependent flaky in mixed runs and passes in isolation — NOT a regression); jsdom 38; agent docs 54/425/0 (two new `do_action` extension points mapped) | Any topology/exclusion change now atomically rebuilds the durable index without ever letting reads observe a half-built copy — reads keep serving the complete OLD generation until the fresh one is fully built, then the pointer swaps and the old is pruned; in-flight edits during the rebuild are captured by the dual-write invalidator so they survive the swap | R2-H/Slice 4b-2 |

| E-080 | 2026-08-19 | DBVC | `EligibilityPolicy` (`$structural` mode); `MediaIndexBuilder` (new); `MediaIndexScheduler` (`BUILD_HOOK` drain); `MediaIndexProjector::onScanRefreshed`/`refreshFromSnapshot`; `Addon` (structural pipeline + hook rewire); `VisualEditorMediaIndexBuilderTest` | R2-H Phase 1 Slice 4b-1 (structural cross-user first-run build). Fixes a correctness gap: the Slice 2 completion hook only populated the index from ONE user's on-demand scan, so the shared, cross-user index held only that user's editable set. `EligibilityPolicy` gains a `$structural` mode that skips **only** the per-object capability check (status/public/show-UI/exclusions preserved); the read path keeps the capability-enforcing policy, so the D-053 read-time-authority contract is intact. `MediaIndexBuilder` runs its own structural pipeline to enumerate every structurally-eligible entity via `ScanCandidateProvider` in bounded chunks — scanning each with an ad-hoc `vmsg_` scan generation and upserting rows under the `vmig_` index generation — persisting a `{generation, cursor, status}` build-state option across runs and marking complete when the cursor is exhausted. `MediaIndexScheduler` drains the build as a self-continuing chain (Action Scheduler async action when available, else WP-Cron single-event re-armed each incomplete chunk via `BUILD_HOOK`), stopping on completion. Because the builder is now authoritative, the manual-scan completion hook is rewired `onScanCompleted` (rotate fresh capability-limited generation + prune) → `onScanRefreshed`/`refreshFromSnapshot` (upsert scanned entities into the current generation, no rotate/prune) so an on-demand scan refreshes-in-place without clobbering the cross-user index. First build fills the current empty generation directly; atomic build-into-fresh-generation-then-swap for topology/exclusion rebuilds is Slice 4b-2. Verified (5 tests/26 assertions): a structural build indexes posts the acting subscriber cannot edit while a capability-scoped build indexes none (proving cross-user structural enumeration); the build advances across multiple small chunks with the cursor persisted and completes (all entities indexed); the `needsBuild`/`reset` lifecycle; a manual-scan refresh leaves the generation and an untouched entity's row intact (no clobber); the scheduler arms the WP-Cron drain, drives it to completion, and then stops. Combined Media Manager PHP 106 tests; full suite 790 with the same six inherited failures; agent docs 54/423/0 (the build hook is a cron listener, not a discovered do_action surface). Remaining: Slice 4b-2 (rebuild triggers), Slice 5 (JSON export) | The durable index now builds itself automatically on first run and is complete for ALL users (not just the first scanner), while a manual scan refreshes it without clobbering — closing the cross-user population gap | R2-H/Slice 4b-1 |

| E-079 | 2026-08-19 | DBVC | `media-manager-app.js` (index source: `source`/`expansion.itemKey`/`expansion.scan`, `loadIndex`, `commitIndexPayload`, index `expandGroup`, `openInitialSource`); `api-client.js` (`index`/`indexExpand`); `AssetLoader` (`indexList` flag + strings); `VisualEditorMediaManagerR1DTest`; `visual-editor-media-manager-state.test.cjs` (+4 index cases) | R2-H Phase 1 Slice 2c (frontend flip). Gated behind a filterable bootstrap flag `mediaManager.indexList` (`dbvc_visual_editor_media_index_list_enabled`, default = Media-Manager-enabled; **absent in existing fixtures so the scan path is unchanged**). When on, the Manager **opens from `GET .../index`** (instant, read-time per-user filtered) rendering `vemx_`-keyed rows, and **expands via `POST .../index/expand`**: the response's detached snapshot becomes the per-expansion working identity (`expansion.scan` + working `vemg_` group) while the list row stays keyed by the opaque `vemx_` ref, so the existing descriptor/assign/replace flow drives mutation **unchanged** and a save reconciles the row in place with no list reload. **Automatic fallback to the ephemeral scan** when the index request fails or returns no rows (index not yet built), and "Start new scan" returns to the scan source. Search/entity/field/sort + offset paging route through the index in index mode. Verified (jsdom): index open sets `source='index'` and never calls `/scans`; index-expand adopts the detached snapshot (itemKey `vemx_`, working group `vemg_`) and a subsequent save **posts to the detached scan ref** and reconciles the `vemx_` row in place without an index reload; index error → scan fallback (`/scans/latest`); empty index → scan fallback. jsdom 38/38 (34 scan-mode cases unchanged); media-manager lint clean; R1-D read-only invariant updated for the `expandGroup` rename and passing; combined Media Manager PHP 101 tests; full suite 785 with the same six inherited failures; agent docs 54/423/0 (new filter hook mapped). Remaining: Slice 4b (first-run build), Slice 5 (JSON export) | The Media Manager opens instantly from the durable, per-user-filtered index and remediates in place through a detached snapshot, with a safe automatic fallback to the scan — the user-visible payoff of the R2-H plumbing | R2-H/Slice 2c |

| E-078 | 2026-08-19 | DBVC | `MediaIndexStore` (`buildFilter` search/entity-family, `orderByClause`); `MediaIndexReadModel::getList` (query normalize + `fieldFamily` + echo); `MediaIndexController::handleIndexList`; `VisualEditorMediaIndexQueryTest` | R2-H Phase 1 Slice 2c (server — index list query parity, behavior-neutral). The durable index list now honors the **same query surface as the ephemeral scan list** so the Slice 2c flip loses no filter control: `search` (label `LIKE`, `esc_like`-escaped) and `entityFamily` (`entity_type` = post/term) filter in SQL; `sort` maps to a whitelisted `ORDER BY` (entity/missing/scanned asc·desc); `fieldFamily` is applied after hydration against the per-family counts; and `getList` echoes the normalized query for control sync. Read-time per-user eligibility filtering is unchanged and still authoritative. No UI wiring yet (the flip is E-079). Verified: search matches only the label substring; entityFamily separates posts and terms; fieldFamily returns only the matching-family rows; entity/missing sorts order correctly; and a bogus sort/family/whitespaced search is normalized in the echo. 5 tests/14 assertions; full suite 785 with the same six inherited failures; agent docs 54/422/0 | The durable index is filterable/sortable/searchable exactly like the scan list, so flipping the Manager's source will not regress any filter control | R2-H/Slice 2c |

| E-077 | 2026-08-18 | DBVC | `MediaIndexStore` (`entity_ref`/`getByEntityRef`); `MediaScanCoordinator::snapshotEntity`; `ScanSnapshotStore::createDetached`; `Rest/Controllers/MediaIndexController` (+ `Routes` wiring); `VisualEditorMediaIndexControllerTest` | R2-H Phase 1 Slice 2b — index-backed read plumbing (server only). `MediaIndexStore` now derives a stable opaque `entity_ref` (`vemx_[a-f0-9]{24}` = HMAC of the entity identity under the current index generation) at upsert time and exposes `getByEntityRef` (a forged/stale ref resolves to nothing). `MediaScanCoordinator::snapshotEntity` builds a **complete, detached** single-entity snapshot via `ScanSnapshotStore::createDetached` — user/blog-bound and loadable by its `scan_ref`, but it never updates the user's "latest scan" pointer, so it cannot disturb a real full scan. The new MM-gated `MediaIndexController` adds `GET .../media-manager/index` (read-time-filtered list) and `POST .../media-manager/index/expand`, which resolves an opaque ref, **re-checks `EligibilityPolicy` for the requesting user**, builds the detached snapshot, runs the existing `expandGroup`, and returns the live detail plus the snapshot's `vems_` scan/group refs so the existing assign/replace routes drive mutation unchanged. No UI behavior changes — the frontend still uses the ephemeral snapshot; the flip is Slice 2c. Verified: the ref is opaque and store-only-resolvable (forged ref → null); index-expand returns live `fields` with a usable `vems_` scan ref **without clobbering `loadLatest`**; a forged ref → 404 `media_index_ref_unavailable`; an entity the user can no longer view → 403 `media_index_forbidden`; the list returns the filtered index. 5 tests/40 assertions; full suite 780 with the same six inherited failures; agent docs 54/422/0 (two new index routes mapped) | The durable index is safely readable end-to-end through REST — an opaque, per-user-eligibility-checked ref expands to live detail via a detached snapshot that never disturbs the user's full scan — with the user-visible flip deferred to Slice 2c | R2-H/Slice 2b |

| E-076 | 2026-08-18 | DBVC | `MediaIndexReconciler`; `MediaIndexScheduler`; `MediaIndexInvalidator::onAttachmentDeleted`; `Addon::register` wiring (MM-gated); `VisualEditorMediaIndexReconcileTest` | R2-H Phase 1 Slice 4 — background reconcile + scheduler. `MediaIndexReconciler::run()` re-indexes a bounded chunk of dirty entities via the invalidator's single-entity rescan (each upserted afresh, clearing dirty, or removed if no longer indexable). `onAttachmentDeleted` (hooked `delete_attachment`) flags the whole current generation dirty (an attachment deletion can empty any referencing field; a targeted reverse lookup is deferred), so the reconcile recomputes affected rows over time. `MediaIndexScheduler` registers the reconcile callback and schedules the recurring job via **Action Scheduler when available, else stock WP-Cron** — proven by the test using the WP-Cron fallback (AS absent in the env). Verified: reconcile re-indexes dirty + clears the flag; reconcile removes a dirty entity that lost eligibility; attachment deletion flags the generation dirty; the scheduler registers and clears the recurring job. 4 tests/14 assertions; full suite 775/8,935 with the same six inherited failures; agent docs 54/420/0. Deferred to Slice 4b: automatic first-run chunked full build + ACF-topology/exclusion rebuild triggers | The durable index self-heals in the background — dirty entities are reconciled on a schedule and attachment deletions are swept — on any site via Action Scheduler or WP-Cron | R2-H/Slice 4 |

| E-075 | 2026-08-18 | DBVC | `MediaIndexInvalidator`; `MediaIndexProjector::indexGroup` (extracted); `MediaIndexStore::deleteEntityById`; `Addon::register` wiring (MM-gated); `VisualEditorMediaIndexInvalidatorTest` | R2-H Phase 1 Slice 3 — incremental invalidation. `MediaIndexInvalidator` subscribes to `save_post`/`trashed_post`/`deleted_post`/`edited_term`/`delete_term` and keeps just the affected entity current between full scans: on a save/term edit it runs a bounded single-entity rescan and upserts the row into the current index generation (via the extracted `MediaIndexProjector::indexGroup`); an entity no longer indexable (unpublished, excluded, deleted, or without supported media fields) has its row removed (`deleteEntityById` handles the hard-delete path where the subtype is gone). Autosaves/revisions skipped. Index membership uses the same eligibility as the building scan; the per-user read-time filter still applies on read. Verified: re-index lowers the missing count when a field is filled (2→1) and changes the content hash; `save_post` re-indexes; an unpublished entity is removed; a deleted post's row is removed; a term edit re-indexes. 5 tests/13 assertions; full suite 771/8,921 with the same six inherited failures; agent docs 54/420/0. Deferred to Slice 4 (documented): attachment-deletion reverse lookup, ACF field-group topology, exclusion-option changes, and direct-metadata writes not firing `save_post` | The durable index self-maintains per entity on the common admin edit/delete paths without a full rebuild; broader invalidation pairs with the background reconcile | R2-H/Slice 3 |

| E-074 | 2026-08-18 | DBVC | `MediaScanCoordinator` (`dbvc_visual_editor_media_scan_completed` action); `MediaIndexProjector`; `MediaIndexReadModel`; `Addon::register` wiring (MM-gated); `VisualEditorMediaIndexPhase2Test` | R2-H Phase 1 Slice 2 — index population + read-time filtered read model. The coordinator fires a `media_scan_completed` action on completion; `MediaIndexProjector` rebuilds the index from the completed snapshot under a fresh generation (per-entity identity, cached label/url, missing count, per-family breakdown, content hash; prior generation pruned) — write-only, storing no field keys/selectors/paths. `MediaIndexReadModel::getList` lists missing-media-first and enforces the shared-index security contract: for every row it re-resolves the entity and re-runs `EligibilityPolicy` for the **current user** and drops any row that user may not see (stored row is never authority). Verified: population from a real completed scan + the completion-hook path; eligible listing with no raw-target leak; an entity unpublished after indexing filtered out at read time while its shared row is retained; a subscriber sees zero rows. 5 tests/44 assertions; full suite 766/8,908 with the same six inherited failures; agent docs 54/420/0 (new completion-action hook mapped). The live REST/frontend list flip is deferred to Slice 2b (ref/generation unification) — the index is built and safely readable but the interactive path still uses the ephemeral snapshot | The durable index is populated from completed scans and can be listed safely with per-user read-time filtering on a shared store; the interactive read source is not yet flipped | R2-H/Slice 2 |

| E-073 | 2026-08-18 | DBVC | `MediaIndexStore` (`{prefix}dbvc_ve_media_field_index` table); `VisualEditorMediaIndexStoreTest`; `releases/R2H-PERSISTENT-MEDIA-INDEX-PHASE-1.md`; D-053 | R2-H Phase 1 Slice 1 — durable Media Index storage layer. New custom table (schema-versioned via `maybeUpgrade`/`dbDelta`) + `MediaIndexStore` repository: `upsertEntity` (insert or in-place update keyed by entity identity), `getEntity`, filtered `listEntities`/`countEntities` (generation/onlyMissing/onlyDirty + bounded pagination), `markDirty`/`markGenerationDirty`, `deleteEntity`/`pruneOtherGenerations`/`deleteAll`, and opaque `vmig_…` generation rotation; inputs sanitized, reads prepared. Named `media_field_index` to avoid confusion with the pre-existing core DBVC `dbvc_media_index` attachment-file table (different purpose: this indexes media-*field* completeness of entities). Storage-only and behavior-neutral: `register()` is not wired into the bootstrap and no Media Manager path reads/writes the index yet (Slice 2 wires population + read-time listing with per-user eligibility filtering). 6 tests/32 assertions; full suite 761/8,864 with the same six inherited failures; agent docs 54/419/0 (new table mapped). Backup portability (JSON export), incremental invalidation, and background build/reconcile are later slices | The persistent index has a durable, tested per-entity store; it changes no current behavior until Slice 2 flips the read source | R2-H/Slice 1 |

| E-072 | 2026-08-18 | DBVC | `VisualEditorMediaManagerR2E4Test`; `DBVC_Visual_Editor_Addon::is_media_manager_enabled`; `MediaManagerController::canAccess`; `releases/MEDIA-MANAGER-RELEASE-NOTES-AND-ROLLBACK.md` | R2-E4 feature isolation + ship-readiness. Proves the Media Manager kill switch gates the whole remediation surface, not just assets: `is_media_manager_enabled()` requires BOTH the Visual Editor master flag and the feature flag; the REST permission gate `canAccess` (the permission_callback for every route) is closed when the MM flag is off, the master flag is off, the user lacks the base capability, or the request is logged out — and open only when all hold. 4 tests/10 assertions; combined Media Manager PHP 71 tests. Added a consolidated release-notes/rollback runbook (feature summary R1→R2, gates/isolation, side-effect boundaries, non-destructive rollback: disable the flag → surface off with existing assignments intact; no MM-specific schema migration; per-change revert via the journal). Full suite 755/8,832 with the same six inherited failures; agent docs 54/418/0. R2-E (E1–E4) is complete; real-browser/AT QA remains the standing residual gate (D-049) | The Media Manager kill switch is proven end-to-end (routes closed when disabled/uncapable/logged-out), and the ship-readiness release-notes/rollback runbook is documented | R2/R2-E4 |

| E-071 | 2026-08-18 | DBVC | `MediaScanReadModel::expandGroup` (now-populated → `projectAssignedField`); `VisualEditorMediaManagerR2CTest`; `VisualEditorMediaManagerR1CTest`; `visual-editor-media-manager-state.test.cjs` (`reconciledRow` + R2-C save case) | R2-F Slice 4 / D-052 fix for the field report on the "Agencies" CPT: after assigning a previously-missing field, the reconcile projected it as a terminal `resolved_or_changed` ("refresh the scan") with a preview but no `valueRef`, so no replace control rendered and no assign control either — a dead end affecting every just-assigned family. Fix: `expandGroup` projects an original finding that is now populated (absent from the current missing findings and present in `assigned_by_ref`) as an **`assigned`** field via `projectAssignedField` (valueRef + `replace: true`), incrementing `populated` not `resolvedOrChanged`; the genuinely gone/ineligible case keeps `resolved_or_changed`. Read-model projection only — no new REST/mutation/capability surface; the just-assigned field's `valueRef` comes from the same live rescan as any assigned field, so the Slice 3 expected-current-value precondition still gates replace. Verified: R2-C PHP asserts a just-assigned featured/ACF-image field reconciles as `assigned` with a `vemv_` valueRef + `replace: true`; R1-C asserts a now-populated field counts as `populated` (not `resolvedOrChanged`); the R2-C jsdom save case asserts the field is `assigned` and immediately offers a `replace-media` control with the row still marked resolved (missing 0). Full suite 751/8,822 with the same six inherited failures (one order-dependent flaky ContentMigration test unrelated to this change passes in isolation); jsdom 34/34; media-manager lint clean; agent docs 54/418/0 | A field you just assigned is immediately replaceable (valueRef + replace control) with no "refresh the scan" dead end and no rescan; genuinely gone fields still prompt a refresh | R2-F/Slice 4 |

| E-070 | 2026-08-18 | DBVC | `media-manager-app.js` (`disposeActiveFrame`, single-active-frame lifecycle in `openAssignFrame`/`resetExpansion`/`expandGroup`/`close`); `visual-editor-media-manager-state.test.cjs` (4 R2-E3 jsdom cases + frame-disposal mock) | R2-E3 repeated-remediation frame lifecycle + surgical DOM patch (presentation only, no REST/mutation change). Root issue (RK-011): `openAssignFrame` created a new `wp.media` frame on every open and never disposed it, accumulating frames/listeners over repeated remediation. Fixed to a single-active-frame lifecycle — `disposeActiveFrame()` tears down the prior frame (guarded `detach()`/`remove()`/`dispose()`) before each open and on collapse, group-switch, list reload, and close. jsdom proves: 3 repeated opens keep at most one live frame (2 disposed, 1 live); collapsing the row and closing the manager each dispose the frame; and saving one field patches only its row — the untouched sibling row is the SAME DOM node (not rebuilt) and no list/scan request accompanies the reconcile (current-page DOM patch is truthful, no reload). 34 jsdom cases total; R1-D read-only invariant intact; media-manager lint clean; full PHP suite unchanged at 751/8,815 with the same six inherited failures; agent docs 54/418/0. Residual: real-browser memory/listener profiling over long sessions (D-049) | Repeated media remediation no longer accumulates frames/listeners, and a save updates only the affected row without reloading the table; real-browser profiling deferred | R2/R2-E3 |

| E-069 | 2026-08-18 | DBVC | `VisualEditorMediaManagerR2E2Test` (assign + replace); `EligibilityPolicy::assessPost`; `CapabilityManager::canEditDescriptor`/`canEditPostId` | R2-E2 security/stale/permission hardening for the end-to-end mutation paths (complements the R2-C/R2-F edge coverage). Proves both `assign()` and `replace()` fail closed AND leave content untouched when: a different user presents the user/blog-bound snapshot (`media_scan_expired_or_invalid`), the scan revision changed (`media_scan_revision_changed`), the owner is unpublished after the scan/read (`media_assignment_stale` / `media_replace_unavailable` — eligibility re-checked before write), and the object edit capability is revoked mid-flow via a `map_meta_cap` deny filter (surfaces as an eligibility failure before the write). Also confirms a non-existent attachment id is rejected without writing (`media_assignment_save_failed`) — no silent bad-id write. 9 tests/67 assertions; combined Media Manager PHP 67 tests/1,680 assertions; full suite 751/8,815 with the same six inherited failures; agent docs 54/418/0 | The assign/replace write paths fail closed for cross-user, stale-revision, lost-eligibility, revoked-capability, and invalid-attachment cases with the content provably unchanged | R2/R2-E2 |

| E-068 | 2026-08-18 | DBVC | ACF source `class-acf-field-group.php` (`prepare_field_for_db`/`load_value`/`update_value`); `AbstractAcfResolver` (`writeGroupedFieldValue`/`readGroupedFieldValue`/`getGroupWritePath`); `MediaFindingDescriptorBridge::buildDescriptor` (`group_write_path`, root selector); `VisualEditorMediaManagerGroupedFieldTest`; updated `VisualEditorMediaManagerR2ATest` | Corrected the group-nested ACF save. Field report: after the first fix the nested gallery/image saved with no error but did not persist (gone on refresh); R2-G's optimistic thumbnail masked it. Root cause proven from ACF source: a Group subfield is stored at `{group}_{subfield}` and only written correctly through the root group's `update_value` (`prepare_field_for_db` prefixes the name; `load_value` returns the group key-indexed). The first fix (leaf-key selector) wrote via `update_field(leaf_key)` to the bare, unprefixed meta ACF never reads. Corrected: the descriptor targets the ROOT group selector and carries `group_write_path` (the {key,name} chain to the leaf); `writeGroupedFieldValue` writes `update_field(root, [nested→leaf])` (ACF preserves siblings via `continue`) and `readGroupedFieldValue` reads back through the group, returning ok:false on mismatch so a non-persisting write errors honestly (which also removes the masking). Scoped to descriptors carrying `group_write_path` (Media Manager findings) — the main editor resolver path is unchanged. Verified: `VisualEditorMediaManagerGroupedFieldTest` (4 tests) proves the root-selector + `group_write_path` contract and that a grouped write never lands in a bare leaf meta; the R2-A grouped test now asserts the root-selector/write-path contract; combined Media Manager PHP 58 tests/1,613 assertions; full suite 742/8,748 with the same six inherited failures; agent docs 54/418/0. Residual: no real nested ACF in the test env, so ACF-happy-path persistence is proven from ACF source + the contract; live nested-group save re-test on the reporting site pending (RK-013) | Nested-group media fields now write/read through the parent group so values actually persist; a non-persisting write fails honestly instead of silently vanishing; live re-test pending | R2-F/fix |

| E-067 | 2026-08-18 | DBVC | `media-manager-app.js` (`savedSelectionPreview`/`applySavedPreviewFallback` in `reconcileAfterSave`; `createStatusPanel`/`createHeader`/`createBody` refactor); `media-manager.css` (`__header` column, `__header-top`, compact `__status-panel`); `releases/R2G-...`; `visual-editor-media-manager-state.test.cjs` | R2-G Media Manager UX polish (presentation only, no new REST/mutation/PHP surface). Slice 1: on a successful save the picked selection's thumbnail (already held client-side from `wp.media`) fills the reconciled field's `preview` when the server reread preview has no URL, so the left thumbnail reflects the saved media immediately without a reload — the server preview stays the source of truth. Slice 2: the status panel (state title/description/progress + scan action buttons) moved from the results body into the header as a compact `[content | actions]` grid with smaller buttons/labels and a narrow-width stack, reducing popup crowding. Verified: 2 new jsdom cases (30 total) — Slice 1 shows `pic-21-150x150` in the thumbnail with no list/scan call; Slice 2 asserts the panel is in `__header` (not `__body`) with the `data-*` state hooks and action button intact — plus a faithful static layout preview; media-manager lint clean; R1-D read-only invariant intact; full PHP suite unchanged at 741/8,737 with the same six inherited failures; agent docs 54/418/0 | The picked media populates the thumbnail without a reload, and the status panel/controls sit compactly in the header to declutter the popup, with the state machine, action visibility, and ARIA unchanged | R2-G |

| E-066 | 2026-08-18 | DBVC | `MediaFindingDescriptorBridge::buildDescriptor` (leaf-key write selector for grouped fields); `VisualEditorMediaManagerGroupedFieldTest`; updated `VisualEditorMediaManagerR2ATest` grouped-selector assertion | Field-report fix: a group-nested ACF gallery/image ("Assortment-Objects / Item/Object / Media") returned `400 "ACF field update did not succeed."` on assign and silently failed on retry. Root cause: the scan catalog's `selector` for a nested field is the ROOT group key (`AcfMediaFieldCatalog:319`) — correct for reading, wrong for writing — and `buildDescriptor` copied it into the descriptor's write identifier, so `AbstractAcfResolver::writeAcfValue` (which prefers the identifier for grouped sources) called `update_field(<root group key>, <gallery ids>)`. The fix targets the leaf `field_key` for grouped fields so `update_field()`/`get_field()` resolve the nested subfield, matching the main editor's `resolveAcfFieldSelector`; top-level fields (selector already === leaf key) are unchanged, and the single change fixes both assign (R2-C) and replace (R2-F Slice 3). `VisualEditorMediaManagerGroupedFieldTest` 3 tests/30 assertions prove the grouped gallery/image descriptor now targets `field_leaf_gallery`/`field_leaf_image` not `field_container`; the R2-A grouped-selector test was corrected from the old (buggy) root-key expectation. Full suite 741/8,737 with the same six inherited failures; agent docs 54/418/0. Note: the disposable PHPUnit env has no real nested ACF, so end-to-end grouped **save** is verified at the descriptor-targeting level; live nested-group confirmation on the reporting site is the residual gate | Group-nested ACF media fields now write to the correct subfield instead of the group root; the descriptor-targeting fix is proven and the live save is pending user re-test | R2-F/fix |

| E-065 | 2026-08-17 | DBVC | `MediaScanService` (`value_fingerprint`); `MediaScanReadModel` (`projectAssignedField` → `valueRef`/`replace`); `MediaFindingDescriptorBridge::resolveReplaceable`; `MediaAssignmentService::replace`/`applyMutation`; `MediaManagerController` (`/replacement` route + `handleReplaceFinding`); `api-client.js` (`replace()`); `media-manager-app.js` (`createFieldReplaceControls`/`beginReplaceMedia`/`saveReplacement`); `VisualEditorMediaManagerR2FReplaceTest` | R2-F Slice 3 (gated replace mutation). Populated image/gallery fields now carry an opaque `value_fingerprint` (`vemv_[a-f0-9]{24}`) surfaced as `valueRef`; the replace path re-resolves the owner from the snapshot, rescans in inventory mode, confirms the field is still populated, and enforces the expected-current-value precondition by `hash_equals` before writing — every non-writable outcome is a hard `WP_Error` (`media_replace_stale`, `media_replace_not_populated`, `media_replace_value_ref_invalid`). The write shares the R2-C `applyMutation` pipeline (validate → normalize → mutate → journal/audit/cache → `expandGroup` reconcile), overwrites the field reference only, and never deletes attachments. `VisualEditorMediaManagerR2FReplaceTest` 6 tests/62 assertions (success + fresh fingerprint + prior attachment retained + cache event; stale-ref block; emptied-after-read block; malformed-ref reject; non-image reject; gallery overwrite) plus 3 jsdom cases (control gating on `valueRef`; expected-current-value POST + reconcile; stale 409 retains the staged selection); full suite 738/8,705 with the same six inherited failures; media-manager lint clean; agent docs 54/418/0 (new `/replacement` route mapped); R1-D read-only invariant extended to the distinct gated endpoint and still forbids `fetch(`/`.save(`/composite-save | A populated media field can be deliberately overwritten only when its current value matches what the user read, the overwrite is journaled/audited/cache-invalidated, no attachment is deleted, and the write target is always the freshly server-resolved descriptor | R2-F/Slice 3 |

## Required Media Manager evidence

- Existing scanner/job/session implementation
- Existing ACF field catalog/location matcher
- Existing public object enumerator/capability filter
- Existing featured-image descriptor/editor/mutation
- Existing ACF image descriptor/editor/mutation
- Existing ACF gallery descriptor/editor/mutation
- Existing nested path support
- Existing Media Library frame lifecycle
- Existing composite/multi-field save pattern
- Existing journal/cache/revalidation hooks
- Existing large-table/pagination/virtualization components
- VerticalFramework Media Health/missing-file scanner evidence
- VerticalFramework ACF image/gallery catalogs and exact JSON paths

## Required Brand/Workspace evidence

- Existing Shared Globals configuration and flow
- Registry-like structures
- Option owner canonical representation
- Current option-family support
- Toolbar/overflow/state conventions
- Go To Object/object navigation APIs
- CSS/z-index/focus conventions
- Tests/fixtures/QA notes

## Evidence quality

Strong evidence includes:

- exact path and symbol;
- relevant line/function/class;
- commit or working-tree status;
- test proving behavior;
- captured command output;
- representative fixture/data count.

Avoid vague entries such as “image support exists” without the actual resolver/control/write path.
